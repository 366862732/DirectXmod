# GL4DX12 - Minecraft DirectX 12 渲染桥接模组

## 🎯 最终目标

**实现类似 VulkanMod 的效果：让显卡负载吃满，替代 Minecraft 原生的 OpenGL 渲染管线，将全部 3D 渲染工作交由 Rust + wgpu (DX12) 后端处理。**

当前架构是过渡方案：Surface 模式下 MC 正常 GL 渲染 → GameRendererMixin TAIL 捕获 framebuffer → D3D12 纹理 → swapchain present。最终目标是让 Rust/DX12 直接渲染 MC 区块几何体，消除 GL 帧捕获中间层，实现零读回、零纹理上传的 VulkanMod 级性能。

---

## 📋 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                   Minecraft 26.1.2                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Fabric Loader 0.19.3                      │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │           Fabric API (ClientTickEvents)          │  │  │
│  │  │  Tick Callback → 全速渲染 → Rust renderFrame()   │  │  │
│  │  │  + 相机 MVP 矩阵提取 → nativeUpdateCamera()      │  │  │
│  │  │  + 相机位置传递 → nativeUpdateCameraPos()        │  │  │
│  │  │  + 区块网格上传 → nativeUploadChunkMesh()        │  │  │
│  │  │  + 地形图集上传 → nativeUploadTerrainAtlas()     │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         Mixins (SpongePowered)                    │  │  │
│  │  │  GameRendererMixin: HEAD 始终 pass through       │  │  │
│  │  │               TAIL: 无chunks → glReadPixels → D3D12│  │  │
│  │  │               TAIL: 有chunks → 跳过              │  │  │
│  │  │               TAIL: Offscreen → Dx12Mod.onPostRender()│  │
│  │  │  MinecraftMixin: runTick TAIL → GL解除 → D3D12 Present│  │
│  │  │  GlDeviceMixin: HEAD 抑制 GL swap                │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                           ↕ JNI                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              wgpu_mc_jni.dll (Rust)                    │  │
│  │  nativeInit() → 初始化日志 + 创建 WmRenderer           │  │
│  │  nativeSetWindow(HWND) → 创建 D3D12 Surface/Swapchain  │  │
│  │  nativeRenderFrame() → Surface 模式: Present 到窗口    │  │
│  │                     → Offscreen: 返回 byte[] 像素数据  │  │
│  │  nativeResize(width, height) → 懒 resize               │  │
│  │  nativeUpdateCamera(float[16]) → 同步相机 MVP 矩阵     │  │
│  │  nativeUpdateCameraPos(x,y,z) → 同步相机世界坐标       │  │
│  │  nativeSetFramePixels(byte[], w, h) → Surface 接收 GL 帧│  │
│  │  nativeUploadChunkMesh(...) → 上传 MC 区块网格         │  │
│  │  nativeClearChunkSection(...) → 清除旧区块网格         │  │
│  │  nativeUploadTerrainAtlas(buffer, w, h) → 上传地形图集 │  │
│  │  nativeHasSurface() → 是否 Surface 模式                │  │
│  │  nativeHasChunkGeometry() → 是否已上传区块网格         │  │
│  │  nativeIsReady() / nativeGetStatus() → 状态查询        │  │
│  └───────────────────────────────────────────────────────┘  │
│                           ↕                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              wgpu-mc (Rust)                            │  │
│  │  wgpu::Instance(DX12) → Adapter → Device + Queue       │  │
│  │  Surface 模式: 三种渲染路径                             │  │
│  │    1. 有 chunk 几何 → CHUNK_SHADER_SRC 直接渲染 MC 场景 │  │
│  │    2. 有 GL 帧 → TEX_SHADER_SRC textured quad 呈现    │  │
│  │    3. 无数据 → SHADER_SRC 测试几何体 (地面+立方体)     │  │
│  │  Offscreen 模式: 三缓冲 → render → readback → Vec<u8> │  │
│  │  Terrain Atlas: CPU mip chain + GPU 采样               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**核心设计原则（当前版本）：**
- 使用 Mixin 注入 Minecraft 渲染循环（GameRenderer + Minecraft + GlDevice）
- 在 MC 窗口 HWND 上直接创建 D3D12 swapchain（无子窗口）
- **GL 上下文临时解除机制**：D3D12 Present 前通过 `glfwMakeContextCurrent(0)` 解除 GL 上下文，避免 WDDM 驱动层 HWND 争用导致 GPU TDR
- **Surface 模式智能渲染**：有 chunk 几何 → D3D12 直接渲染 MC 场景；无 chunk → GL 帧捕获 → D3D12 纹理呈现
- **Offscreen 模式**：三缓冲异步读回 + PBO 纹理上传（title screen / 初始化阶段）
- Rust wgpu 引擎完全独立于 Minecraft，通过 JNI 通信

---

## ✅ 第一阶段：JNI 通信链路

**状态：已完成**

### 1.1 环境准备

```powershell
# 1. 安装 Rust
rustup default stable
rustup component add rust-analyzer rust-src

# 2. JDK 25（Windows 从 Adoptium 下载）

# 3. 配置 JAVA_HOME
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-25.0.x"
```

### 1.2 项目结构

```
dx12-lib-template-26.1.2/
├── fabric/                          # Fabric 模组（Java）
│   ├── src/main/java/com/dx12/
│   │   ├── Dx12Mod.java            # 模组入口，注册事件回调
│   │   ├── D3D12Bridge.java        # JNI 桥接层
│   │   └── mixin/
│   │       ├── MinecraftMixin.java      # runTick TAIL: GL解除→D3D12 Present
│   │       ├── GameRendererMixin.java   # HEAD pass through / TAIL 帧捕获
│   │       └── GlDeviceMixin.java       # HEAD 抑制 GL swap
│   ├── src/main/resources/
│   │   ├── fabric.mod.json         # Fabric 模组描述
│   │   └── gl4dx12.mixins.json     # Mixin 配置
│   ├── build.gradle                # Gradle 构建配置
│   └── gradle.properties           # 版本参数
├── rust/
│   ├── Cargo.toml                  # Workspace 配置
│   ├── wgpu-mc/                    # 核心渲染库
│   │   ├── src/lib.rs              # WmRenderer + 双模渲染 + Chunk 几何
│   │   └── Cargo.toml              # wgpu 23, raw-window-handle, bytemuck, image
│   └── wgpu-mc-jni/                # JNI 桥接层
│       ├── src/lib.rs              # 14 个 native 方法
│       └── Cargo.toml              # jni 0.21, log, env_logger
└── 步骤.md                         # 本文档
```

### 1.3 Rust 依赖配置

**`rust/wgpu-mc-jni/Cargo.toml`**：
```toml
[package]
name = "wgpu-mc-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
log = "0.4"
env_logger = "0.10"
wgpu-mc = { path = "../wgpu-mc" }
```

**`rust/wgpu-mc/Cargo.toml`**：
```toml
[package]
name = "wgpu-mc"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "23"
bytemuck = { version = "1.14", features = ["derive"] }
log = "0.4"
env_logger = "0.10"
futures = "0.3"
raw-window-handle = "0.6"
image = { version = "0.25", features = ["png"] }
```

### 1.4 Rust JNI 桥接层（14 个 native 方法）

**`rust/wgpu-mc-jni/src/lib.rs`**：

```rust
use std::sync::{Mutex, MutexGuard};
type RendererResult = Option<Result<wgpu_mc::WmRenderer, String>>;
static RENDERER: Mutex<RendererResult> = Mutex::new(None);

// 1.  nativeInit() — 模组启动时创建 WmRenderer（GL 未绑定，安全）
// 2.  nativeHello(String) — 测试 JNI 字符串通信
// 3.  nativeTestDeviceInfo() — 返回 GPU 可用性信息
// 4.  nativeSetWindow(long hwnd) — 创建 D3D12 Surface/Swapchain
// 5.  nativeRenderFrame() → byte[] — 渲染一帧（Surface 返回空，Offscreen 返回像素）
// 6.  nativeResize(int width, int height) — 更新渲染器窗口尺寸（懒 resize）
// 7.  nativeUpdateCamera(float[16]) — 传入相机 MVP 矩阵（列主序）
// 8.  nativeUpdateCameraPos(float x, y, z) — 传入相机世界坐标
// 9.  nativeSetFramePixels(byte[], w, h) — Surface 模式接收 GL 捕获的像素
// 10. nativeUploadChunkMesh(sectionXYZ, buffer, verts, stride) — 上传 MC 区块网格
// 11. nativeClearChunkSection(sectionXYZ) — 清除旧区块网格
// 12. nativeUploadTerrainAtlas(buffer, w, h) — 上传地形纹理图集 + mip chain
// 13. nativeHasSurface() → boolean — 是否 Surface 模式
// 14. nativeHasChunkGeometry() → boolean — 是否已上传区块网格
// 15. nativeIsReady() → int — 渲染器状态（1=就绪, 0=初始化中, -1=失败）
// 16. nativeGetStatus() → String — 人类可读状态字符串
```

关键实现：
- `lock_or_poisoned()` 安全锁：从 poison 状态恢复，防止 render_frame panick 后级联 JVM 崩溃
- `nativeInit()` 中 `catch_unwind` 捕获 panic，存储为 `Err(String)` 而非传播
- `nativeSetWindow` 调用 `renderer.init_surface(hwnd)` 创建 surface
- `nativeRenderFrame` 中 `catch_unwind` 捕获 panic，返回 null 而非崩溃
- `nativeUploadChunkMesh()` — 解析 MC 顶点数据（36 字节/顶点：Pos(12)+Color(4)+UV(8)+UV2(4)+Normal(4)+Padding(4)），转换为 ChunkVertex（32 字节），世界坐标偏移
- `nativeUploadTerrainAtlas()` — CPU 生成完整 mip chain，保存 atlas 像素用于诊断

### 1.5 Java JNI 桥接层

**`D3D12Bridge.java`** — 封装 native 方法 + 业务逻辑：

```java
// Native 方法声明（16 个）
public static native void nativeInit();
public static native String nativeHello(String input);
public static native String nativeTestDeviceInfo();
public static native void nativeSetWindow(long hwnd);
public static native byte[] nativeRenderFrame();
public static native void nativeResize(int width, int height);
public static native void nativeUpdateCamera(float[] matrix);
public static native void nativeUpdateCameraPos(float x, float y, float z);
public static native void nativeSetFramePixels(ByteBuffer buffer, int width, int height);
public static native void nativeUploadChunkMesh(int sectionX, int sectionY, int sectionZ,
    ByteBuffer buffer, int vertexCount, int vertexStride);
public static native void nativeClearChunkSection(int sectionX, int sectionY, int sectionZ);
public static native void nativeUploadTerrainAtlas(ByteBuffer buffer, int width, int height);
public static native boolean nativeHasSurface();
public static native boolean nativeHasChunkGeometry();
public static native int nativeIsReady();
public static native String nativeGetStatus();
```

关键实现：
- `getWindowHandle()` 使用 `GLFW.glfwGetCurrentContext()` + `GLFWNativeWin32.glfwGetWin32Window()` 获取 HWND
- `renderFrame()` 使用 `BufferUtils.createByteBuffer(paddedSize)` + 4KB padding 防止 NVIDIA 驱动 DMA 越界
- DLL 从 JAR 提取到 `{user.dir}/dx12mod/wgpu_mc_jni.dll`，支持版本隔离

### 1.6 Fabric 模组入口

**`Dx12Mod.java`** — 实现 `ClientModInitializer`：

```java
@Override
public void onInitializeClient() {
    D3D12Bridge.init();
    D3D12Bridge.sayHello("Hello from Minecraft!");
    D3D12Bridge.getDeviceInfo();

    // Tick Callback: 相机提取 + 窗口同步（Surface 模式下跳过 renderFrame）
    ClientTickEvents.END_CLIENT_TICK.register(client -> {
        // 1. 获取 GLFW 窗口尺寸
        // 2. 资源重载检测（tick 间隔 > 2s）
        // 3. 启动延迟：3s + 2s 缓冲
        // 4. 提取相机 MVP 矩阵（inWorld 检测：mc.player != null && mc.level != null）
        // 5. 提取相机世界坐标 → nativeUpdateCameraPos()
        // 6. syncWindowSize(width, height)
        // 7. Surface 模式且 inWorld → return（renderFrame 由 MinecraftMixin 调用）
        // 8. Offscreen 模式 → renderFrame → pendingPixels（供 GameRendererMixin TAIL 使用）
    });
}
```

### 1.7 构建配置

**`fabric/build.gradle`**：
```gradle
plugins {
    id 'fabric-loom' version '1.10.3'
    id 'maven-publish'
}

dependencies {
    minecraft "com.mojang:minecraft:26.1.2"
    mappings "net.fabricmc:yarn:26.1.2+build.1:v2"
    modImplementation "net.fabricmc:fabric-loader:0.19.3"
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.154.2+26.1.2"
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = 25
}
```

### 1.8 验收结果

```
[INFO] GL4DX12 Mod initializing...
[D3D12Bridge] Native library loaded from: ...\dx12mod\wgpu_mc_jni.dll
[D3D12Bridge] Rust JNI library initialized.
[INFO] Rust responded: Hello from Rust wgpu! You said: Hello from Minecraft!
[INFO] Device info: wgpu-mc-jni loaded. DX12: READY
[dx12-wm] Creating WmRenderer 800x600 (triple-buffer + surface support)
[dx12-wm] Adapter: NVIDIA GeForce RTX 3080
[dx12-wm] Device created OK
[dx12-wm] Offscreen slots created (800x600)
[INFO] GL4DX12 Mod initialized!
```

进入游戏中，当 HWND 可用后会看到：

```
[dx12-wm] Surface OK! HWND=0x123456 fmt=Rgba8UnormSrgb 1920x1080
```

此时渲染模式自动切换到 **Surface 模式**（world 中）。当 chunk 几何上传后，D3D12 直接渲染 MC 场景；否则 GL 帧捕获 → D3D12 纹理呈现。

---

## ✅ 第二阶段：wgpu 渲染引擎骨架（3D 几何管线 + Chunk 渲染）

**状态：已完成**

### 2.1 WmRenderer 核心结构

**`rust/wgpu-mc/src/lib.rs`**：

```rust
pub struct WmRenderer {
    instance: wgpu::Instance,
    adapter: wgpu::Adapter,
    device: wgpu::Device,
    queue: wgpu::Queue,
    width: u32,
    height: u32,
    pub camera_mvp: [[f32; 4]; 4],
    camera_prev: [[f32; 4]; 4],
    camera_target: [[f32; 4]; 4],
    camera_pos: [f32; 3],

    // 标准 3D 管线
    pipeline: wgpu::RenderPipeline,
    bind_group: wgpu::BindGroup,
    uniform_buffer: wgpu::Buffer,
    plane_vb: wgpu::Buffer,
    plane_ib: wgpu::Buffer,
    plane_count: u32,
    cube_vbs: Vec<wgpu::Buffer>,
    cube_ib: wgpu::Buffer,
    cube_count: u32,

    // Surface 模式
    surface: Option<wgpu::Surface<'static>>,
    surface_config: Option<wgpu::SurfaceConfiguration>,
    surface_format: wgpu::TextureFormat,
    surface_depth: Option<wgpu::Texture>,
    resize_pending: bool,

    // Textured fullscreen quad (GL framebuffer → D3D12)
    tex_pipeline: wgpu::RenderPipeline,
    tex_bind_group: Option<wgpu::BindGroup>,
    tex_sampler: wgpu::Sampler,
    frame_texture: Option<wgpu::Texture>,
    fs_quad_vb: wgpu::Buffer,
    fs_quad_ib: wgpu::Buffer,

    // Offscreen 模式（三缓冲）
    slots: [Slot; RING_SIZE],
    idx: usize,
    pending_rx: [Option<mpsc::Receiver>; RING_SIZE],
    prev_pixels: Vec<u8>,

    // Chunk 几何（Phase 7: native MC 区块渲染）
    chunk_meshes: HashMap<(i32,i32,i32), Vec<ChunkMesh>>,
    has_chunk_geometry: bool,

    // Chunk 纹理管线
    chunk_shader: Option<wgpu::ShaderModule>,
    chunk_pipeline: Option<wgpu::RenderPipeline>,
    chunk_bind_group: Option<wgpu::BindGroup>,
    chunk_bind_group_layout: Option<wgpu::BindGroupLayout>,
    atlas_texture: Option<wgpu::Texture>,
    atlas_sampler: wgpu::Sampler,
    atlas_width: u32,
    atlas_height: u32,
    atlas_pixels: Option<Vec<u8>>,
}
```

### 2.2 WGSL Shaders

**主 Shader（测试几何体）**：
```wgsl
struct CameraUniform {
    view_proj: mat4x4<f32>,
    camera_pos: vec3<f32>,
}
@vertex fn vs_main(pos: vec3<f32>, color: vec3<f32>) -> VertexOutput {
    let world_pos = pos + camera.camera_pos;
    out.position = camera.view_proj * vec4<f32>(world_pos, 1.0);
    out.color = color;
}
```

**Chunk Shader（MC 区块渲染）**：
```wgsl
@group(0) @binding(1) var atlas: texture_2d<f32>;
@group(0) @binding(2) var atlas_sampler: sampler;
@vertex fn vs_main(pos: vec3<f32>, color: vec3<f32>, uv: vec2<f32>) -> VertexOutput {
    out.uv = uv;
    out.tint = color;
}
@fragment fn fs_main(in: VertexOutput) -> vec4<f32> {
    let base = textureSample(atlas, atlas_sampler, in.uv);
    return vec4(base.rgb * in.tint, base.a);
}
```

**Textured Quad Shader（GL 帧捕获呈现）**：
```wgsl
@fragment fn fs_main(in: VertexOutput) -> vec4<f32> {
    var uv = in.uv;
    uv.y = 1.0 - uv.y;  // Flip Y: GL bottom-up → wgpu top-down
    return textureSample(tex, samp, uv);
}
```

注意：Push constants 已移除——部分 GPU 不支持。模型变换预烘焙到顶点缓冲。

### 2.3 几何体生成

| 几何体 | 顶点数 | 索引数 | 描述 |
|--------|--------|--------|------|
| 地平面 | 4 | 6 | 200x200 绿色平面 |
| 立方体 | 24 (×5 位置) | 36 (共享 IB) | 橙色立方体，6 面双色 |
| Chunk 顶点 | 可变 | 可变 | MC GL_QUADS → D3D12 TriangleList，32 字节/顶点 |
| Terrain Atlas | 可变 | - | CPU mip chain，GPU 采样 |

### 2.4 渲染流程

**Surface 模式（智能渲染，三种路径）**：
```rust
render_surface() {
    // 1. Lazy resize: resize_pending 时 reconfigure swapchain
    // 2. get_current_texture() → Lost/Outdated → 自动 reconfigure
    // 3. 根据可用性选择渲染路径:
    //    a. 有 chunk 几何 → draw_chunks() (CHUNK_SHADER_SRC)
    //    b. 有 GL 帧 → draw textured quad (TEX_SHADER_SRC)
    //    c. 无数据 → draw_scene() (SHADER_SRC 测试几何体)
    // 4. queue.submit + frame.present()
    // 5. panic::catch_unwind 确保 frame 始终 present
}
```

**Offscreen 模式**：
```rust
render_offscreen() {
    // 1. 更新相机 MVP + lerp 平滑
    // 2. 三缓冲 Ring 轮转
    // 3. begin_render_pass → draw_scene → end
    // 4. copy_texture_to_buffer (async)
    // 5. queue.submit + poll
    // 6. 读回 staging buffer → Vec<u8>
}
```

### 2.5 Chunk 网格上传

**`upload_chunk_mesh()`** 解析 MC 顶点数据：
- MC 26.1.2 BLOCK 格式：36 字节/顶点（Pos(12) + Color(4) + UV0(8) + UV2(4) + Normal(4) + Padding(4)）
- 转换为 `ChunkVertex`：32 字节（Pos(12) + Color(12) + UV(8)）
- 世界坐标：section_origin + local_pos
- UV 范围 clamp 到 [0, 1]
- 支持 u16/u32 索引（`index_is_u32` 字段）
- 诊断日志：dump 第一个 chunk 的顶点数据、UV 范围、atlas 像素

### 2.6 Terrain Atlas 上传

**`upload_terrain_atlas()`**：
- CPU 生成完整 mip chain（box filter 降采样）
- 保存原始像素到 `atlas_pixels` 用于诊断
- 输出 `atlas_debug.png` 到项目目录
- 创建 GPU 纹理 + bind_group（camera + atlas_texture + atlas_sampler）
- 调用 `ensure_chunk_pipeline()` 重建 chunk pipeline

### 2.7 验收结果

- Rust wgpu 成功初始化 DX12 adapter（NVIDIA GeForce RTX 4070）
- Surface 模式：三种渲染路径智能切换（chunk/GL 帧/测试几何体）
- Offscreen 模式：三缓冲异步读回
- 相机视角随 MC 移动实时更新（MVP 矩阵 + LERP 平滑）
- Chunk 几何上传：MC GL_QUADS → D3D12 TriangleList
- Terrain atlas 上传：CPU mip chain + GPU 采样

---

## ✅ 第三阶段：Fabric 事件系统 + Mixin 集成

**状态：已完成**

### 3.1 架构设计

当前使用 **ClientTickEvents + Mixin 组合**：

| 组件 | 职责 | 注入点 |
|------|------|--------|
| `ClientTickEvents.END_CLIENT_TICK` | 相机提取、窗口同步、尺寸更新 | Fabric API 事件 |
| `GameRendererMixin` | HEAD: 始终 pass through / TAIL: Surface 帧捕获或 Offscreen PBO 上传 | Mixin |
| `MinecraftMixin` | TAIL: GL 上下文解除 → D3D12 Surface 创建 + Present | Mixin |
| `GlDeviceMixin` | HEAD: 抑制 GL swap（避免与 D3D12 Present 冲突） | Mixin |

### 3.2 Tick Callback 逻辑

```java
ClientTickEvents.END_CLIENT_TICK.register(client -> {
    // 1. 获取 GLFW 窗口尺寸
    // 2. 资源重载检测（tick 间隔 > 2s）
    // 3. 启动延迟：3s + 2s 缓冲
    // 4. 提取相机 MVP 矩阵（inWorld 检测：mc.player != null && mc.level != null）
    // 5. 提取相机世界坐标 → nativeUpdateCameraPos()
    // 6. syncWindowSize(width, height)
    // 7. Surface 模式且 inWorld → return（renderFrame 由 MinecraftMixin 调用）
    // 8. Offscreen 模式 → renderFrame → pendingPixels（供 GameRendererMixin TAIL 使用）
});
```

### 3.3 GameRendererMixin 逻辑

```java
// HEAD: 始终 pass through — 让 GL 正常渲染（HUD/UI 兼容）
@Inject(method = "render", at = @At("HEAD"), cancellable = true)
private void onRenderHead(CallbackInfo ci) {
    // Pass through — let GL render everything for UI/HUD compatibility.
}

// TAIL: 根据模式决定行为
@Inject(method = "render", at = @At("TAIL"))
private void onRenderTail(CallbackInfo ci) {
    if (D3D12Bridge.hasSurface()) {
        if (!D3D12Bridge.hasChunkGeometry()) {
            captureFramebufferForD3D12();  // GL 帧捕获 → D3D12 纹理
        }
        // 有 chunk 时跳过 — D3D12 直接渲染 chunk
    } else {
        Dx12Mod.onPostRender();  // Offscreen 模式: PBO 上传 + 全屏 quad
    }
}
```

**`captureFramebufferForD3D12()`**：
- FBO-aware：从 MC 实际 draw FBO 读取（而非 GL_BACK）
- `GL30.glBindFramebuffer(GL_READ_FRAMEBUFFER, oldDrawFbo)` → `glReadBuffer(GL_COLOR_ATTACHMENT0)`
- `glReadPixels(0, 0, w, h, GL_RGBA, GL_UNSIGNED_BYTE, buffer)`
- `D3D12Bridge.setFramePixels(buffer, w, h)` → Rust 上传到 D3D12 纹理

### 3.4 GL 上下文解除机制（Surface 模式核心）

**`MinecraftMixin.java`**：

```java
@Inject(method = "runTick", at = @At("TAIL"))
private void onRunTickTail(CallbackInfo ci) {
    if (mc.level != null && mc.player != null) {
        // 1. 在解除 GL 上下文之前获取 HWND
        long glfwWindow = GLFW.glfwGetCurrentContext();
        long hwnd = GLFWNativeWin32.glfwGetWin32Window(glfwWindow);

        // 2. 解除 GL 上下文（避免 DXGI ↔ WGL HWND 争用）
        GLFW.glfwMakeContextCurrent(0);

        try {
            // 3. 创建/激活 D3D12 Surface
            if (hwnd != 0) D3D12Bridge.setWindow(hwnd);
            // 4. D3D12 Present 到窗口
            if (D3D12Bridge.hasSurface()) D3D12Bridge.renderFrame();
        } finally {
            // 5. 重绑 GL 上下文
            GLFW.glfwMakeContextCurrent(glfwWindow);
        }
    }
}
```

### 3.5 验收结果

- 模组加载成功，Mixin 注入正常（3/3）
- Surface 模式正常创建 D3D12 swapchain
- Offscreen 模式回落正常
- 正常退出无崩溃
- Chunk 几何上传 + Terrain atlas 上传正常

---

## ✅ 第四阶段：3D 场景渲染基础

**状态：已完成**

### 4.1 实现内容

- 3D 几何管线：WGSL shader + camera_pos + 深度测试 + 背面剔除
- 深度缓冲区：`Depth32Float` 格式，正确遮挡关系
- 地面平面网格：200x200 绿色平面
- 5 个彩色立方体：每个独立 VB（预烘焙偏移），共享 IB
- 相机 MVP 传递：Java 提取 MC 相机视角 → `nativeUpdateCamera(float[16])` → Rust 实时同步（带 LERP 平滑）
- 相机位置传递：Java 提取 MC 玩家世界坐标 → `nativeUpdateCameraPos()` → Rust 偏移几何体
- PBO 纹理上传（Offscreen 模式）：绕过 NVIDIA 驱动 DMA 页边界 crash
- 三缓冲异步渲染（Offscreen 模式）：持续 GPU 管线
- `lock_or_poisoned()`：防止 panic 级联 JVM 崩溃

### 4.2 稳定性修复历史

| 修复 | 原因 | 方案 | 状态 |
|------|------|------|------|
| Esc 菜单崩溃 | HUD callback GL 状态与 Screen 渲染冲突 | `currentScreen != null` 时跳过 GL 绘制 | ✅ |
| 纹理闪烁 | 窗口 resize 后纹理尺寸不匹配 | `texWidth`/`texHeight` 追踪 + 自动重建 | ✅ |
| ByteBuffer 页边界崩溃 | NVIDIA 驱动 DMA 越界 | PBO + 4KB padding | ✅ |
| MemoryUtil.memAlloc 崩溃 | LWJGL allocator 不兼容 | 改用 `BufferUtils.createByteBuffer` | ✅ |

---

## ✅ 第五阶段：性能优化

**状态：已完成**

| 优化项 | 方案 | 状态 |
|--------|------|------|
| 三缓冲异步渲染 | Ring(3) 轮转 + map_async + mpsc (Offscreen) | ✅ |
| Push constants → baked vertices | 模型变换预烘焙到顶点缓冲，兼容所有 GPU | ✅ |
| 相机平滑插值 | `mat4_lerp(prev, target, 0.3)` | ✅ |
| Resize 平滑过渡 | 懒 resize + 尺寸不匹配跳过 | ✅ |
| Poison mutex 恢复 | `lock_or_poisoned()` | ✅ |
| Surface resize 保护 | 不在 `resize()` 中 reconfigure，在 `render_surface()` 中检测 Lost/Outdated | ✅ |
| Chunk pipeline 动态格式 | `ensure_chunk_pipeline()` 使用 `surface_format` | ✅ |
| Chunk 渲染 panic 保护 | `std::panic::catch_unwind` 确保 frame 始终 present | ✅ |
| Chunk mesh u32 索引 | 大数据区块支持 u32 索引 | ✅ |

---

## ✅ 第六阶段：D3D12 Surface 模式（原生 Swapchain）

**状态：已完成**

### 6.1 目标

在 MC 窗口 HWND 上直接创建 D3D12 swapchain，绕过 OpenGL 回读/上传，实现 DX12 直接 Present 到窗口。类似 VulkanMod 的渲染接管方式。

### 6.2 D3D12 Surface 创建

**`rust/wgpu-mc/src/lib.rs` — `init_surface()`**：

```rust
fn init_surface(&mut self, hwnd: usize) {
    let surface = create_surface_from_hwnd(&self.instance, hwnd);
    // 使用 raw-window-handle: Win32WindowHandle + WindowsDisplayHandle

    // 优先匹配 Rgba8UnormSrgb（与 pipeline 格式一致）
    let format = caps.formats.iter()
        .find(|f| **f == wgpu::TextureFormat::Rgba8UnormSrgb)
        .or_else(|| caps.formats.iter().find(|f| f.is_srgb()))
        .copied()
        .unwrap_or(caps.formats[0]);

    // 配置 surface
    surface.configure(&self.device, &config);

    // 创建并缓存 depth texture（每帧复用）
    self.surface_depth = Some(make_depth_texture(...));
}
```

### 6.3 HWND 共享冲突解决方案（GPU TDR 预防）

**问题根因**：D3D12 (DXGI) 和 OpenGL (WGL) 同时对同一 HWND 的呈现操作导致 WDDM 驱动层面争用，触发 GPU Timeout Detection & Recovery (TDR)，画面冻结 ~2s。

**经历方案**：
| 尝试 | 方案 | 结果 |
|------|------|------|
| 第1次 | GlDeviceMixin 抑制 GL swap | ❌ Mixin 注入成功但 TDR 仍发生 |
| 第2次 | 子窗口方案 (WS_CHILD + STATIC) | ❌ 白屏 — 子窗口覆盖 MC 窗口 |
| 第3次 | GL 上下文临时解除 + 重绑 | ✅ 解决 |

**最终方案**：
1. `MinecraftMixin.runTick TAIL` 中 `GLFW.glfwMakeContextCurrent(0)` 临时解除 GL 上下文
2. 在解除状态下创建 D3D12 Surface + 执行 Present
3. `try-finally` 保证 GL 上下文重绑
4. `GlDeviceMixin` 取消 `presentFrame()` 的 GL swap

### 6.4 关键 Bug 修复记录

| 日期 | Bug | 原因 | 修复 |
|------|-----|------|------|
| 2026-07-11 | 标题界面卡死（9秒+） | 标题界面创建 surface 导致驱动争用 | `inWorld` 守卫：只在地图中创建 surface |
| 2026-07-11 | C++ 异常崩溃 | `resize()` 中 `surface.configure()` 触发 DXGI ResizeBuffers 异常 | 懒 resize：不在 resize() 中配置 surface，等 Lost/Outdated 时处理 |
| 2026-07-11 | 进入世界白屏 | 子窗口 (WS_CHILD) 覆盖 MC 窗口 + 未渲染到子窗口 | 回退子窗口，改用 GL 上下文解除方案 |
| 2026-07-11 | 卡加载世界（15秒无响应） | `setWindow()` 在 GL 上下文绑定状态调用 `surface.configure()` | 将 `setWindow` 移至 MinecraftMixin 的 GL 解除区域内 |
| 2026-07-11 | `glfwGetCurrentContext returned 0` 刷屏 | 先解除 GL 再调用 `getWindowHandle()`，而该方法内部依赖 `glfwGetCurrentContext()` | 在解除 GL 之前捕获 HWND：`GLFWNativeWin32.glfwGetWin32Window(glfwWindow)` |
| 2026-07-11 | `Bgra8UnormSrgb != Rgba8UnormSrgb` panic | Pipeline 格式 Rgba8 不匹配 Surface 格式 Bgra8 | 优先匹配 `Rgba8UnormSrgb` surface 格式 |
| 2026-07-11 | `Surface image already acquired` 链式 panic | 格式不匹配导致首帧 panic → `present()` 未执行 → 纹理永久锁定 | 格式修复后自动解决 |
| 2026-07-11 | 测试几何体不可见（浅蓝色全屏） | 几何体在世界原点，玩家生成在 ~100 单位外 | 改用全屏三角形（NDC 空间），始终可见 |

### 6.5 Surface 模式验证

**最终测试结果**：
- D3D12 swapchain 成功创建在 MC 窗口 HWND ✅
- 智能渲染三种路径：chunk 几何 → D3D12 直接渲染 / GL 帧捕获 → textured quad / 测试场景 ✅
- 无 GPU TDR ✅
- 无崩溃 ✅
- 正常退出 ✅

### 6.6 注意事项

- Surface 模式下 `GameRendererMixin` HEAD 始终 pass through（让 GL 正常渲染）
- Surface 模式下 TAIL：有 chunk 时跳过 GL，无 chunk 时捕获 framebuffer
- Offscreen 模式下 TAIL：调用 `Dx12Mod.onPostRender()` 上传 PBO
- Surface 创建必须在 GL 上下文解除后，否则 DXGI 与 WGL 争用 HWND

---

## ❌ 第七阶段：实际 Minecraft 场景渲染（VulkanMod 级全接管）

**状态：进行中**

### 7.1 当前进展

已经实现了：
- ✅ Chunk 几何上传：`nativeUploadChunkMesh()` — MC GL_QUADS → D3D12 TriangleList
- ✅ Terrain atlas 上传：`nativeUploadTerrainAtlas()` — CPU mip chain + GPU 采样
- ✅ Chunk 渲染管线：`CHUNK_SHADER_SRC` — texture atlas UV 采样 + vertex color 光照 tint
- ✅ Surface 模式智能渲染：有 chunk 几何时 D3D12 直接渲染 MC 场景

### 7.2 剩余工作

还需要：
- 从 MC 获取完整的区块网格数据（当前仅手动上传测试数据）
- 处理方块纹理/mipmap
- 处理光照、天空、实体、粒子
- 半透明排序 + 后处理特效

### 7.3 技术路线

1. **Mixin 注入 Chunk Mesh 提取** — 在 MC 构建区块网格后拦截数据
2. **JNI 批量传输** — 使用 `ByteBuffer` 传输顶点/索引/纹理数据
3. **Rust wgpu 本地渲染** — D3D12 直接渲染所有几何体到 surface
4. **UI/HUD 回退** — MC 原生 OpenGL 渲染 UI 层叠

### 7.4 实现优先级

1. **区块渲染**（性能收益最大）— 预计算 Chunk Mesh + 顶点缓冲 ✅ 已开始
2. **天空盒与云雾** — 简单着色器
3. **实体渲染** — 模型加载 + 骨骼动画
4. **粒子系统** — 点精灵（point sprites）
5. **半透明排序 + 后处理特效**

---

## 🔧 部署说明

### 构建步骤

```powershell
# 1. 构建 Rust DLL
cd rust
cargo build --release
# 输出: target/release/wgpu_mc_jni.dll

# 2. 构建 Fabric 模组
cd fabric
gradlew build
# 输出: build/libs/gl4dx12-*.jar

# 3. 部署到 Minecraft
copy fabric\build\libs\gl4dx12-*.jar "$env:APPDATA\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\"
copy rust\target\release\wgpu_mc_jni.dll "$env:APPDATA\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\"
```

### 部署路径

```
D:\.minecraft\versions\26.1.2-Fabric_0.19.3\
├── mods\
│   └── gl4dx12-*.jar       # Fabric 模组 JAR
└── dx12mod\
    └── wgpu_mc_jni.dll     # Rust JNI DLL（自动提取）
```

---

## 📊 项目进度更新（2026-07-11）

### ✅ 已完成的工作节点

| 节点 | 内容 | 完成日期 |
|------|------|----------|
| Phase 1 | JNI 通信链路（16 个 native 方法） | 2026-07-09 |
| Phase 2 | wgpu DX12 渲染引擎 + 3D 几何管线 | 2026-07-09 |
| Phase 3 | Fabric 事件系统 + Mixin 注入（3 个 Mixin） | 2026-07-10 |
| Phase 4 | 3D 场景渲染基础 + PBO 纹理上传 | 2026-07-10 |
| Phase 5 | 性能优化（三缓冲、懒 resize、lock_or_poisoned） | 2026-07-10 |
| Phase 6 | D3D12 Surface 模式（原生 swapchain 直出） | 2026-07-11 |
| Phase 7 | Chunk 几何上传 + Terrain atlas + 智能渲染 | 2026-07-11 |

### 🔄 待推进的任务事项

| 优先级 | 任务 | 预估复杂度 |
|--------|------|-----------|
| **高** | 从 MC 自动获取 Chunk Mesh（Mixin 拦截） | 高 |
| **高** | 天空盒与云雾渲染 | 中 |
| 中 | 实体渲染（模型 + 骨骼动画） | 高 |
| 中 | 粒子系统 + 后处理特效 | 中 |
| 低 | HUD/UI 叠加层 | 低 |

### 📈 里程碑

| 阶段 | 目标 | 状态 | 日期 |
|------|------|------|------|
| 基础链路 | JNI 通信 + wgpu 后端初始化 | ✅ | 2026-07-09 |
| 事件集成 | Fabric 事件 + Mixin 注入 | ✅ | 2026-07-10 |
| 渲染管线 | 3D 几何 + 三缓冲 + 相机同步 | ✅ | 2026-07-10 |
| Surface 模式 | D3D12 直出 MC 窗口（零读回） | ✅ | 2026-07-11 |
| Chunk 渲染 | MC 区块网格上传 + Terrain atlas | ✅ | 2026-07-11 |
| MC 场景渲染 | 自动获取 MC 区块/实体数据 | 🚧 | — |
| VulkanMod 级 | 全接管、GPU 满载、60fps+ | ❌ | — |

### 🎯 关键技术突破

1. **GL 上下文解除机制** — 解决 D3D12 + OpenGL 同 HWND 的 WDDM 驱动争用（经过 3 轮迭代）
2. **懒 resize** — 避免 `surface.configure()` 在 GL 绑定状态下触发 DXGI C++ 异常
3. **HWND 捕获时序** — 在解除 GL 上下文前获取 HWND，避免 `glfwGetCurrentContext()=0`
4. **Surface 格式匹配** — 优先 `Rgba8UnormSrgb` 匹配 pipeline，避免格式校验 panic
5. **`lock_or_poisoned()`** — 从 Mutex poison 状态恢复，防止 panic 级联 JVM 崩溃
6. **Chunk 几何上传** — MC GL_QUADS → D3D12 TriangleList，支持 u32 索引
7. **Terrain atlas 上传** — CPU mip chain + GPU 采样，chunk shader UV 纹理映射
8. **Surface 智能渲染** — 三种路径自动切换（chunk/GL 帧/测试场景）
9. **FBO-aware GL 帧捕获** — 从 MC 实际 draw FBO 读取，而非固定 GL_BACK
10. **Panic 保护** — `render_surface()` 中 `catch_unwind` 确保 frame 始终 present

---

## 📝 已知问题与解决方案汇总

| 问题 | 原因 | 解决方案 | 状态 |
|------|------|----------|------|
| 标题界面卡死（9s+） | 创建 surface 驱动争用 | inWorld 守卫 | ✅ |
| C++ EXCEPTION_UNCAUGHT | surface.configure DXGI 异常 | 懒 resize | ✅ |
| 白屏（子窗口方案） | STATIC 类窗口覆盖 MC 窗口 | 回退子窗口 | ✅ |
| 卡加载世界（15s 无响应） | setWindow 在 GL 绑定状态调用 | 移至 GL 解除区域 | ✅ |
| glfwGetCurrentContext=0 刷屏 | 先解除再获取 HWND | 解除前捕获 HWND | ✅ |
| Bgra8 != Rgba8 格式 panic | Pipeline 与 Surface 格式不匹配 | 优先 Rgba8UnormSrgb | ✅ |
| Surface image already acquired | 格式 panic 导致 present() 未执行 | 格式修复后自动解决 | ✅ |
| 测试几何体不可见（浅蓝色） | 几何体远离玩家位置 | 全屏 NDC 三角形 | ✅ |

---

## 📝 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 游戏 | Minecraft | 26.1.2 |
| 加载器 | Fabric Loader | 0.19.3 |
| API | Fabric API | 0.154.2+26.1.2 |
| Mixin | SpongePowered Mixin | 0.8.7 (via Fabric 0.17.3) |
| 语言 | Java | 25 |
| 图形 | wgpu (WebGPU) → DX12 | 23 |
| 语言 | Rust | 2021 edition |
| JNI | jni crate | 0.21 |
| 窗口句柄 | raw-window-handle | 0.6 |
| 图像处理 | image crate | 0.25 (PNG 诊断) |
| 测试 GPU | NVIDIA GeForce RTX 4070 | Driver 576.x |

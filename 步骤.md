# GL4DX12 — 在官方 GPU 后端架构上嵌入原生 D3D12 渲染后端

## 🎯 最终目标

Minecraft **26.2** 官方重构了渲染器，引入可插拔 GPU 后端架构（`GpuBackend` / `GpuDeviceBackend` / `GpuSurfaceBackend` 等），并新增官方 **Vulkan** 实现。我们的目标是：

**用原生 D3D12 实现官方这套 `GpuBackend` 接口**，作为 `PreferredGraphicsApi` 的第三个后端（DX12）嵌入。官方全部渲染逻辑（RenderState 提取、区块网格、实体、天空、粒子、后处理）原样运行，只替换 GPU 层。

- **技术栈**：**C++17 手写 D3D12**（MSVC + CMake，**不用 wgpu**）+ Java 实现官方接口 + JNI 桥接（原 Rust/windows crate 方案已废弃归档）
- **学习路径**：官方 `com.mojang.blaze3d.vulkan.*` 是移植模板，逻辑直接照搬，语言/API 重写为 D3D12
- **已归档**：旧 wgpu 叠加方案（Plan C，13 阶段）与 Rust 方案均已归档移除，仅作学习参考

---

## 📋 整体架构

```
Minecraft 26.2 (官方 GpuBackend 体系)
│
├── PreferredGraphicsApi (DEFAULT / OPENGL / VULKAN)
│     └── getBackendsToTry() → [Dx12Backend, GlBackend, VulkanBackend]   ← Mixin 注入
│
├── com.mojang.blaze3d.systems.GpuBackend          ← 顶层后端接口
│     └── Dx12Backend (Java) ── 我们实现
│           createDevice(hwnd, shaderSource, debugOptions, criticalShaderLoader)
│             ↓ JNI
├── Dx12Native (Java JNI 声明，DLL 从 JAR 提取加载)
│             ↓ JNI
├── dx12_mc.dll (C++17, MSVC + CMake)
│     └── D3D12CreateDevice → ID3D12Device          ← P1 已完成，创建 12_1 设备
│
├── GpuDeviceBackend (工厂) ← Dx12Device — P2 资源层 ✅ 已完成
│     ├── createTexture / createTextureView / createBuffer / createSampler  ✅ D3D12 committed resource + descriptor
│     ├── createCommandEncoder / createSurface                                ⬜ P3 stub
│     ├── precompilePipeline / createTimestampQueryPool / getDeviceInfo       ⬜ P4/P3 stub
│
├── CommandEncoderBackend / RenderPassBackend      ← P3 命令层（command list）
├── GpuSurfaceBackend (DXGI swapchain)             ← P5 Surface
└── 资源：GpuTexture / GpuBuffer / GpuSampler / GpuFence / CompiledRenderPipeline
```

**数据流**：官方高层门面类（`GpuDevice` / `GpuSurface` / `CommandEncoder` / `RenderPass`）由官方提供 → 我们只实现 Backend 接口 + 资源对象 → 底层全部由 C++ D3D12 执行。

---

## ✅ 已完成

### 26.2 升级（2026-08）
- 26.1.2 → 26.2：loom 1.15.5 默认 Mojang 官方映射（26.2 无 Yarn），fabric-api 0.156.0+26.2 / loader 0.19.3 / ModMenu 20.0.1
- `Dx12Mod` 适配 26.2 API（`gameRenderState()`、`setScreenAndShow`、`ParticleGroup` 反射遍历）

### 官方 Vulkan 架构研究（docs/official-262/）
- 全量反编译 26.2 客户端到 `docs/official-262/`（blaze3d 211 个文件）
- 确认官方后端选择机制：`Minecraft.<init>` 里 `for (GpuBackend backend : getBackendsToTry())` 逐个 try/catch `BackendCreationException`，失败自动换下一个
- 确认 `createDevice` 成功后**立即**调用 `getDeviceInfo()` + `RenderSystem.initRenderer`（内部创建 sampler/buffer/uniform）
- 确认官方 Vulkan 必需扩展/特性（`VK_KHR_dynamic_rendering` 等 5 扩展 + 9 特性）与 shader 机制（GLSL 源码 → SPIR-V）

### P1 挂点验证（✅ 已通过，日志验证 2026-08-06）
```
[dx12] Native library loaded from: ...\dx12mod\dx12_mc.dll
[dx12] D3D12 probe OK (createDevice called by vanilla): NVIDIA GeForce RTX 4070 (D3D_FEATURE_LEVEL 49408)
Failed to create backend DX12  → 按设计抛异常回退
Using graphics backend OpenGL
```
- C++ `dx12_mc`（native/，CMake + MSVC）：`D3D12CreateDevice` 创建 12_1 设备 + `GetAdapterLuid`/`EnumAdapterByLuid` 查适配器名
- Java `Dx12Backend implements GpuBackend`：`createDevice` 内 JNI 探测 D3D12 → 日志输出设备信息 → 抛 `BackendCreationException(OTHER)` 回退（资源层未实现前最安全）
- `PreferredGraphicsApiMixin`：重写 `getBackendsToTry()` 返回数组，DEFAULT/OPENGL 优先 DX12
- 游戏完整启动无崩溃（atlas、OpenAL、声音引擎全部正常）

### wgpu 方案移除（2026-08）
- 用户已归档 wgpu 方案，代码库删除：`D3D12Bridge.java`、5 个旧 mixin、`wgpu_mc_jni.dll`、`wgpu-mc` / `wgpu-mc-jni` crates
- `Dx12Mod` 精简为最小入口；`Dx12SettingsScreen` 保留 AA 配置持久化（待 DX12 后端接入）

### 技术栈转向 Rust → C++（2026-08-07）
- 用户确认：D3D12 原生层改用 **C++17**（MSVC 19.51 + CMake 4.3.1），Rust 方案废弃
- 删除 `rust/` 目录；新建 `native/`：CMakeLists.txt + `dx12_device.h/.cpp`（D3D12 设备上下文 + 资源层）+ `jni_bridge.cpp`（JNI 导出）
- 构建：`cmake -S native -B native/build -G "Visual Studio 18 2026" -A x64` + `--build --config Release` → `native/build/bin/Release/dx12_mc.dll`（34KB）
- 已修复：UTF-8 中文注释需 `/utf-8` 编译选项（否则 MSVC 按 GBK 误读级联报错）；`NOMINMAX` 防 `std::max` 宏冲突；SRV 成员名 `MostDetailedMip`

### P2 资源层（✅ 已完成，2026-08-07）
- **C++ 侧**（`dx12_device.cpp`）：56 个 `GpuFormat` ordinal → DXGI_FORMAT 映射（RGB 三通道近似 RGBA）；buffer heap 选择（MAP_WRITE→UPLOAD / MAP_READ→READBACK / 否则 DEFAULT）；texture `RENDER_ATTACHMENT`→RTV/DSV 标志；sampler 创建（aniso>1→ANISOTROPIC）；4 个 descriptor heap（SRV/RTV/DSV/Sampler）；`mapBuffer` → `NewDirectByteBuffer`；`runResourceSelfTest` 自检（创建 texture/buffer/sampler/view + map 写读 0x5A 验证）
- **Java 侧**（`com.dx12.dx12`）：
  - `Dx12Native`：8 个 JNI 方法声明（dx12CreateDevice / Texture / Buffer / Sampler / TextureView / DestroyResource / MapBuffer / UnmapBuffer）
  - `Dx12Device implements GpuDeviceBackend`：资源创建方法全走 JNI；surface/commandEncoder/pipeline/query 为 P3+ stub；`getDeviceInfo()` 返回真实 DeviceInfo
  - `Dx12GpuTexture` / `Dx12GpuBuffer` / `Dx12GpuSampler` / `Dx12GpuTextureView`：封装 native 句柄（long = `Dx12Object*`），close 调 `dx12DestroyResource`；`Dx12GpuBuffer.map` → `GpuBufferSlice.MappedView`（onClose 自动 unmap）
- **验证**：`gradlew build` 通过，JAR 内嵌 `dx12_mc.dll`；`Dx12Backend.createDevice` 现在执行 Java 资源层自检（texture/sampler/buffer map 写读/view 全链路）后仍抛异常回退（P3 渲染层未实现前保持安全）
- 待用户手动启动验证日志：`[dx12] Device probe + resource self-test: ...` 与 `[dx12] Java resource self-test OK (...)`

---

## 🚧 待办：P2 → P7+（我们要做什么）

### P2 资源层 — `Dx12Device implements GpuDeviceBackend` ✅ 已完成（2026-08-07）
**目标**：资源创建方法（texture/buffer/sampler/view）全部可用，Java→JNI→C++ 链路打通。
- [x] `DeviceInfo` / `DeviceLimits` / `DeviceFeatures` 数据结构（对照官方 VulkanDevice.getDeviceInfo，D3D12 下 ZZeroToOne=true）
- [x] `createTexture` → D3D12 committed resource（含 RENDER_ATTACHMENT→RTV/DSV、CUBEMAP 等 usage 映射）
- [x] `createTextureView` → SRV descriptor（`MostDetailedMip`）
- [x] `createBuffer` → D3D12 resource（MAP_WRITE→UPLOAD / MAP_READ→READBACK / 否则 DEFAULT）
- [x] `createSampler` → sampler descriptor（aniso>1→ANISOTROPIC）
- [x] `getDeviceInfo()` 返回真实 limits（maxAnisotropy 16 等）
- [x] `getLastDebugMessages` / `isDebuggingEnabled` / `close`
- **验证**：`createDevice` 内执行 Java 资源自检（真实 JNI 链路）后仍抛异常回退（P3 渲染层未实现前保持安全，不返回真 GpuDevice）

### P3 命令层 — `CommandEncoderBackend` + `RenderPassBackend`（✅ 已完成，2026-08-07，日志验证通过）
**目标**：官方每帧的 command 录制/提交全链路可用。
- **C++ 侧**（`dx12_device.cpp` / `dx12_device.h`，镜像官方 `VulkanCommandEncoder`）：
  - `CommandContext`：双 command allocator（`fenceValue % 2`）+ command list + `ID3D12Fence` + event；`submitCommandList` = `ExecuteCommandLists` → `Signal(++fenceValue)` → 等待 `fenceValue-2`（对应官方 timeline semaphore 双缓冲 `awaitSubmitCompletion(currentSubmitIndex-2)`）
  - `beginCommandList` 懒惰开启（对应官方 lazy `commandBuffer()`）；`endCommandList`；`createFence` = 捕获当前值，`awaitCompletion` 等待下一提交
  - copy/clear 系列：`copyBufferToBuffer`（CopyBufferRegion + barrier）；`clearColorTexture`/`clearDepthTexture`（临时 RTV/DSV + ClearRTV/DSV）；`copyBufferToTexture`（CopyTextureRegion，行距未 256 对齐时逐行拷贝，blockSize 按 DXGI 格式计算）；`copyTextureToBuffer`（读回行距 256 对齐）；`copyTextureToTexture`
  - render pass：`beginRenderPass`（分配 RTV/DSV、OMSetRenderTargets、viewport/scissor、清屏）+ `endRenderPass`（P4 补 draw 命令）
  - query：`QueryPool`（D3D12_QUERY_HEAP_TYPE_TIMESTAMP + readback resolve）；`writeTimestampToPool`（EndQuery）；`readQueryValues`（瞬时提交+阻塞读回）；`getTimestampNow`；`getTimestampFrequency`
- **Java 侧**（`com.dx12.dx12`）：
  - `Dx12CommandEncoderBackend implements CommandEncoderBackend`：submit/transientMemory/createRenderPass/submitRenderPass/clear*/writeToBuffer/copyToBuffer/writeToTexture/copyBufferToTexture/copyTextureToBuffer/copyTextureToTexture/createFence/writeTimestamp
  - `Dx12RenderPassBackend implements RenderPassBackend`：writeTimestamp 实现；draw/描述符方法 P4 stub（抛 UnsupportedOperationException）
  - `Dx12TransientMemory implements TransientMemory`：UPLOAD=staging / DEFAULT=gpu / UPLOAD=gpuMapped；upload* = staging 写 + CopyBufferRegion；每帧分配保留 2 帧（对应双缓冲）后释放
  - `Dx12GpuQueryPool implements GpuQueryPool`：size/getValue/getValues/close
  - `Dx12Device`：createCommandEncoder / createTimestampQueryPool / getTimestampNow（lazy ctx）接线；`timestampPeriod` 用 `GetTimestampFrequency`
- **验证**：`createDevice` 自检扩展命令层（writeToBuffer→copyToBuffer→readback 字节校验 + writeToTexture→copyTextureToBuffer 纹理回读校验）→ ✅ 日志验证通过（2026-08-07）：
  ```
  [dx12] Command layer self-test OK (submit/fence/copy/readback via JNI)
  ```
  通过后仍抛异常回退（P4 渲染层未实现前保持安全）；修复：`copyTextureToBuffer` 读回行距从 256 对齐改为紧凑逐行拷贝（Minecraft 读回 buffer 只分配紧凑大小）

### P4 管线与 Shader — `precompilePipeline` + GLSL→SPIR-V→HLSL→DXBC 链路（✅ 已通过，2026-08-07 日志验证）
**目标**：官方 `RenderPipeline` 描述 → D3D12 PSO；shader 编译链路落地。**最大风险项。**
- **技术路线（用户确认，2026-08-07）**：Java 复用游戏自带 LWJGL（shaderc + spvc 3.4.1 把 GLSL→SPIR-V→HLSL SM5.1）；native 只收 HLSL，用系统自带 `d3dcompiler_47.dll` 编译 **DXBC SM5.1**（零捆绑文件，无需 DXC）
- **Java 侧**（镜像官方 `GlslCompiler` / `IntermediaryShaderModule` / `VulkanDevice`）：
  - `Dx12ShaderCompiler`：shaderc（target Vulkan 1.2、auto_bind_uniforms/auto_map_locations、debug info、opt 0、全局 defines `gl_VertexID→gl_VertexIndex`）→ SPIR-V → spvc 反射（UBO/SAMPLED_IMAGE/STAGE_INPUT/STAGE_OUTPUT + BINDING/LOCATION 偏移）→ 打平输出 location → `rebind`（顶点输入按 vertex format 属性名、片元按 VS 输出顺序编 attribLocation/binding，剩余资源检查抛 `ShaderCompileException`）→ spvc HLSL（backend=2, shader model 51）
  - `Dx12IntermediaryShaderModule`：record + `INVALID` 占位 + `createFromSpirv`/`rebind`/`toHlsl`
  - `Dx12CompiledShader`：`(vertexHlsl, fragmentHlsl, entries, vertexShaderInputs)` —— vertexShaderInputs 供原生层按 location 推导 TEXCOORD 语义
  - `Dx12Device`：`precompilePipeline`（pipelineCache.computeIfAbsent，镜像官方）、`getOrCompileShader`/`compileShader`（shaderCache + `ShaderCompilationKey(id,type,defines)`，失败返回 INVALID）、`clearPipelineCache`（close 全部销毁）、`buildNativeDesc`（little-endian 打包：vs/ps 源码 + 颜色目标（format/writeMask/blend 因子/op 序数）+ 深度（D32_FLOAT）+ topology/cull/polygonMode + 输入元素（location/binding/format/offset/stride/stepRate）+ 绑定条目（type/register））
- **C++ 侧**（`dx12_device.cpp` / `jni_bridge_p4.cpp`）：
  - `createGraphicsPipeline`：`D3DCompile(vs_5_1/ps_5_1, entry=main)` → root signature（单 descriptor table：CBV b{i} / SRV t{i} 混合 + static sampler s{i}，仅 SAMPLED_IMAGE 条目）→ 输入布局（语义 `TEXCOORD<location>`，`toDxgiVertexFormat` 对 RGB32_* 用精确 R32G32B32_* 三分量格式）→ 双 PSO（withDepth 总是创建 DSV=D32_FLOAT 镜像官方 depthAttachmentFormat=126；无深度状态时再建 withoutDepth DSV=UNKNOWN）
  - 枚举映射：BlendFactor/BlendOp/CompareOp/PrimitiveTopology/PolygonMode ordinal → D3D12（官方枚举序数为字母序，native switch 映射）；`FrontCounterClockwise=TRUE`（MC 前端逆时针）；writeMask 位序与 D3D12 一致
- **验证（✅ 通过，2026-08-07）**：`createDevice` 自检编译官方真实管线（`RenderPipelines.GUI` + `GUI_TEXTURED`，内嵌提取自游戏 jar 的 `core/gui` + `core/position_tex_color` GLSL 原文）全链路成功，日志：
  ```
  [dx12] Pipeline self-test OK (GLSL->SPIR-V->HLSL->DXBC->PSO for core/gui + core/position_tex_color)
  ```
  通过后仍抛异常回退（P5 Surface 未实现前保持安全），异常文案 `DX12 device + pipeline verified; render layer (P5) not yet implemented`
- **修复（2026-08-07 首轮验证）**：自检报 `java.nio.BufferOverflowException` —— `buildNativeDesc` 的 size 按 `colorCount * 10` 分配，但每色目标实际写入 12 字节（putInt + 8×byte：writeMask/blendEnabled/srcColor/dstColor/colorOp/srcAlpha/dstAlpha/alphaOp），少算 2 字节/色溢出。已改 `colorCount * 12`，同步修正 `jni_bridge_p4.cpp` 的 `remaining -= count*12`（一致性，未使用值）；自检异常改为打印完整栈便于定位
- **修复（2026-08-07 二轮验证）**：native 报 `dx12CreateGraphicsPipeline: bad psLen` —— `jni_bridge_p4.cpp` 的 `readBytes` 内部已推进指针 `p += len`，调用处又手动 `p += vsLen/psLen`，**指针双倍推进**，psLen 从 vs 数据中间读到垃圾值。已删除调用处多余推进，bad vsLen/psLen 日志加参数诊断
- 验证已通过（✅ 2026-08-07），日志：
  ```
  [dx12] Pipeline self-test OK (GLSL->SPIR-V->HLSL->DXBC->PSO for core/gui + core/position_tex_color)
  ```
  通过后仍抛异常回退（P5 Surface 实现完成前保持安全）

### P5 Surface 呈现 — `GpuSurfaceBackend` + DXGI swapchain（✅ 已通过，2026-08-07 日志验证）
**目标**：窗口呈现全屏颜色（DXGI flip-model swapchain 全链路）。
- **Java 侧**（镜像官方 `VulkanGpuSurface` / `GpuSurface` 高层）：
  - `Dx12GpuSurface implements GpuSurfaceBackend`：构造（`dx12CreateSurface(hwnd)` + `supportedPresentModes` = DXGI 支持的 {IMMEDIATE, FIFO, FIFO_RELAXED}）/ `configure`（`dx12ConfigureSurface`）/ `acquireNextTexture` / `blitFromTexture`（强转 `Dx12CommandEncoderBackend` 取 ctx，`textureView.texture()` 取纹理 handle）/ `present` / `isSuboptimal` / `close`
  - `Dx12Device.createSurface(windowHandle)`：`GLFWNativeWin32.glfwGetWin32Window` 把 GLFW window 转 HWND（D3D12 需要真 HWND，Vulkan 用 GLFW 内部）
  - `Dx12CommandEncoderBackend.nativeHandle()`：暴露 CommandContext* 给 blit 录制
- **C++ 侧**（`dx12_surface.cpp` + `jni_bridge_p5.cpp`，镜像官方 `VulkanGpuSurface`）：
  - `Dx12Surface`：IDXGISwapChain3 + back buffers + RTV handles
  - `createSurface`：`CreateDXGIFactory1` → `CreateSwapChainForHwnd(queue, hwnd)`（**FLIP_DISCARD + ALLOW_TEARING**，1x1 占位）；**swapchain 格式 `R8G8B8A8_UNORM`**（与 GpuFormat.RGBA8_UNORM 同族，`CopyTextureRegion` 可直接拷贝，避免 RGBA↔BGRA 通道转换——官方 Vulkan 的 BGRA 是 VkFormat 惯例，GpuFormat 枚举并无 B8G8R8A8）
  - `configureSurface`：`ResizeBuffers(3, w, h)`（BufferCount=3 镜像官方 minImageCount max(3,min)）+ 重新取 back buffers + 从 rtvHeap 分配 RTV（新增共享 `allocRtvHandle`）
  - `acquireSurface`：`GetCurrentBackBufferIndex`（FLIP 模型 Present 内部同步）
  - `blitSurface`：barrier backbuffer PRESENT→RENDER_TARGET → `CopyTextureRegion`（源纹理整区域 → backbuffer）→ barrier 回 PRESENT；录制进当前 command list
  - `presentSurface`：IMMEDIATE=0 → `Present(0,0)`；FIFO=2 → `Present(1,0)`；FIFO_RELAXED=3 → `Present(1, ALLOW_TEARING)`；OCCLUDED/MODE_CHANGED 置 suboptimal
- **验证（✅ 通过，2026-08-07）**：`createDevice` 自检追加 `selfTestSurface`：建 RGBA8 中间纹理（USAGE_COPY_SRC）→ `new GpuSurface(createSurface(window))` → `getSupportedVsyncMode(false)` 选 IMMEDIATE → configure 64x64 → acquire → blit（`CommandEncoder` facade 包装 backend）→ submit → present。日志：
  ```
  [dx12] Surface self-test OK (DXGI swapchain configure/acquire/blit/present via JNI)
  ```
  注：自检 present 的是未写入内容的纯黑纹理且仅一帧，随后回退 OpenGL 立即覆盖，故窗口看不到颜色属预期（P6 返回真实 GpuDevice 后才有持续画面）
- 通过后仍抛异常回退（P6 首帧画面未实现前保持安全）

### P6 首帧画面 — 官方渲染流程跑通（🔨 进行中，2026-08-07 首轮构建完成）
**目标**：`createDevice` 返回真实 `GpuDevice`，官方渲染流程全部走 DX12，窗口显示 MC 画面（清屏色/菜单/游戏画面）。
- [x] `Dx12Backend.createDevice`：`new Dx12Device(defaultShaderSource)` 跑完整自检后 `return new GpuDevice(device, criticalShaderLoader)`（不再抛异常回退；失败才 close + 抛 `BackendCreationException`）
- [x] `Dx12Device`：构造签名 `Dx12Device(ShaderSource)`；`getOrCompilePipeline`（渲染 pass setPipeline 按需编译）；`precompilePipeline`/`getOrCompilePipeline` 共用 `pipelineCache`；`DeviceFeatures(true,true,false,true,true,true,true)`（`multiDrawDirectSeparate=false` → 官方不会调 `multiDraw(PointerBuffer,…)` 变体）
- [x] `Dx12CommandEncoderBackend`：`device` 字段 + 构造重载；`createRenderPass` 传 area/w/h/hasDepth 给 pass
- [x] `Dx12RenderPassBackend`（完整 draw 全链路，镜像官方 `VulkanRenderPass`）：setPipeline（withDepth/withoutDepth）/ bindTexture / setUniform / pushDescriptors（按 `pipeline.bindings()` 顺序把 uniform/texture 推入原生瞬时描述符堆）/ enableScissor/disableScissor / setVertexBuffer（stride 从 pipeline vertex format 取）/ setIndexBuffer / drawIndexed / draw / drawIndexedIndirect / drawIndirect / drawMultipleIndexed / multiDraw*
- [x] 原生 P6 绘制层（`dx12_device.cpp` + `jni_bridge_p6.cpp`）：瞬时 `drawHeap`（SHADER_VISIBLE CBV_SRV_UAV，ring x2 与双 allocator 同步）；`createBuffer` 资源宽度 256 对齐；`CreateCommandSignature`（indexed stride 20 / non-indexed stride 16）→ `ExecuteIndirect`；`SetGraphicsRootDescriptorTable(0)` 绑定 root table
- [ ] 官方 `RenderSystem` 初始化（uniform 缓冲、samplerCache）全通过 ← **待游戏实测**
- [ ] 首帧 command 提交 → swapchain present → 窗口有画面 ← **待游戏实测**
- **验证**：日志无错误（不再出现 `Failed to create backend DX12` / `Using graphics backend OpenGL`），窗口显示渲染结果；F3 若仍显示 OpenGL 属预期（P6 之后单独做 backend 信息替换）
- **后续**：F3 调试界面把 OpenGL 信息换成 DX12（学官方 DebugScreen 的 backend 描述）
- **首轮实测（2026-08-07 10:59，DX12 选中成功但崩溃）**：
  - ✅ 日志出现 `Using graphics backend DX12` / `Using graphics device: NVIDIA GeForce RTX 4070 (D3D12)` —— 后端被官方选中，不再回退
  - ❌ 随后崩溃：`dx12CreateBuffer: CreateCommittedResource(buffer): HRESULT 0x887A0005`（= DXGI_ERROR_DEVICE_REMOVED，GPU 设备被移除），栈：`MappableRingBuffer.<init>` ← `CloudRenderer` ← `LevelRenderer` ← `Minecraft.<init>:674`
  - **根因 1（设备移除）**：`dx12_surface.cpp` 的 `blitSurface` 把 backbuffer 过渡到 `RENDER_TARGET` 后却用 `CopyTextureRegion` 写入 —— 拷贝目标必须处于 `COPY_DEST` 状态；RENDER_TARGET 状态拷贝属非法 GPU 使用 → 驱动重置 → 设备移除，错误延迟到后续 `CreateCommittedResource` 才浮现。**已改为 PRESENT→COPY_DEST→拷贝→COPY_DEST→PRESENT**
  - **根因 2（预期后续暴露）**：RTV/DSV/SRV 描述符计数全局单调递增、永不重置；Minecraft 每帧 clear/渲染 pass 消耗 RTV（heap 512）与 DSV（heap 64），几十帧即耗尽。RTV/DSV 为 CPU-only heap（提交时驱动已捕获描述符），`beginCommandList` 现每帧重置 `gNextRtv/gNextDsv=0`（配合双 allocator + value-2 完成等待保证安全）；SRV 长生命周期不重置
- **二轮实测（2026-08-07 11:05，修复 COPY_DEST 后仍 DEVICE_REMOVED）**：
  - ❌ 仍崩溃同点：`dx12CreateBuffer ... 0x887A0005`（CloudRenderer ← LevelRenderer ← Minecraft.<init>:674）
  - **根因 3（真正根因，竞态）**：`submitCommandList` 只等 `value-2`（双缓冲，不等待本次提交）。`selfTestSurface` 在 blit 命令可能仍在 GPU 队列时 `present` 后 `close()` 直接销毁 swapchain → backbuffer 被 GPU 使用时释放 → 设备移除。P5 时设备随后整体销毁掩盖了问题；P6 设备保留立刻暴露
  - **修复**：① native 新增 `deviceWaitIdle()`（Signal 临时 fence 并阻塞等待队列空闲），`destroySurface` 销毁 swapchain 前调用；② Java `selfTestSurface` 中 `submit()` 后创建 fence 并 `awaitCompletion(5000)` 再 close
- **三轮实测（2026-08-07 11:11，fence 等待生效、崩溃点后移）**：
  - ❌ 仍 `0x887A0005`，但崩溃点从 Minecraft:674（CloudRenderer）**后移到 667（GameRenderer → DebugCrosshairRenderer）**——自检不再杀设备，设备在 initRenderer 之后又被杀
  - **根因 4（真正根因）**：`Dx12Device.createBuffer(data)`（DynamicUniforms/字体上传大量走此路径）是 submit 后**不等待立即 close** → `destroyCommandEncoder` 销毁时命令列表可能仍在 GPU 队列 → allocator 被 GPU 使用中释放 → 驱动重置。设备移除是迟到错误：initRenderer 埋雷，GameRenderer 才爆
  - **根因 5（allocator ring 差一帧）**：`beginCommandList` 用 `allocators[fenceValue%2]`，submit 只等 `value-2` → 帧 N+2 复用帧 N 的 allocator 但只保证了 Signal(N-1) 完成。**双 ring + 等 value-2 差一帧**，渲染循环几帧即炸
  - **修复**：① allocator 改三 ring（`allocators[3]`、`currentAllocator()=fenceValue%3`，帧 N+3 复用同 index 时 submit 已等 N 完成；drawHeap ring x2 独立正确，不动）；② `destroyCommandEncoder` 销毁前 `waitForFenceValue(ctx, ctx->fenceValue)` 等本 ctx 全部已提交命令完成

### P7+ 完整渲染 — 对照官方 Vulkan 逐 RenderPass 移植
**目标**：区块、实体、天空、粒子、lightmap、后处理逐步上线。
- [ ] 区块渲染（`UberGpuBuffer` 合并顶点 + multiDraw 批处理）← 对照官方 `LevelRenderer` / `UberGpuBuffer` 和 wgpu 方案的 Phase 11e
- [ ] 天空 / 云 / 雾
- [ ] 实体（含第一人称手臂）
- [ ] 粒子（D3D12 无 `point_size`，用 quad 展开 ← 沿用 wgpu 方案 13.3 的解法）
- [ ] lightmap 动态光照
- [ ] 后处理（FXAA / 色调映射 / gamma）
- [ ] 视锥裁剪 / 批处理 / 上传去重等性能优化（对照 wgpu 方案 Phase 11 全套经验）

---

## ⚠️ 关键技术难点

1. **Shader 链路（最大风险）**：官方给 GLSL 源码，Vulkan 内部 GLSL→SPIR-V；D3D12 需要 GLSL→SPIR-V→HLSL→DXIL 或直接 HLSL 移植，DXC 工具链集成
2. **描述符管理**：D3D12 descriptor heap（CBV/SRV/UAV/sampler）管理，对应官方 BindGroupLayout
3. **资源状态转换**：D3D12 手动 barrier，对应官方自动布局转换
4. **GPU 同步**：fence + 多帧飞行（官方 `GpuFence` 接口）
5. **UberGpuBuffer**：官方把所有顶点/索引数据塞进超大 GPU 缓冲，需要 D3D12 heap 管理 + 增量追加
6. **Shader 缓存**：对应 `ShaderCompilationKey(id, type, defines)` → DXIL 缓存
7. **P1/P2 阶段的回退策略**：渲染层（P3）未完成前，`createDevice` 必须抛异常回退，否则官方 `initRenderer` / 渲染循环崩溃

---

## 🔧 构建与部署

```powershell
# 1. 构建 C++ DLL（MSVC + CMake，VS 18 2026 生成器）
cd native
& "D:\Visual studio\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" -S . -B build -G "Visual Studio 18 2026" -A x64
& "D:\Visual studio\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" --build build --config Release
# 输出: native/build/bin/Release/dx12_mc.dll

# 2. 复制为 Fabric 资源（JAR 内嵌，启动时自动提取到 dx12mod/）
Copy-Item native\build\bin\Release\dx12_mc.dll fabric\src\main\resources\dx12_mc.dll -Force

# 3. 构建 Fabric 模组
cd fabric
.\gradlew build
# 输出: build/libs/gl4dx12-0.1.0.jar

# 4. 部署到测试实例（只需 JAR；DLL 启动时自动提取）
Copy-Item fabric\build\libs\gl4dx12-0.1.0.jar "D:\.minecraft\versions\26.2-Fabric_0.19.3\mods\"
```

### 部署路径

```
D:\.minecraft\versions\26.2-Fabric_0.19.3\
├── mods\
│   └── gl4dx12-0.1.0.jar       # Fabric 模组 JAR（内嵌 dx12_mc.dll）
└── dx12mod\
    └── dx12_mc.dll             # 启动时从 JAR 自动提取（REPLACE_EXISTING）
```

### 运行验证（P1/P2 已达成，P3+ 逐阶段追加）

```
[dx12] Native library loaded from: ...\dx12mod\dx12_mc.dll
[dx12] Device probe + resource self-test: NVIDIA GeForce RTX 4070 (D3D_FEATURE_LEVEL_12_1); SELF-TEST OK (map write/read verified)
[dx12] Java resource self-test OK (texture/buffer/sampler/view via JNI)
P3 起：命令层自检追加 → "[dx12] Command layer self-test OK (submit/fence/copy/readback via JNI)"  ✅ 已验证
P3 仍抛异常回退（P4 渲染层未实现前保持安全）
P4 起：createDevice 返回真实 GpuDevice → 不再有 "Failed to create backend DX12"
P6 起：窗口显示渲染画面
```

---

## 📝 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 游戏 | Minecraft | 26.2 |
| 加载器 | Fabric Loader | 0.19.3 |
| API | Fabric API | 0.156.0+26.2 |
| 映射 | Mojang 官方映射（26.2 无 Yarn） | loom 1.15.5 |
| Mixin | SpongePowered Mixin | 0.8.7 (via Fabric 0.17.3) |
| 语言 | Java | 26 |
| 图形 | **D3D12 手写（C++17）** | 无 wgpu |
| 语言 | C++ | 17（MSVC 19.51） |
| 构建 | CMake | 4.3.1（VS 18 2026 生成器） |
| JNI | 手写 JNI 桥（jni_bridge.cpp） | 无 jni crate |
| 学习参照 | 官方 `com.mojang.blaze3d.vulkan.*` 反编译 | docs/official-262/ |
| 测试 GPU | NVIDIA GeForce RTX 4070 | Driver 610.74 |

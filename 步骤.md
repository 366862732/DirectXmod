# GL4DX12 — 在官方 GPU 后端架构上嵌入原生 D3D12 渲染后端

## 🎯 最终目标

Minecraft **26.2** 官方重构了渲染器，引入可插拔 GPU 后端架构（`GpuBackend` / `GpuDeviceBackend` / `GpuSurfaceBackend` 等），并新增官方 **Vulkan** 实现。我们的目标是：

**用原生 D3D12 实现官方这套 `GpuBackend` 接口**，作为 `PreferredGraphicsApi` 的第三个后端（DX12）嵌入。官方全部渲染逻辑（RenderState 提取、区块网格、实体、天空、粒子、后处理）原样运行，只替换 GPU 层。

- **技术栈**：Rust 手写 D3D12（windows crate 0.58，**不用 wgpu**）+ Java 实现官方接口 + JNI 桥接
- **学习路径**：官方 `com.mojang.blaze3d.vulkan.*` 是移植模板，逻辑直接照搬，语言/API 重写为 D3D12
- **已归档**：旧 wgpu 叠加方案（Plan C，13 阶段）已完整归档移除，仅作学习参考

---

## 📋 整体架构

```
Minecraft 26.2 (官方 GpuBackend 体系)
│
├── PreferredGraphicsApi (DEFAULT / OPENGL / VULKAN)
│     └── getBackendsToTry() → [Dx12Backend, GlBackend, VulkanBackend]   ← Mixin 注入
│
├── com.mojang.blaze3d.systems.GpuBackend          ← 顶层后端接口
│     └── Dx12Backend (Java) ── 我们实现
│           createDevice(hwnd, shaderSource, debugOptions, criticalShaderLoader)
│             ↓ JNI
├── Dx12Native (Java JNI 声明，DLL 从 JAR 提取加载)
│             ↓ JNI
├── dx12_mc.dll (Rust, windows crate 0.58)
│     └── D3D12CreateDevice → ID3D12Device          ← P1 已完成，创建 12_1 设备
│
├── GpuDeviceBackend (工厂) ← Dx12Device — P2 资源层
│     ├── createTexture / createTextureView / createBuffer / createSampler
│     ├── createCommandEncoder / createSurface
│     ├── precompilePipeline / createTimestampQueryPool / getDeviceInfo
│
├── CommandEncoderBackend / RenderPassBackend      ← P3 命令层（command list）
├── GpuSurfaceBackend (DXGI swapchain)             ← P5 Surface
└── 资源：GpuTexture / GpuBuffer / GpuSampler / GpuFence / CompiledRenderPipeline
```

**数据流**：官方高层门面类（`GpuDevice` / `GpuSurface` / `CommandEncoder` / `RenderPass`）由官方提供 → 我们只实现 Backend 接口 + 资源对象 → 底层全部由 Rust D3D12 执行。

---

## ✅ 已完成

### 26.2 升级（2026-08）
- 26.1.2 → 26.2：loom 1.15.5 默认 Mojang 官方映射（26.2 无 Yarn），fabric-api 0.156.0+26.2 / loader 0.19.3 / ModMenu 20.0.1
- `Dx12Mod` 适配 26.2 API（`gameRenderState()`、`setScreenAndShow`、`ParticleGroup` 反射遍历）

### 官方 Vulkan 架构研究（docs/official-262/）
- 全量反编译 26.2 客户端到 `docs/official-262/`（blaze3d 211 个文件）
- 确认官方后端选择机制：`Minecraft.<init>` 里 `for (GpuBackend backend : getBackendsToTry())` 逐个 try/catch `BackendCreationException`，失败自动换下一个
- 确认 `createDevice` 成功后**立即**调用 `getDeviceInfo()` + `RenderSystem.initRenderer`（内部创建 sampler/buffer/uniform）
- 确认官方 Vulkan 必需扩展/特性（`VK_KHR_dynamic_rendering` 等 5 扩展 + 9 特性）与 shader 机制（GLSL 源码 → SPIR-V）

### P1 挂点验证（✅ 已通过，日志验证 2026-08-06）
```
[dx12] Native library loaded from: ...\dx12mod\dx12_mc.dll
[dx12] D3D12 probe OK (createDevice called by vanilla): NVIDIA GeForce RTX 4070 (D3D_FEATURE_LEVEL 49408)
Failed to create backend DX12  → 按设计抛异常回退
Using graphics backend OpenGL
```
- Rust `dx12-mc` crate：`D3D12CreateDevice` 创建 12_1 设备 + `GetAdapterLuid`/`EnumAdapterByLuid` 查适配器名
- Java `Dx12Backend implements GpuBackend`：`createDevice` 内 JNI 探测 D3D12 → 日志输出设备信息 → 抛 `BackendCreationException(OTHER)` 回退（资源层未实现前最安全）
- `PreferredGraphicsApiMixin`：重写 `getBackendsToTry()` 返回数组，DEFAULT/OPENGL 优先 DX12
- 游戏完整启动无崩溃（atlas、OpenAL、声音引擎全部正常）

### wgpu 方案移除（2026-08）
- 用户已归档 wgpu 方案，代码库删除：`D3D12Bridge.java`、5 个旧 mixin、`wgpu_mc_jni.dll`、`wgpu-mc` / `wgpu-mc-jni` crates
- `Dx12Mod` 精简为最小入口；`Dx12SettingsScreen` 保留 AA 配置持久化（待 DX12 后端接入）

---

## 🚧 待办：P2 → P7+（我们要做什么）

### P2 资源层 — `Dx12Device implements GpuDeviceBackend`
**目标**：`createDevice` 成功后能创建真实 `GpuDevice`，资源创建方法全部可用。
- [ ] `DeviceInfo` / `DeviceLimits` / `DeviceFeatures` 数据结构（对照官方 VulkanDevice.getDeviceInfo）
- [ ] `createTexture` → D3D12 committed resource（含 textureFlags：render target / depth / sampling 等 usage 映射）
- [ ] `createTextureView` → SRV / RTV / DSV descriptor
- [ ] `createBuffer` → D3D12 resource（含 flags：vertex/index/uniform/copy 等 usage + 上传/默认堆策略）
- [ ] `createSampler` → sampler descriptor
- [ ] `getDeviceInfo()` 返回真实 limits（maxTextureSize 等，官方 `maxTextureSizeForFormat` 会立即调用）
- [ ] `getLastDebugMessages` / `isDebuggingEnabled` / `close`
- **验证**：`createDevice` 改返回真实 `GpuDevice`，官方 `RenderSystem.initRenderer` 走通不崩

### P3 命令层 — `CommandEncoderBackend` + `RenderPassBackend`
**目标**：官方每帧的 command 录制/提交全链路可用。
- [ ] `createCommandEncoder` → command allocator + command list 池（多帧飞行）
- [ ] `transientMemory` → `TransientMemory` 实现（官方临时缓冲分配，对照 `UberGpuBuffer` 思路）
- [ ] clear / copy / write 系列（clearColorTexture、writeToBuffer、copyTextureToBuffer 等）→ resource barrier + copy
- [ ] `createFence` / `writeTimestamp` → D3D12 fence / query
- [ ] `RenderPassBackend`：setPipeline / bindTexture / setUniform / setVertexBuffer / setIndexBuffer / drawIndexed / draw
- [ ] `submit` / `submitRenderPass` → `ExecuteCommandLists` + fence 信号/等待
- **验证**：官方渲染循环（RenderState 提取 → command 录制 → submit）走通

### P4 管线与 Shader — `precompilePipeline` + GLSL→DXIL 链路
**目标**：官方 `RenderPipeline` 描述 → D3D12 PSO；shader 编译链路落地。**最大风险项。**
- [ ] 确定 shader 编译链路：GLSL（官方 `ShaderSource`）→ SPIR-V（glslang）→ HLSL（SPIRV-Cross）→ DXIL（DXC），或直接移植官方 shader 为 HLSL
- [ ] `ShaderDefines` 处理（对应 `ShaderCompilationKey(id, type, defines)`）
- [ ] `RenderPipeline` 各状态（blend / depth / cull / vertex layout / input layout）→ `D3D12_GRAPHICS_PIPELINE_STATE_DESC`
- [ ] root signature / descriptor layout 对应官方 BindGroupLayout
- [ ] shader 缓存（DXIL 按 key 缓存，`clearPipelineCache` 清缓存）
- **验证**：官方预编译管线（启动时 `precompilePipeline` 批量调用）全部成功

### P5 Surface 呈现 — `GpuSurfaceBackend` + DXGI swapchain
**目标**：窗口呈现全屏颜色。
- [ ] `createSurface(hwnd)` → DXGI factory + swapchain（对照官方 Vulkan `createSurface` 流程）
- [ ] `configure(Configuration)` → ResizeBuffers + 格式/缓冲数匹配
- [ ] `acquireNextTexture` → `GetCurrentBackBufferIndex` + 等待 fence
- [ ] `blitFromTexture` → 从中间 RT blit 到 backbuffer
- [ ] `present` → `Present(1, 0)` vsync + 同步逻辑
- [ ] `supportedPresentModes` / `isSuboptimal`
- **验证**：窗口显示纯色/清屏画面

### P6 首帧画面 — 官方渲染流程跑通
**目标**：清屏 → 简单 draw，窗口呈现 MC 画面（哪怕只有天空/清屏色）。
- [ ] 官方 `RenderSystem` 初始化（uniform 缓冲、samplerCache）全通过
- [ ] 首帧 command 提交 → swapchain present → 窗口有画面
- **验证**：日志无错误，窗口显示渲染结果

### P7+ 完整渲染 — 对照官方 Vulkan 逐 RenderPass 移植
**目标**：区块、实体、天空、粒子、lightmap、后处理逐步上线。
- [ ] 区块渲染（`UberGpuBuffer` 合并顶点 + multiDraw 批处理）← 对照官方 `LevelRenderer` / `UberGpuBuffer` 和 wgpu 方案的 Phase 11e
- [ ] 天空 / 云 / 雾
- [ ] 实体（含第一人称手臂）
- [ ] 粒子（D3D12 无 `point_size`，用 quad 展开 ← 沿用 wgpu 方案 13.3 的解法）
- [ ] lightmap 动态光照
- [ ] 后处理（FXAA / 色调映射 / gamma）
- [ ] 视锥裁剪 / 批处理 / 上传去重等性能优化（对照 wgpu 方案 Phase 11 全套经验）

---

## ⚠️ 关键技术难点

1. **Shader 链路（最大风险）**：官方给 GLSL 源码，Vulkan 内部 GLSL→SPIR-V；D3D12 需要 GLSL→SPIR-V→HLSL→DXIL 或直接 HLSL 移植，DXC 工具链集成
2. **描述符管理**：D3D12 descriptor heap（CBV/SRV/UAV/sampler）管理，对应官方 BindGroupLayout
3. **资源状态转换**：D3D12 手动 barrier，对应官方自动布局转换
4. **GPU 同步**：fence + 多帧飞行（官方 `GpuFence` 接口）
5. **UberGpuBuffer**：官方把所有顶点/索引数据塞进超大 GPU 缓冲，需要 D3D12 heap 管理 + 增量追加
6. **Shader 缓存**：对应 `ShaderCompilationKey(id, type, defines)` → DXIL 缓存
7. **P1 阶段的回退策略**：资源层（P2）未完成前，`createDevice` 必须抛异常回退，否则官方 `initRenderer` 崩溃

---

## 🔧 构建与部署

```powershell
# 1. 构建 Rust DLL（workspace 只剩 dx12-mc）
cd rust
cargo build -p dx12-mc --release
# 输出: target/release/dx12_mc.dll

# 2. 复制为 Fabric 资源（JAR 内嵌，启动时自动提取到 dx12mod/）
Copy-Item rust\target\release\dx12_mc.dll fabric\src\main\resources\dx12_mc.dll -Force

# 3. 构建 Fabric 模组
cd fabric
.\gradlew build
# 输出: build/libs/gl4dx12-0.1.0.jar

# 4. 部署到测试实例（只需 JAR；DLL 启动时自动提取）
Copy-Item fabric\build\libs\gl4dx12-0.1.0.jar "D:\.minecraft\versions\26.2-Fabric_0.19.3\mods\"
```

### 部署路径

```
D:\.minecraft\versions\26.2-Fabric_0.19.3\
├── mods\
│   └── gl4dx12-0.1.0.jar       # Fabric 模组 JAR（内嵌 dx12_mc.dll）
└── dx12mod\
    └── dx12_mc.dll             # 启动时从 JAR 自动提取（REPLACE_EXISTING）
```

### 运行验证（P1 已达成，P2+ 逐阶段追加）

```
[dx12] Native library loaded from: ...\dx12mod\dx12_mc.dll
[dx12] D3D12 probe OK (createDevice called by vanilla): NVIDIA GeForce RTX 4070 (D3D_FEATURE_LEVEL 12_1)
P2 起：createDevice 返回真实 GpuDevice → 不再有 "Failed to create backend DX12"
P6 起：窗口显示渲染画面
```

---

## 📝 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 游戏 | Minecraft | 26.2 |
| 加载器 | Fabric Loader | 0.19.3 |
| API | Fabric API | 0.156.0+26.2 |
| 映射 | Mojang 官方映射（26.2 无 Yarn） | loom 1.15.5 |
| Mixin | SpongePowered Mixin | 0.8.7 (via Fabric 0.17.3) |
| 语言 | Java | 25 |
| 图形 | **D3D12 手写（windows crate 0.58）** | 无 wgpu |
| 语言 | Rust | 2021 edition |
| JNI | jni crate | 0.21 |
| 学习参照 | 官方 `com.mojang.blaze3d.vulkan.*` 反编译 | docs/official-262/ |
| 测试 GPU | NVIDIA GeForce RTX 4070 | Driver 610.74 |

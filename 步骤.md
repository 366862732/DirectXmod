# GL4DX12 - Minecraft 1.21.1 DirectX 12 渲染桥接模组

## 📋 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                   Minecraft 1.21.1                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              Fabric Loader 0.19.3                      │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │           Fabric API (ClientTickEvents)          │  │  │
│  │  │  Tick Callback → 节流 100ms → Rust renderFrame() │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │         Fabric API (HudRenderCallback)           │  │  │
│  │  │  GL 绘制 → glTexSubImage2D + VAO + Shader → 全屏quad │ │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
│                           ↕ JNI                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              wgpu_mc_jni.dll (Rust)                    │  │
│  │  nativeSetWindow(HWND) → 初始化 DX12 Adapter           │  │
│  │  nativeRenderFrame() → 返回 byte[] (RGBA 像素数据)      │  │
│  │  nativeResize(width, height) → 更新窗口尺寸            │  │
│  └───────────────────────────────────────────────────────┘  │
│                           ↕                                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │              wgpu-mc (Rust)                            │  │
│  │  wgpu::Instance(DX12) → Adapter → Device + Queue       │  │
│  │  render_frame() → 生成纯色/三角形像素数据               │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**核心设计原则：**
- 不使用 Mixin（避免与 Fabric 渲染器冲突）
- 不创建额外窗口（直接使用 Minecraft 窗口 HWND）
- 版本通用（1.21.1 ~ 1.21.11 + 26.x，不依赖 Yarn 映射）
- Rust wgpu 在后台线程渲染 → 读回像素 → Java 通过 OpenGL 纹理上传 → 绘制全屏 quad

---

## ✅ 第一阶段：JNI 通信链路打通

**状态：已完成**

### 1.1 环境准备

```bash
# 1. 安装 Rust
rustup default stable
rustup component add rust-analyzer rust-src

# 2. JDK 21（Windows 从 Adoptium 下载）

# 3. 配置 JAVA_HOME
set JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.x
```

### 1.2 项目结构

```
dx12-lib-template-26.1.2/
├── fabric/                          # Fabric 模组（Java）
│   ├── src/main/java/com/dx12/
│   │   ├── Dx12Mod.java            # 模组入口，注册事件回调
│   │   └── D3D12Bridge.java        # JNI 桥接层
│   ├── src/main/resources/
│   │   └── fabric.mod.json         # Fabric 模组描述
│   ├── build.gradle                # Gradle 构建配置
│   └── gradle.properties           # 版本参数
├── rust/
│   ├── Cargo.toml                  # Workspace 配置
│   ├── wgpu-mc/                    # 核心渲染库
│   │   ├── src/lib.rs              # WmRenderer 结构
│   │   └── Cargo.toml              # wgpu 23, futures, raw-window-handle
│   └── wgpu-mc-jni/                # JNI 桥接层
│       ├── src/lib.rs              # nativeSetWindow/renderFrame/resize
│       └── Cargo.toml              # jni 0.21, log, env_logger
└── 步骤.md                         # 本文档
```

### 1.3 Rust 依赖配置

**`rust/wgpu-mc-jni/Cargo.toml`**：
```toml
[package]
name = "wgpu-mc-jni"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
log = "0.4"
env_logger = "0.10"
wgpu-mc = { path = "../wgpu-mc" }
```

**`rust/wgpu-mc/Cargo.toml`**：
```toml
[package]
name = "wgpu-mc"
version = "0.1.0"
edition = "2021"

[dependencies]
wgpu = "23"
bytemuck = { version = "1.14", features = ["derive"] }
log = "0.4"
env_logger = "0.10"
futures = "0.3"
raw-window-handle = "0.6"
winit = "0.30"
windows-sys = { version = "0.59", features = ["Win32_Foundation", ...] }
```

### 1.4 Rust JNI 桥接层

**`rust/wgpu-mc-jni/src/lib.rs`** — 导出 5 个 native 方法：

```rust
use std::sync::Mutex;
static RENDERER: Mutex<Option<wgpu_mc::WmRenderer>> = Mutex::new(None);

// 1. nativeInit() — 模组启动时调用，初始化日志
// 2. nativeHello(String) — 测试 JNI 字符串通信
// 3. nativeTestDeviceInfo() — 返回 GPU 可用性信息
// 4. nativeSetWindow(long hwnd) — 传入 Minecraft 窗口 HWND，创建 WmRenderer
// 5. nativeRenderFrame() → byte[] — 渲染一帧并返回 RGBA 像素数据
// 6. nativeResize(int width, int height) — 更新渲染器窗口尺寸
```

关键实现：
- `nativeSetWindow` 中调用 `wgpu_mc::WmRenderer::create(width, height)` 初始化 wgpu
- `nativeRenderFrame` 从 `RENDERER` 全局 Mutex 取出渲染器，调用 `render_frame()` 返回 `Vec<u8>`
- 像素数据通过 `env.set_byte_array_region()` 拷贝到 Java byte array

### 1.5 Java JNI 桥接层

**`D3D12Bridge.java`** — 封装 native 方法 + 业务逻辑：

```java
// Native 方法声明
public static native void nativeInit();
public static native String nativeHello(String input);
public static native String nativeTestDeviceInfo();
public static native void nativeSetWindow(long hwnd);
public static native byte[] nativeRenderFrame();
public static native void nativeResize(int width, int height);

// 便利方法
public static void init();                    // 一次性初始化
public static String sayHello(String msg);    // JNI 测试
public static long getWindowHandle();         // 通过 LWJGL GLFW 获取 HWND
public static void setWindow(long hwnd);      // 仅在 HWND 变化时调用 nativeSetWindow
public static ByteBuffer renderFrame();       // 调用 nativeRenderFrame，转换 byte[] → ByteBuffer
public static void syncWindowSize(int w, int h); // 仅在尺寸变化时调用 nativeResize
```

关键实现：
- `getWindowHandle()` 使用 `GLFW.glfwGetCurrentContext()` + `GLFWNativeWin32.glfwGetWin32Window()` 获取 HWND
- `setWindow()` 有 `lastSetHwnd` 缓存，重复 HWND 直接跳过
- `syncWindowSize()` 有 `lastWidth`/`lastHeight` 缓存，尺寸不变跳过
- DLL 从 JAR 提取到 `{user.dir}/dx12mod/wgpu_mc_jni.dll`，支持版本隔离

### 1.6 Fabric 模组入口

**`Dx12Mod.java`** — 实现 `ClientModInitializer`：

```java
@Override
public void onInitializeClient() {
    D3D12Bridge.init();
    D3D12Bridge.sayHello("Hello from Minecraft!");
    D3D12Bridge.getDeviceInfo();
}
```

### 1.7 构建配置

**`fabric/build.gradle`**：
```gradle
plugins {
    id 'fabric-loom' version '1.10.3'
    id 'maven-publish'
}

dependencies {
    minecraft "com.mojang:minecraft:1.21.1"
    mappings "net.fabricmc:yarn:1.21.1+build.3:v2"
    modImplementation "net.fabricmc:fabric-loader:0.19.3"
    modImplementation "net.fabricmc.fabric-api:fabric-api:0.116.13+1.21.1"
}

tasks.withType(JavaCompile).configureEach {
    it.options.release = 21
}
```

### 1.8 验收结果

```
[INFO] GL4DX12 Mod initializing...
[D3D12Bridge] Native library loaded from: ...\dx12mod\wgpu_mc_jni.dll
[D3D12Bridge] Rust JNI library initialized.
[INFO] Rust responded: Hello from Rust wgpu! You said: Hello from Minecraft!
[INFO] Device info: wgpu-mc-jni loaded. DX12: READY
[INFO] GL4DX12 Mod initialized!
```

---

## ✅ 第二阶段：wgpu 渲染引擎骨架

**状态：已完成**

### 2.1 WmRenderer 核心结构

**`rust/wgpu-mc/src/lib.rs`**：

```rust
pub struct WmRenderer {
    pub device: wgpu::Device,
    pub queue: wgpu::Queue,
    pub width: u32,
    pub height: u32,
}

impl WmRenderer {
    pub fn create(width: u32, height: u32) -> Result<Self, &'static str> {
        // 1. 创建 wgpu::Instance，后端指定 DX12
        let instance = wgpu::Instance::new(wgpu::InstanceDescriptor {
            backends: wgpu::Backends::DX12,
            ..Default::default()
        });

        // 2. 请求高性能适配器（NVIDIA/AMD/Intel DX12 GPU）
        let adapter = block_on(instance.request_adapter(...)).ok_or("No adapter")?;

        // 3. 请求 Device + Queue
        let (device, queue) = block_on(adapter.request_device(...))
            .map_err(|_| "Device failed")?;

        Ok(Self { device, queue, width, height })
    }

    pub fn resize(&mut self, width: u32, height: u32) {
        self.width = width;
        self.height = height;
    }

    pub fn render_frame(&mut self) -> Vec<u8> {
        // 当前生成纯色帧（蓝色 RGBA: 0,0,128,255）
        // 后续替换为实际 Minecraft 场景渲染
        let mut pixels = vec![0u8; w * h * 4];
        for chunk in pixels.chunks_exact_mut(4) {
            chunk[0] = 0;   // R
            chunk[1] = 0;   // G
            chunk[2] = 128; // B
            chunk[3] = 255; // A
        }
        pixels
    }
}
```

关键点：
- 使用 `futures::executor::block_on` 将异步 wgpu 调用转为同步
- `backends: DX12` 强制使用 DirectX 12 后端
- 当前 `render_frame()` 生成纯色像素（后续替换为实际渲染）

### 2.2 验收结果

- Rust wgpu 成功初始化 DX12 adapter
- `render_frame()` 每帧返回 1920x1080x4 = 8,294,400 字节 RGBA 数据
- 通过 JNI 成功传递给 Java

---

## ✅ 第三阶段：Fabric 事件系统集成

**状态：已完成（架构调整：不用 Mixin，用 Fabric API 事件系统）**

### 3.1 架构设计

原始计划使用 Mixin 拦截 `LevelRenderer.renderLevel()`，但改为 Fabric API 事件系统：

| 组件 | 职责 | 回调 |
|------|------|------|
| Tick Callback | 计时、资源重载检测、调用 Rust 渲染 | `ClientTickEvents.END_CLIENT_TICK` |
| HUD Callback | OpenGL 纹理上传 + 全屏 quad 绘制 | `HudRenderCallback.EVENT` |

### 3.2 Tick Callback 逻辑

```java
ClientTickEvents.END_CLIENT_TICK.register(client -> {
    // 1. 获取 GLFW 窗口尺寸（主线程安全）
    GLFW.glfwGetWindowSize(glfwWindow, wBuf, hBuf);

    // 2. 资源重载检测：tick 间隔 > 2s 说明正在加载资源
    if (lastTickTime > 0 && (now - lastTickTime) > 2000) {
        renderReady = false;
        vaoId = 0; shaderValid = false; texAllocated = false; pendingPixels = null;
    }

    // 3. 启动延迟：等待 3s 确保 Minecraft GL 状态稳定
    if (!renderReady) {
        if (elapsed < 3000) return;
        renderReady = true;
    }

    // 4. 二次缓冲：renderReady 后再等 2s
    if (now - renderStartTime < 2000) return;

    // 5. 节流：每 100ms 调用一次 Rust 渲染，避免 GPU 竞争
    if (lastRenderTime > 0 && (now - lastRenderTime) < 100) return;

    // 6. 更新 HWND（仅在变化时）
    if (hwnd != 0 && hwnd != lastHwnd) {
        D3D12Bridge.setWindow(hwnd);
        lastHwnd = hwnd;
    }
    D3D12Bridge.syncWindowSize(width, height);

    // 7. 调用 Rust 渲染
    ByteBuffer pixels = D3D12Bridge.renderFrame();
    if (pixels != null) {
        pendingPixels = pixels;  // 交给 HUD callback 绘制
    }
});
```

时序控制常量：

| 常量 | 值 | 用途 |
|------|-----|------|
| `LOADING_GAP_MS` | 2000ms | 检测资源重载（tick 间隔 > 2s） |
| `RENDER_DELAY_MS` | 3000ms | 启动后延迟 3s 再渲染 |
| 二次缓冲 | 2000ms | 资源重载后额外等待 2s |
| 节流间隔 | 100ms | 每 100ms 调用一次 Rust 渲染 |

### 3.3 HUD Callback 逻辑

```java
HudRenderCallback.EVENT.register((drawContext, tickDelta) -> {
    if (pendingPixels == null) return;

    // 1. 纹理：首次分配，之后每帧更新
    if (!texAllocated) {
        texId = glGenTextures();
        glTexImage2D(GL_TEXTURE_2D, 0, GL_RGBA8, width, height, 0, GL_RGBA,
                     GL_UNSIGNED_BYTE, (ByteBuffer) null);
        texAllocated = true;
    }
    glTexSubImage2D(GL_TEXTURE_2D, 0, 0, 0, width, height, GL_RGBA,
                    GL_UNSIGNED_BYTE, pixels);

    // 2. Shader：持久化
    if (!shaderValid) {
        shaderProg = createShaderProgram();  // GLSL 330 core
        texUniformLoc = glGetUniformLocation(shaderProg, "uTexture");
    }

    // 3. VAO：持久化
    if (vaoId == 0) vaoId = createVAO();  // 全屏 quad + VBO + IBO

    // 4. 绘制
    glUseProgram(shaderProg);
    glUniform1i(texUniformLoc, 0);
    glBindVertexArray(vaoId);
    glDisable(GL_DEPTH_TEST);
    glEnable(GL_BLEND);
    glBlendFunc(GL_SRC_ALPHA, GL_ONE_MINUS_SRC_ALPHA);
    glDrawElements(GL_TRIANGLES, 6, GL_UNSIGNED_BYTE, 0L);

    // 5. 解绑
    glBindVertexArray(0);
    glBindTexture(GL_TEXTURE_2D, 0);
    glUseProgram(0);
});
```

### 3.4 GLSL Shader

**Vertex Shader**：
```glsl
#version 330 core
layout(location = 0) in vec3 aPos;
layout(location = 1) in vec2 aTexCoord;
out vec2 vTexCoord;
void main(){ gl_Position = vec4(aPos, 1.0); vTexCoord = aTexCoord; }
```

**Fragment Shader**：
```glsl
#version 330 core
in vec2 vTexCoord;
uniform sampler2D uTexture;
out vec4 FragColor;
void main(){ FragColor = texture(uTexture, vTexCoord); }
```

### 3.5 VAO 数据结构

全屏 quad 顶点（position xyz + texture uv xy = 5 floats/vertex）：

```
(0,0,0) (0,0)  → 左下角
(1,0,0) (1,0)  → 右下角
(0,1,0) (0,1)  → 左上角
(1,1,0) (1,1)  → 右上角
```

索引：`[0,1,2, 2,1,3]` → 两个三角形覆盖全屏

### 3.6 验收结果

- 模组加载成功，无 crash
- 蓝色覆盖层正常显示（1920x1080 @ RGBA）
- 日志输出：`Rendering frame: 1639680 bytes (frame=1/61/121...)`
- 按 Esc 不会 crash（已修复 pendingPixels 清理）

---

## ❌ 第四阶段：实际 Minecraft 场景渲染

**状态：未开始**

### 4.1 当前限制

当前 `render_frame()` 只生成纯色帧。要实现实际 Minecraft 场景渲染，需要：

1. **从 Minecraft 获取场景数据**
   - 方块类型、位置、光照
   - 实体位置、旋转、皮肤
   - 粒子效果

2. **Rust 端渲染管线**
   - 顶点缓冲区（方块网格）
   - 索引缓冲区（面剔除）
   - 着色器（光照、纹理）
   - 渲染通道（不透明 → 半透明 → 粒子）

3. **数据传递优化**
   - 方块状态压缩（BlockstateKey = u32）
   - 增量更新（只传变化的区块）
   - 批量传输（减少 JNI 调用次数）

### 4.2 参考 wgpu-mc 架构

wgpu-mc 的 RenderGraph 设计：

```yaml
passes:
  - name: "sky_pass"
    render_target: "main"
    shader: "sky.wgsl"
    depth_test: false

  - name: "terrain_pass"
    render_target: "main"
    shader: "terrain.wgsl"
    depth_test: true

  - name: "entities_pass"
    render_target: "main"
    shader: "entity.wgsl"
    depth_test: true
    blending: alpha

  - name: "particles_pass"
    render_target: "main"
    shader: "particle.wgsl"
    depth_test: false
    blending: alpha
```

### 4.3 实现优先级

1. **区块渲染**（性能收益最大）— 方块网格生成 + 顶点缓冲
2. **天空盒与云雾** — 简单着色器即可
3. **实体渲染** — 模型加载 + 骨骼动画
4. **粒子系统** — 点精灵（point sprites）
5. **半透明物体排序** — 深度排序算法
6. **后期特效** — 泛光、阴影、色调映射

---

## 🔧 部署说明

### 5.1 DLL 存放位置

模组打包时 DLL 嵌入 JAR，运行时自动提取到：

```
{user.dir}/dx12mod/wgpu_mc_jni.dll
```

例如版本隔离目录：
```
D:\.minecraft\versions\1.21.1-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll
```

### 5.2 构建步骤

```bash
# 1. 构建 Rust DLL
cd rust
cargo build --release
# 输出: target/release/wgpu_mc_jni.dll

# 2. 将 DLL 复制到 JAR 资源
# （手动或通过 Gradle 任务）

# 3. 构建 Fabric 模组
cd fabric
./gradlew build
# 输出: build/libs/gl4dx12-0.1.0.jar

# 4. 安装到 .minecraft
# 将 .jar 复制到 .minecraft/mods/
# DLL 会自动提取到版本隔离目录
```

### 5.3 已知问题与解决方案

| 问题 | 原因 | 解决方案 | 状态 |
|------|------|----------|------|
| `glTexImage2D(pixels)` 一步完成 crash | NVIDIA 驱动 bug | 改用 `glTexImage2D(null)` + `glTexSubImage2D(pixels)` 两步 | ✅ 已修复 |
| `glTexSubImage2D` 在页边界 ACCESS_VIOLATION | NVIDIA 驱动按页粒度预读，缓冲区非页对齐 | `MemoryUtil.memAlloc` + 4KB 填充，`buf.limit(pixels.length)` | ✅ 已修复 |
| 按 Esc/设置菜单 crash | HUD callback 与 Screen 渲染 GL 状态冲突 | `currentScreen != null` 时跳过 GL 绘制 | ✅ 已修复 |
| 缓冲区大小不匹配导致 nvoglv64 越界 | 帧大小与窗口尺寸不一致 | 用 `bufferBytes` 反推安全 `height` 再调用 glTexSubImage2D | ✅ 已修复 |
| 资源重载后渲染 crash | GL 状态未清理 | 重载检测时重置 `vaoId`/`shaderValid`/`texAllocated`/`pendingPixels` | ✅ 已修复 |
| 每帧调用 Rust 渲染 freeze | GPU 命令队列竞争 | 节流到每 100ms 一次 | ✅ 已修复 |
| 调试日志导致卡顿 | 每 tick 写磁盘 | 移除所有非必要日志 | ✅ 已修复 |
| `setWindow` 重复调用 | JNI 开销 + 日志轰炸 | `lastSetHwnd` 缓存，相同 HWND 直接跳过 | ✅ 已修复 |

---

## 📊 当前状态总结

### ✅ 已修复（稳定性）
- 纹理上传两步分离法（glTexImage2D + glTexSubImage2D）
- 页对齐 ByteBuffer 分配（MemoryUtil.memAlloc + 4KB 填充）
- Esc/设置菜单 GL 冲突检测（currentScreen 检查）
- 缓冲区边界安全钳（bufferBytes vs expectedBytes）
- 资源重载后 GL 状态清理
- 节流策略（100ms）
- 日志静默化
- HWND 去重调用

### ✅ 已完成（核心功能）
- 第一阶段：JNI 通信链路完全打通
- 第二阶段：wgpu DX12 渲染引擎可返回像素数据
- 第三阶段：Fabric 事件系统集成，OpenGL 全屏 quad 绘制正常
- 蓝色覆盖层渲染正常
- 进游戏、按 Esc、调设置均无 JVM 崩溃

### ⚠️ 待优化
- 渲染节流策略（当前每 100ms 调用一次 Rust 渲染）
- 性能监控和 FPS 统计
- 窗口尺寸变化时的平滑过渡

### ❌ 未开始
- 实际 Minecraft 场景渲染（当前是纯色覆盖层）
- RenderGraph 渲染管线
- 区块/实体/粒子渲染
- 顶点数据压缩优化

---

## 📝 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 游戏 | Minecraft | 1.21.1 |
| 加载器 | Fabric Loader | 0.19.3 |
| API | Fabric API | 0.116.13+1.21.1 |
| 语言 | Java | 21 |
| 图形 | wgpu (WebGPU) → DX12 | 23 |
| 语言 | Rust | 2021 edition |
| JNI | jni crate | 0.21 |
| 渲染 | OpenGL (Java 端) | Core Profile 330 |

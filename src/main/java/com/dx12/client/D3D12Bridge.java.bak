package com.dx12.client;

public class com.dx12.D3D12Bridge {
    static {
        // ?? DLL ??? Dx12Mod ? NativeUtils ????????
    }
    public static native int glGenBuffers();
    public static native void glBindBuffer(int target, int buffer);
    public static native void glBufferData(int target, java.nio.ByteBuffer data, int usage);
    public static native boolean nativeInit(long hwnd, int width, int height);
}

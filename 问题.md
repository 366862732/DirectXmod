# DirectXmod 问题分析

> 基于 `步骤.md`、`README.md`、C++ 原生层（`native/src/`）及 Java 层（`fabric/src/main/java/com/dx12/dx12/`）代码综合分析。
> 分析日期：2026-08-17。

---

## 🔴 严重问题（影响运行时渲染）

### P1：全场景强制绿色覆盖（诊断代码未清除）

**文件**：[Dx12ShaderCompiler.java](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12ShaderCompiler.java#L105-L130)

**现象**：所有 Fragment Shader 被 `stripFragMainAndReplace()` 强制替换为输出纯绿色：
```java
sb.append("void frag_main() { fragColor = float4(0.0, 1.0, 0.0, 1.0); }\n");
```

**位置**：[第 109–122 行](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12ShaderCompiler.java#L109-L122)

**根因**：原为 `core/gui` + `core/panorama` 专项调试用的绿色覆盖，注释写的是"去掉管线名过滤，覆盖 core/terrain 等主渲染管线"，但此分支从未恢复。当前 `compile()` 方法对**每一条 Fragment Shader**（terrain/entity/particle/lightmap 全部）都强制执行绿色替换。

**影响**：
- 世界渲染（terrain）全绿，实体全绿，粒子全绿
- GUI 本身不受影响（gui 管线不走此路径，或 gui 管线有独立 GLSL 源码）
- 所有诊断日志 `"[DIAG] frag forced GREEN (all pipelines)"` 每帧重复输出

**修复方向**：恢复管线名过滤，仅对测试用管线（`core/gui`、`core/panorama`）保留绿色覆盖，或完全移除诊断代码。

---

### P2：blit 传参已修复但未确认提交状态

**文件**：[Dx12GpuSurface.java](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12GpuSurface.java#L71-L77)

**现象**：README 标注为"🔧 未提交"，但当前 Java 代码已正确实现：
```java
Dx12GpuTexture tex = (Dx12GpuTexture) ((Dx12GpuTextureView) textureView).texture();
Dx12Native.dx12BlitSurface(encoder.nativeHandle(), this.handle, tex.handle());
```

**确认**：Java 侧代码已正确传入 `texture().handle()`，非 `textureView.handle()`。**此问题已在 Java 层修复，待确认 DLL 版本是否同步。**

---

## 🟡 中等问题（潜在稳定性/正确性风险）

### P3：semanticNames 重建与 HLSL 注入语义可能不一致

**文件**：[Dx12ShaderCompiler.java](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12ShaderCompiler.java#L125-L129)

```java
List<String> semanticNames = new ArrayList<>();
for (int i = 0; i < vertexInputNames.size(); ++i) {
    semanticNames.add(i == 0 ? "POSITION" : ("TEXCOORD" + (i - 1)));
}
return new Dx12CompiledShader(vertexHlsl, fragmentHlsl, entries, vertexInputNames, semanticNames);
```

同时，[Dx12IntermediaryShaderModule.java](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12IntermediaryShaderModule.java#L279-L297) 的 `toHlsl()` 中：
```java
String semantic = (inputIdx == 0) ? "POSITION" : ("TEXCOORD" + (inputIdx - 1));
SpvcHlslVertexAttributeRemap remap = ...
remap.location(loc);
remap.semantic(stack.UTF8(semantic));
Spvc.spvc_compiler_hlsl_add_vertex_attribute_remap(compiler, remap, 1);
```

**分析**：两边逻辑一致（location 0 → POSITION，其余 → TEXCOORD<n>），但存在以下风险：
- 某些管线（如 `debug_points`）使用内置语义（`gl_Position` 等）注入 SPVC semantic 后，HLSL 变量名可能与 Java 侧重建的 `TEXCOORD0`/`TEXCOORD1` 不对齐
- 若 spvc 自动生成的语义名与手动注入的语义名冲突，PSO 创建可能失败

**建议**：统一从 `compiled.semanticNames()` 取值，而非 Java 侧重建。步骤文档提到 `9d2847d` 已"移除过时的手动 HLSL 语义注入逻辑"，但当前代码仍有两处手动注入（`toHlsl()` + `compile()`），需核实 `9d2847d` 实际改动范围。

---

### P4：pipelineCache.clearPipelineCache() 导致启动期重编

**文件**：[Dx12Backend.java](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Backend.java#L93)

```java
// 自测阶段用嵌入 GLSL 填充了 pipeline cache...
// 必须在返回给游戏前清除
device.clearPipelineCache();
```

**现象**：自检阶段编译了 `core/gui` + `core/position_tex_color` 两条管线（使用了内嵌 GLSL 源）。`createDevice()` 返回前调用 `clearPipelineCache()` 清空缓存，游戏启动后首次渲染时这两条管线会重新编译。

**影响**：启动时首帧 GUI 渲染会有一次 shader 编译停顿（约 50–200ms）。功能正确，但有可感知的卡顿。

**建议**：改用单独 cache 存储自检管线，或在 `clearPipelineCache()` 后按需重新编译这两条管线（它们的 GLSL 是确定的，编译代价低）。

---

### P5：诊断日志过多，影响性能与可读性

**文件分布**：
- [Dx12ShaderCompiler.java:110–122](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12ShaderCompiler.java#L110-L122)：每个管线 `compile()` 打印 4 行 `[DIAG]` 日志
- [Dx12CommandEncoderBackend.java:140–155](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12CommandEncoderBackend.java#L140-L155)：每次 `createRenderPass()` 打印调用栈
- [Dx12RenderPassBackend.java:298–305](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12RenderPassBackend.java#L298-L305)：每次 `drawIndexed()` 打印参数
- [Dx12GpuSurface.java:80–123](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12GpuSurface.java#L80-L123)：每 30 帧读回 color texture + backbuffer 像素（阻塞 GPU idle）
- [Dx12Device.java:384–399](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L384-L399)：`buildVertexInputElements()` 对非 GUI 管线每次都 `warn` 打印格式

**影响**：
- 每帧多次 draw 调用产生大量 stderr 输出，拖慢渲染线程
- `dx12ReadbackTexturePixels` / `dx12ReadbackSurfacePixels` 在 Java 侧每 30 帧触发 native 层 `deviceWaitIdle()`，造成同步阻塞
- `createRenderPass` 每帧调 `Thread.currentThread().getStackTrace()`，产生不必要的 GC 压力

**建议**：所有诊断日志通过 `DX12_LOG_VERBOSE` 环境变量控制，默认关闭。读回操作应仅在自检阶段执行。

---

### P6：GPU 读回函数使用静态缓存，多线程不安全

**文件**：[jni_bridge_p5.cpp](file:///d:/dx12-lib-template-26.1.2/native/src/jni_bridge_p5.cpp#L150-L177) 及 [dx12_surface.cpp](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_surface.cpp#L508-L544)

```cpp
static ComPtr<ID3D12Resource> staging;
static ComPtr<ID3D12CommandAllocator> alloc;
static ComPtr<ID3D12GraphicsCommandList> cl;
```

**现象**：`dbgReadbackTexturePixels()` 和 `readbackSurfacePixels()` 使用文件级 static 缓存 staging/allocator/command list。若未来有多个线程同时调用读回（如诊断工具并发），会产生竞态。

**影响**：当前单线程渲染路径下安全，但架构上存在隐患。

**建议**：改为在 `DeviceContext` 中维护一个 per-thread 或 lock-protected 的读回缓冲区池。

---

## 🟢 已知已通过（记录在案，确认状态）

| 问题 | 状态 | 备注 |
|------|------|------|
| RootSignature 未设置（UMD AV） | ✅ 已修复 | cd1e029，`setPipeline` 先调 `SetGraphicsRootSignature` |
| 命令列表拓扑未设置 | ✅ 已修复 | `setPipeline` 补 `IASetPrimitiveTopology` |
| 顶点语义硬编码 → 纯红显示 | ✅ 已修复 | 88a9253/cb653a7，从 pipeline binding 反射取语义 |
| DXGI adapter 不匹配 | ✅ 已修复 | d688fce，`GetAdapterLuid` → `EnumAdapterByLuid` |
| 顶点缓冲 stride 错误 | ✅ 已修复 | dcaff2a，按 DXGI_FORMAT 实际字节数计算 |
| 交换链 ResizeBuffers 竞态 | ✅ 已修复 | 715895e/9e494b8，`deviceWaitIdle` + 重试 10 次 |
| 命令编码器共享导致渲染异常 | ✅ 已修复 | 共享单例 encoder |
| 描述符堆耗尽 | ✅ 已修复 | free-list + kSrvHeapSize=65536 |
| gl_PointSize UNSUPPORTED_SPIRV | ✅ 已修复 | HLSL_POINT_SIZE_COMPAT 选项 |
| HLSL static/const 误判 | ✅ 已修复 | 86b5f7c/784d331 |
| SemanticName 悬空指针 | ✅ 已修复 | 使用 `std::vector<std::string>` 持久化 |
| 三 ring allocator + fence 等待 | ✅ 已修复 | 三帧飞行，value%3 |
| 命令列表状态残留（TDR 冻结） | ✅ 已修复 | endRenderPass 显式回切 COMMON |

---

## 🔜 后续规划（步骤.md 明确列出）

| 任务 | 优先级 | 状态 |
|------|--------|------|
| 完整 Shader 支持（terrain/entity/particle 等全量） | 🔴 P0 | 🚧 当前绿色覆盖阻止验证，需先修复 P1 |
| 天空盒渲染 | 🟠 P1 | 未开始 |
| 实体渲染（模型+骨骼动画） | 🟠 P1 | 未开始 |
| 粒子系统（点精灵 quad 展开） | 🟡 P2 | 未开始 |
| spvc 语义注入 API 升级（Khronos #2579） | 🔜 远期 | 消除字符串后处理 |

---

## 总结：当前最优先修复项

1. **P1（紧急）**：恢复 `stripFragMainAndReplace()` 的管线名过滤或移除诊断代码，否则 terrain 等场景全绿不可用
2. **P3**：统一 semanticNames 来源，避免与 spvc 注入语义产生歧义
3. **P4**：考虑缓存自检管线，避免启动首帧重编卡顿
4. **P5**：将诊断日志/读回操作统一受 `DX12_LOG_VERBOSE` 控制，默认关闭

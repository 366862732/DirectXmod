# 黑屏问题全面审查报告

> 审查范围：`native/src/` + `fabric/src/main/java/com/dx12/dx12/`
> 审查日期：2026-08-22
> 当前状态：GUI 层渲染正常，但进入游戏世界后完全黑屏（terrain/entity 零 draw call）

---

## 一、黑屏根因（最高优先级）

### BUG-00：`minecraft.level` 始终为 null，`renderLevel()` 从不执行

**位置**：所有帧的 `GameRendererRenderDebugMixin` 输出

**现象**：debug.log 中所有采样帧均显示：
```
levelNotNull=0  advanceGameTime=true  resourcesLoaded=1
willRenderLevel=false
```
Terrain 管线（solid_terrain / translucent_terrain / cutout_terrain）零 draw call，只有 GUI 层在渲染。

**影响**：`renderLevel()` 从不被调用，游戏世界内容完全不渲染，导致黑屏。

**诊断已添加**：`MinecraftSetLevelDebugMixin`（P19）已注入 `Minecraft.setLevel()` 监听，但日志未显示该 mixin 触发，说明 `setLevel` 可能未被调用或调用时机在 mixin 注入之后。

**排查方向**：
1. 确认启动时选择了单人游戏并成功进入世界
2. 检查 Fabric 26.2 的 `Minecraft.setLevel()` 是否仍为正确的注入点
3. 检查 `MinecraftSetLevelDebugMixin` 是否在 `gl4dx12.mixins.json` 中正确注册（已确认注册）

---

## 二、Native 层 Bug

### BUG-01：`beginRenderPass` 中 `activeColorTargets` 重复 push_back（已修复）

**位置**：[dx12_device.cpp:1769](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1769)

**状态**：✅ 已修复。注释已明确说明 "diag loop 只打印日志，不再重复 push_back"。

---

### BUG-02：`toPrimitiveTopology` case 4 (TRIANGLES) 缺失

**位置**：[dx12_device.cpp:1923-1936](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1923-L1936)

**问题**：MC 的 PrimitiveTopology 枚举中 4 = TRIANGLES，但 switch 无 `case 4`，落入 `default` 返回 TRIANGLELIST。虽然结果相同（防御性不足），但若未来新增枚举值会静默回退。

```cpp
// L1928-1934: 缺少 case 4
case 4: return D3D_PRIMITIVE_TOPOLOGY_TRIANGLELIST;  // TRIANGLES — 应显式添加
```

---

### BUG-03：`submitCommandList` 中 `value` 变量阴影

**位置**：[dx12_device.cpp:1225-1236](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1225-L1236)

**问题**：L1225 声明 `UINT64 value = ctx->fenceValue + 1`（临时诊断值）；L1235 重新赋值 `value = ++ctx->fenceValue`（正确提交值）。L1227-1230 的每 30 帧诊断使用了旧 `value`（比实际 fence value 少 1），导致日志帧号偏移。

---

### BUG-04：CBV offset 256 对齐未验证

**位置**：[dx12_device.cpp:2518-2520](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L2518-L2520)

**问题**：注释声明 `offset 须 256 对齐`，但代码无断言或日志验证。若 Java 侧传入未对齐 offset，D3D12 验证层报 ERROR 但驱动可能静默忽略 → shader 读到错误 UBO 数据 → 黑屏。

**修复建议**：添加
```cpp
if (b.offset % 256 != 0) {
    err = "pushDescriptors CBV offset not 256-aligned: " + std::to_string(b.offset);
    return false;
}
```

---

### BUG-05：reverse-Z pre-clear 依赖（已修复但有隐患）

**位置**：[dx12_device.cpp:1739-1746](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1739-L1746)

**状态**：P21 已移除 depthClearFlag=0 时的无条件 clear=1.0 回退。依赖 MC vanilla pre-clear（`clearDepthTexture(0.0)`）正常工作。若 pre-clear 未执行则深度缓冲含垃圾值，GREATER_EQUAL 测试丢弃全部片元 → 黑屏。

---

### BUG-06：`createGraphicsPipeline` stride 修正逻辑有重复计算

**位置**：[dx12_device.cpp:2093-2177](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L2093-L2177)

**问题**：代码先构建 inputLayout（L2046-2092），然后重新计算 correctedStride，再清空 inputLayout 重新构建（L2130-2177）。第一次构建的 inputLayout 完全被丢弃。虽然功能正确但浪费一次遍历。

---

### BUG-07：`flushPendingDeletes` 无堆满检查

**位置**：[dx12_device.cpp:39-52](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L39-L52)

**问题**：`allocSrvSlot` 中当堆满时调用 `flushPendingDeletes()` 后仍然可能 `gNextSrv >= kSrvHeapSize`，此时直接返回 -1 报 "srv heap exhausted"。但 `flushPendingDeletes` 归还的槽位数量可能仍不足（pending 为空时 flush 无效果）。此场景下应有更友好的错误提示。

---

## 三、Java 层 Bug

### BUG-08：`buildVertexInputElements` 对 null bindings 未做安全保护

**位置**：[Dx12Device.java:400](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L400)

**问题**：L383 检查了 `bindings == null` 并警告，但 L400 的 `for (int i = 0; i < bindings.length; i++)` 仍会执行，导致 NPE。虽然 L383-399 的 if 块中有提前 continue/return 的逻辑，但外层的 L400 循环在 `bindings == null` 时会 NPE。

```java
// L383: warnings logged but execution continues
if (bindings == null || bindings.length == 0) {
    LOGGER.warn(...);
    // ... logging code
}
// L400: no null check before accessing bindings.length
for (int i = 0; i < bindings.length; i++) {  // ← NPE if bindings == null
```

---

### BUG-09：`selfTestSurface` 注释与实际行为不符

**位置**：[Dx12Backend.java:389](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Backend.java#L389)

**问题**：注释说 "presented GREEN"，但实际 `blitSurface(srcTex=null)` 走 no-copy pass-through（L410-418），不清空也不填色，只是 barrier 往返。`DIAG_CLEAR_BACKBUFFER_TO_GREEN` 宏已设置为 0（[dx12_surface.cpp:23](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_surface.cpp#L23)），所以屏幕不会变绿。

**修复**：将 L389 的 "GREEN" 改为 "PASS" 或类似描述。

---

### BUG-10：`createRenderPass` 每帧调用 `getStackTrace()` 性能问题

**位置**：[Dx12CommandEncoderBackend.java:142](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12CommandEncoderBackend.java#L142)

**问题**：每个 render pass 创建时调用 `Thread.currentThread().getStackTrace()`，这是一个非常昂贵的操作（会 Walk 整个调用栈）。在渲染循环中每帧可能有数十个 render pass，累积性能开销显著。

**修复建议**：仅在 `DX12_LOG_VERBOSE=1` 时调用。

---

### BUG-11：`Device.close()` 中 `sharedCommandEncoder` 双重访问

**位置**：[Dx12Device.java:260-264](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L260-L264)

**问题**：
```java
Dx12CommandEncoderBackend encoder = this.sharedCommandEncoder;  // L260
if (encoder != null) {
    encoder.close();
    Dx12Native.dx12DestroyCommandEncoder(encoder.nativeHandle());
    this.sharedCommandEncoder = null;  // L264
}
```
L260 取引用后 L264 才置 null，逻辑正确。但若有线程在 L260-L264 之间调用 `createCommandEncoder()`，会创建一个新 encoder 但旧 encoder 已经被 close+destroy，导致新 encoder 的 ctx 被意外销毁。虽然 close() 通常在 render thread 上调用，但此处无线程同步保护。

---

### BUG-12：`Dx12Backend.createDevice` 自测缓存未清除的风险

**位置**：[Dx12Backend.java:91-94](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Backend.java#L91-L94)

**问题**：自测使用 `EMBEDDED_SHADER_SOURCE` 编译了 gui/gui_textured 管线并写入 pipelineCache。L94 调用 `device.clearPipelineCache()` 清除。但若自测过程中 `device.close()` 被提前调用（L88 的 catch 块），`clearPipelineCache` 不会执行，pipelineCache 中残留自测条目。

**实际风险**：低，因为 catch 块会 rethrow 异常并导致设备创建失败，游戏会回退 OpenGL。

---

## 四、TransientMemory / Fence 层

### BUG-13：`transientMemory.close` 时 `queuedFrames=1` 泄漏

**位置**：[Dx12TransientMemory.java:223](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12TransientMemory.java#L223)

**现象**：debug.log 中频繁出现：
```
transientMemory.close: current=0 queuedFrames=1
```

**原因**：`FRAMES_IN_FLIGHT = 2`，`UBO_RING_COUNT = 3`。rotate() 每次将当前 frame 加入 deque，保持最多 2 个 frame。close() 时 `frames.size()` 为 1（最后一帧尚未 rotate 出去），但所有 buffer 都正确关闭了。

**实际影响**：关闭时正确释放，不影响运行时。但日志有误导性。

---

### BUG-14：`createFence` 使用全局队列 fence 的竞态风险

**位置**：[Dx12CommandEncoderBackend.java:277-278](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12CommandEncoderBackend.java#L277-L278)

**问题**：`target = fenceValue + 1`，其中 `fenceValue` 通过 JNI 调用获取。若多个 encoder 并发调用 `createFence()`，它们的 target 值可能相同或相邻，导致 fence 提前完成（其他 encoder 的 submit 已推进 queueFenceValue）。

**当前状态**：单线程渲染路径下暂未触发，但架构上存在隐患。

---

## 五、Surface / Swapchain 层

### BUG-15：`blitSurface` srcTex=null 路径不清空 backbuffer

**位置**：[dx12_surface.cpp:410-418](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_surface.cpp#L410-L418)

**问题**：当 `srcTex=null`（self-test 路径），`blitSurface` 只做 PRESENT↔COPY_DEST barrier 往返，不清空 backbuffer。注释说 "用于 self-test / render loop 自检"，但如果生产代码中某处错误传了 null srcTex，backbuffer 将保留上一帧内容 → 画面残留/闪烁。

---

### BUG-16：`configureSurface` 中 `err` 残留问题

**位置**：[dx12_surface.cpp:157-205](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_surface.cpp#L157-L205)

**问题**：`deviceWaitIdle(err)` 失败时设置 err，但后续 Retry 循环中若 ResizeBuffers 成功，err 仍保留旧值。调用方可能误判为失败。

---

### BUG-17：`readbackSurfacePixels` 每次调用都分配新 staging

**位置**：[dx12_surface.cpp:588-610](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_surface.cpp#L588-L610)

**问题**：`static ComPtr<ID3D12Resource> staging` 的重置条件 `staging->GetDesc().Width < total` 使用 Width 作为尺寸判断，但 staging 是 buffer（非 texture），Width = total（字节数）。若分辨率变化导致 total 变化，会重新分配。正常情况下 staging 复用正确。此行为可接受。

---

## 六、Shader 编译层

### BUG-18：`semanticNames` 补齐逻辑与 spvc 基准潜在不一致

**位置**：[Dx12Device.java:431-433](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L431-L433)

**问题**：补齐时使用 `TEXCOORD` + `semanticNames.size()`（递增索引），而 spvc 的 `toHlsl()` 基准可能使用不同的命名策略。

**当前状态**：debug.log 显示 terrain 管线 `inputElems=4, sems=[POSITION, TEXCOORD0, TEXCOORD1, TEXCOORD2]` 编译 OK，暂未触发。

---

### BUG-19：`buildNativeDesc` 中 semantic names 总字节数计算含 null terminator

**位置**：[Dx12Device.java:443-445](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L443-L445)

**问题**：`sn.getBytes(UTF_8).length` 返回字节数（不含 null terminator），但 native 侧读取时使用 `snBytes.length` 作为长度并拷贝，后续 C++ 代码用 `strlen` 或逐字节读取。需要确认 native 侧的读取方式是否与 Java 侧一致。

---

## 七、优先级汇总

| 编号 | 问题 | 严重度 | 黑屏相关 | 状态 |
|------|------|--------|----------|------|
| BUG-00 | `level=null` 导致 `renderLevel()` 永不执行 | 🔴 P0 | **直接根因** | 🔍 排查中 |
| BUG-02 | `toPrimitiveTopology` case 4 缺失 | 🟡 低 | 否 | 🔧 待修 |
| BUG-03 | `value` 变量阴影 | 🟡 低 | 否（仅诊断） | 🔧 待修 |
| BUG-04 | CBV offset 256 对齐未验证 | 🟠 中 | 潜在 | 🔧 待修 |
| BUG-05 | Reverse-Z pre-clear 依赖 | 🟠 中 | 潜在 | ✅ 已修复 |
| BUG-08 | `buildVertexInputElements` null bindings NPE | 🟠 中 | 潜在 | 🔧 待修 |
| BUG-09 | selfTestSurface 注释说 GREEN 实际不是 | 🟡 低 | 否 | 🔧 待修 |
| BUG-10 | `getStackTrace()` 每帧调用性能问题 | 🟡 低 | 否 | 🔧 待修 |
| BUG-13 | queuedFrames=1 日志误导 | 🟡 低 | 否 | ⚠️ 已知 |
| BUG-14 | createFence 竞态风险 | 🟠 中 | 潜在 | 🔧 待修 |
| BUG-15 | blitSurface srcTex=null 不清空 | 🟡 低 | 潜在 | ⚠️ 已知 |
| BUG-16 | configureSurface err 残留 | 🟡 低 | 否 | 🔧 待修 |
| BUG-18 | semanticNames 补齐逻辑 | 🟡 低 | 潜在 | ✅ 已验证通过 |

---

## 八、下一步行动

1. **P0 优先**：排查 `level=null` 根因
   - 运行游戏并检查 `MinecraftSetLevelDebugMixin` 的输出
   - 确认是否进入了单人世界
   - 若 setLevel 未被调用，检查 Fabric 26.2 的 `Minecraft` 类是否有 API 变更

2. **P1 修复**：
   - BUG-08：`buildVertexInputElements` 添加 null bindings 的 early return
   - BUG-04：`pushDescriptors` 添加 CBV offset 256 对齐断言

3. **P2 加固**：
   - BUG-02：补全 `toPrimitiveTopology` case 4
   - BUG-16：`configureSurface` 重试成功后清除 err
   - BUG-09：修正 selfTestSurface 注释

4. **性能优化**：
   - BUG-10：将 `getStackTrace()` 改为条件日志


---

## 🔍 DX12 渲染黑屏问题——系统性排查清单

**目标**：定位从“渲染命令提交”到“屏幕显示”之间的断裂点。

### ✅ 第一阶段：验证 Backbuffer 通路（确保画面能显示）

| 步骤 | 操作 | 预期结果 | 你的观察 |
|:---|:---|:---|:---|
| **1.1** | 在 `blitSurface` 中，用 `ClearRenderTargetView` 把 Backbuffer 清成纯红色（`1.0, 0.0, 0.0, 1.0`）。 | 屏幕变红。 | ⬜ |
| **1.2** | 在 `blitSurface` 中，用 `CopyTextureRegion` 把一张已知的、非零的纹理（比如测试用的青色三角形纹理）复制到 Backbuffer。 | 屏幕上出现青色三角形。 | ⬜ |

**如果 1.1 或 1.2 失败** → 问题在交换链或 Present（`CreateSwapChain`、`ResizeBuffers`、`Present`）。直接跳到第三阶段。

**如果 1.1 成功** → Backbuffer 通路正常，问题在 `src` 纹理的内容。

---

### ✅ 第二阶段：验证 `src` 纹理的内容

| 步骤 | 操作 | 预期结果 | 你的观察 |
|:---|:---|:---|:---|
| **2.1** | 在 `blitSurface` 中，用 `dbgReadbackTexturePixels` 读回 `src` 纹理的前 16 个像素（RGBA），打印到日志。 | 像素值非零，且看起来像正确的颜色。 | ⬜ |
| **2.2** | 在 `blitSurface` 中，检查 `src` 纹理的格式。如果是 `R16_UNORM`（深度/单通道），而不是 `RGBA8_UNORM`（彩色），则打印警告。 | 格式为 `RGBA8_UNORM` 或类似的彩色格式。 | ⬜ |

**如果 2.1 失败（像素全零）** → 场景没有渲染到 `src` 纹理上。检查 `beginRenderPass` 绑定的 RenderTarget 是否正确。

**如果 2.2 失败（格式不对）** → 你找到了根本原因：`blitSurface` 在拷贝深度/单通道纹理。检查 `blitSurface` 的调用方，确保它传入的是最终的彩色输出纹理（`post_texture` 或 `main_target`）。

---

### ✅ 第三阶段：验证交换链与 Present

| 步骤 | 操作 | 预期结果 | 你的观察 |
|:---|:---|:---|:---|
| **3.1** | 确认 `CreateSwapChain` 的格式是 `DXGI_FORMAT_R8G8B8A8_UNORM`。 | 是。 | ⬜ |
| **3.2** | 在 `presentSurface` 中，用 `GetDesc` 打印 Backbuffer 的格式和尺寸，确认与窗口匹配。 | 格式为 `DXGI_FORMAT_R8G8B8A8_UNORM`，尺寸与窗口一致。 | ⬜ |
| **3.3** | 检查 `ResizeBuffers` 是否在窗口尺寸变化时被正确调用。 | 是。 | ⬜ |

**如果 3.1 失败** → 交换链格式错误，需要修改。

**如果 3.2 失败** → 交换链配置与窗口不匹配。

**如果 3.3 失败** → 窗口缩放时渲染会错位或黑屏。

---


> “请按以下清单逐项排查，每做完一步告诉我结果。不要跳过任何一步，也不要添加额外的‘优化’或‘修复’，除非排查指向了具体问题。
>
> 1. 第一步：在 `blitSurface` 中用纯红色清空 Backbuffer，告诉我屏幕是否变红。
> 2. 如果变红，第二步：读取 `src` 纹理的像素，告诉我前 16 个像素的 RGBA 值，以及纹理的格式。
> 3. 根据这两步的结果，我们再决定下一步。”

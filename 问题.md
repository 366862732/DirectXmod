# 黑屏问题审查报告

> 审查范围：`native/src/` + `fabric/src/`
> 审查日期：2026-08-22
> 当前状态：主菜单 GUI 渲染修复中（BUG-GUI-01 已修复待验证），世界渲染阻塞于 BUG-00

---

## 已修复的 Bug

### BUG-GUI-01（P21）：GUI 管线 `inputElts=0` 导致顶点数据缺失 → 全部图元被裁剪

**位置**：[Dx12Device.java:383](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L383)

**问题**：`buildVertexInputElements` 中硬编码跳过 GUI 管线：
```java
if (bindings == null || bindings.length == 0 ||
    pipeline.getLocation().toString().contains("gui")) { ... }
```
导致 GUI 管线 `inputElts=0`，VS 读不到 POSITION/TEXCOORD，gl_Position 全零，所有图元被视锥裁剪，颜色附件从未写入（`touched=0`）。

**修复**：移除 `contains("gui")` 条件。修复后 GUI 管线将正确构建 4 个 input element（POSITION, TEXCOORD0, TEXCOORD1, TEXCOORD2）。

**验证点**：新日志应出现 `gui inputElems=3`（或 4）且 `endRenderPass touched=1`。

---

### BUG-RING-01（P20）：root descriptor table 地址每帧跳变

**位置**：[dx12_device.cpp:2583](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L2583)

**问题**：`SetGraphicsRootDescriptorTable(0, gpu+base)` 中 `base` 每帧在 0 和 1MB 之间跳变（fenceValue%2），shader 读到错位描述符。

**修复**：固定使用 `gpuRoot = heapBase`（不从 base 偏移）。CPU 写描述符仍用 base 偏移写入正确半区。

---

### BUG-RING-02（P20）：ring buffer x2 不够覆盖三帧并行

**位置**：[dx12_device.cpp:76-77](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L76-L77)

**问题**：3 帧飞行（N、N+1、N+2），但 ring buffer 只有 2 个半区，帧 N+2 CPU 写入时可能覆盖帧 N GPU 仍在读取的半区。

**修复**：改为 fenceValue%4，4 个半区确保安全性。

---

## 当前未解决的问题

### BUG-00：`minecraft.level` 始终为 null，`renderLevel()` 从不执行

**现象**：debug.log 全篇（66,280 行）所有采样帧均显示 `levelNotNull=0`，游戏日志中也从未出现 `levelNotNull=1`。

**影响**：
- `GameRenderer.render()` 中 `shouldRenderLevel = resourcesLoaded && advanceGameTime && level != null` → false
- `renderLevel()` 永不执行
- terrain 管线（solid/translucent/cutout）**零 draw call**
- 全部渲染调用来自 `GuiRenderer`（主菜单）
- 若 GUI 也渲染失败（BUG-GUI-01），则表现为**纯黑屏**

**证据**：
```
frame=1:  resourcesLoaded=0 levelNotNull=0 backend=DX12 paused=false
frame=30: resourcesLoaded=1 levelNotNull=0 backend=DX12 paused=false
frame=60: resourcesLoaded=1 levelNotNull=0 backend=DX12 paused=false
frame=90: resourcesLoaded=1 levelNotNull=0 backend=DX12 paused=false
```
所有 `createRenderPass` 调用栈均为 `...GuiRenderer.executeDrawRange GuiRenderer.draw GuiRenderer.render GameRenderer.render`，**无任何 TerrainRenderer 调用**。

**分析**：
- 官方 gl4dx12 的 mixin 与我们的完全相同（`GameRendererRenderDebugMixin` 仅打印诊断，不修改逻辑）
- `PreferredGraphicsApiMixin` 同样存在于官方和我们的实现
- terrain 管线在 backend 初始化阶段预编译成功（`solid_terrain COMPILE OK`），说明 shader 编译通路正常
- **该问题可能与用户的测试方式有关**：用户是否实际进入了单人世界？

**下一步行动**：
1. 用户需重新启动游戏，选择"单人游戏"进入世界
2. 进入世界后观察 GUI 是否正常显示（验证 BUG-GUI-01 修复）
3. 若世界仍然黑屏，提供新的 debug.log，检查 `levelNotNull` 是否变为 1

---

## 其他已知问题（低优先级）

| 编号 | 问题 | 优先级 | 黑屏相关 |
|------|------|--------|----------|
| BUG-01 | `activeColorTargets` 重复 push_back | P3 | 否（幂等，无害） |
| BUG-02 | `toPrimitiveTopology` case 4 缺失 | P3 | 否（default 分支正确） |
| BUG-06 | CBV offset 256 对齐未验证 | P2 | 潜在（D3D12 验证层会报错） |
| BUG-07 | 纹理 COMMON 状态采样 | P1 | 已修复 |
| BUG-15 | `queuedFrames=1` transient 泄漏 | P2 | 长期风险 |

---

## 修复时间线

| 时间 | 修复 | 文件 |
|------|------|------|
| 上一轮 | P19 depth disable 开关 | dx12_device.cpp |
| P20 | root table 地址跳变修复 | dx12_device.cpp |
| P20 | ring buffer x2→x4 | dx12_device.cpp |
| P21 | GUI inputElts=0 修复 | Dx12Device.java:383 |

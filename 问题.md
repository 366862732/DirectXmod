# 黑屏问题审查报告

> 审查范围：`native/src/` + `fabric/src/`
> 审查日期：2026-08-22（最新：2026-08-23）
> 当前状态：GUI 渲染层已修复，待用户部署并进入单人世界验证世界渲染

---

## 已修复的 Bug

### BUG-GUI-01（P21）：GUI 管线 `inputElts=0` 导致顶点数据缺失 → 全部图元被裁剪 ✅

**位置**：[Dx12Device.java:383](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12Device.java#L383)

**修复**：移除 `contains("gui")` 硬编码跳过条件。

---

### BUG-RING-01（P20）：root descriptor table 地址每帧跳变 ✅

**位置**：[dx12_device.cpp:2585](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L2585)

**修复**：固定使用 `gpuRoot = heapBase`（不从 base 偏移）。

---

### BUG-RING-02（P20）：ring buffer x2 不够覆盖三帧并行 ✅

**位置**：[dx12_device.cpp:76-77](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L76-L77)

**修复**：改为 fenceValue%4，4 个半区确保安全性。

---

### BUG-01（语义名索引错位）：semanticNames 生成基准错误 ✅

**位置**：[Dx12ShaderCompiler.java:130](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12ShaderCompiler.java#L130)

**问题**：semanticNames 循环用 `vertexInputNames.size()`（format 条目数）作基准，
但 spvc 注入的 remap 数量由 `vertex.inputs().size()`（SPIR-V unique inputs）决定。
两者不相等时（如存在多个输入共享同一 location），语义名列表与 input element
列表错位 → D3D12 绑定错误语义 → 顶点数据读不到 POSITION → 黑屏。

**修复**：改为 `vertex.inputs().size()`，与 spvc remap 计数对齐。

---

### P22（GUI 深度 PSO 误选）：GUI 管线在有 depth attachment 的 pass 中启用深度测试 ✅

**位置**：[Dx12RenderPassBackend.java:110-111](file:///d:/dx12-lib-template-26.1.2/fabric/src/main/java/com/dx12/dx12/Dx12RenderPassBackend.java#L110-L111)

**问题**：`setPipeline` 用 `this.hasDepth`（pass 是否有 depth attachment）判断
是否启用深度测试。GUI pass 有时有 depth attachment（如 GUI_TEXTURED pass），
但 GUI 管线本身没有 `DepthStencilState`，若启用深度测试会选中 withDepth PSO
（GREATER_EQUAL 比较丢弃所有 fragment）→ GUI 黑屏。

**修复**：改用 `compiled.info().getDepthStencilState() != null` 判断管线本身是否有
深度状态，再结合 pass 是否有 depth attachment 决定最终 `useDepth`。

---

## 当前待验证的问题

### BUG-00：`minecraft.level` 始终为 null，`renderLevel()` 从不执行

**现象**：debug.log 全篇显示 `levelNotNull=0`，游戏日志中也从未出现 `levelNotNull=1`。

**分析**：
- 官方 gl4dx12 的 mixin 与我们的完全相同（`GameRendererRenderDebugMixin` 仅打印诊断）
- terrain 管线在 backend 初始化阶段预编译成功（`solid_terrain COMPILE OK`）
- **该问题与测试方式有关**：需要用户实际进入单人世界才能触发

**待验证步骤**：
1. 手动部署最新 JAR/DLL（桌面已准备好）
2. 启动游戏，选择"单人游戏"进入世界
3. 检查 debug.log 中 `levelNotNull` 是否变为 1
4. 若 world 仍黑屏，收集新 debug.log 确认 draw call 来源

---

## 其他已知问题（低优先级）

| 编号 | 问题 | 优先级 | 黑屏相关 |
|------|------|--------|----------|
| BUG-02 | `toPrimitiveTopology` case 4 缺失 | P3 | 否（default 分支正确） |
| BUG-06 | CBV offset 256 对齐未验证 | P2 | 潜在（D3D12 验证层会报错） |
| BUG-15 | `queuedFrames=1` transient 泄漏 | P2 | 长期风险 |

---

## 修复时间线

| 时间 | 提交 | 修复内容 |
|------|------|----------|
| 2026-08-23 | `c5dae3b` | BUG-01 语义名索引 + P22 深度 PSO 误选 |
| 2026-08-23 | `1c3905e` | BUG-GUI-01 移除 GUI 跳过条件 |
| 2026-08-14 | `6f7d655` | BUG-RING-02 ring buffer x2→x4 |
| 2026-08-14 | `17e534d` | BUG-RING-01 root table 地址跳变修复 |

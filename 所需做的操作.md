你的代码已经添加了 `g_globalDeviceReady` 标志并在 `SafeCleanD3D` 中置 false，但**关键的 JNI 入口函数并未检查该标志**，导致在清理过程中仍可能被调用并访问已释放资源。这是当前崩溃的直接原因。

---

## 🛠 彻底修复方案

### 1. 在所有 JNI 函数开头检查 `g_globalDeviceReady`
需要修改以下函数（返回 `void` 的）在开头添加：
```cpp
if (!g_globalDeviceReady) return;
```
对于返回非 `void` 的（如 `nativeIsD3D12Active` 返回 `jboolean`），返回默认值（如 `JNI_FALSE`）。

#### 需要添加的函数列表（已检查部分，未检查的需补充）：
- ✅ `nativeRenderSky` — **已有检查**
- ✅ `nativeRenderTransparent` — **已有检查**
- ✅ `nativeRenderParticles` — **已有检查**
- ❌ `nativeRecordVertices` — **缺少**
- ❌ `nativeRecordVerticesUV` — **缺少**
- ❌ `nativeRecordUV` — **缺少**
- ❌ `nativeRecordColors` — **缺少**
- ❌ `nativeDraw` — **缺少**
- ❌ `nativeUploadTextureEx` — **缺少**
- ❌ `nativeSetMvp` — **缺少**
- ❌ `nativeSetSkyParameters` — **缺少**
- ❌ `nativeUploadParticles` — **缺少**（虽然有 `g_deviceLost` 检查，但未检查 `g_globalDeviceReady`）
- ❌ `nativeUploadTransparent` — **缺少**
- ❌ `nativeRenderEntities` — **已有检查**（但内部也有 `g_globalDeviceReady` 检查）
- ❌ `nativeUploadEntities` — 直接 return 了，但应添加检查

#### 修改示例（`nativeRecordVertices`）：
在函数开头，现有检查之前添加：
```cpp
if (!g_globalDeviceReady) {
    // 可选：输出日志，然后返回
    return;
}
```
对于 `nativeSetMvp` 返回 void，同样添加。

对于 `nativeIsD3D12Active`（返回 `jboolean`），应返回 `g_ok && g_globalDeviceReady ? JNI_TRUE : JNI_FALSE;`，因为即使 `g_ok` 为 true，若全局标志 false，也应返回 false。

---

### 2. 确保 `RenderLoop` 在 `g_globalDeviceReady` false 时立即退出
当前 `RenderLoop` 在循环开头检查：
```cpp
if (!g_globalDeviceReady) { Log("[ERROR] Device not ready, skip render"); Sleep(16); continue; }
```
但注意 `Sleep(16); continue;` 会继续循环，不会退出线程。如果 `g_globalDeviceReady` 变为 false，我们希望线程尽快退出，而不是无限循环。建议改成：
```cpp
if (!g_globalDeviceReady) {
    Log("[INFO] Device not ready, exiting render thread.");
    g_run = false;
    break;
}
```
这样线程会退出，避免后续继续运行。

---

### 3. 在 `SafeCleanD3D` 中增加渲染线程退出等待时间
当前等待 3000ms，可能不够，可改为循环等待，增加超时处理：
```cpp
if (g_thread) {
    DWORD waitRet = WaitForSingleObject(g_thread, 3000);
    if (waitRet == WAIT_TIMEOUT) {
        OutputDebugStringA("[WARN] Render thread did not exit, forcing termination.\n");
        TerminateThread(g_thread, 0);
    }
    CloseHandle(g_thread);
    g_thread = nullptr;
}
```
（注意 `TerminateThread` 有风险，但这是备选方案，主要依靠标志让线程自行退出。）

---

### 4. 在 `RenderLoop` 中，若检测到 `g_deviceLost` 或 `!g_globalDeviceReady` 后，设置 `g_run = false` 并 `break`，而不是 `continue`。
当前 `g_deviceLost` 检查后设置了 `g_run = false; break;`，但 `!g_globalDeviceReady` 分支仅 `continue`，需统一为退出。

---

### 5. 在所有 JNI 函数中，优先检查 `g_globalDeviceReady`，然后再检查 `g_dev` 和 `g_deviceLost`。
因为 `g_globalDeviceReady` 在清理开始时立即置 false，而 `g_dev` 可能尚未释放，但设备已无效。所以顺序应改为：
```cpp
if (!g_globalDeviceReady) return;
if (!g_dev || g_deviceLost.load()) { ... }
```

---

### 6. 对于 `nativeRenderSky` 和 `nativeRenderTransparent` 等内部使用 `BuildAlphaBlendPSO` 的函数，虽然已检查 `g_globalDeviceReady`，但调用 `BuildAlphaBlendPSO` 时会创建设备资源，应确保设备有效。

---

## 📦 修改汇总（代码片段）

### 修改 `RenderLoop` 中的 `!g_globalDeviceReady` 分支：
```cpp
if (!g_globalDeviceReady) {
    Log("[INFO] Device not ready, exiting render thread.");
    g_run = false;
    break;
}
```

### 修改所有 JNI 函数（示例）：
```cpp
JNIEXPORT void JNICALL Java_com_dx12_DX12LibClient_nativeRecordVertices(...) {
    if (!g_globalDeviceReady) {
        // 不抛出异常，直接返回（避免在清理时触发异常）
        return;
    }
    // 原有检查...
}
```

对于 `nativeIsD3D12Active`：
```cpp
JNIEXPORT jboolean JNICALL Java_com_dx12_DX12LibClient_nativeIsD3D12Active(JNIEnv*, jclass) {
    return (g_ok && g_globalDeviceReady) ? JNI_TRUE : JNI_FALSE;
}
```

---

## ✅ 预期效果
- 一旦 `SafeCleanD3D` 置 `g_globalDeviceReady = false`，所有 JNI 调用将立即返回，不再访问任何 D3D 资源。
- `RenderLoop` 也会退出，避免空转。
- 彻底杜绝设备移除后的崩溃。

---

## ⚠️ 注意事项
- 确保所有 `return` 前不要释放任何资源（因为清理时资源由 `SafeCleanD3D` 统一释放）。
- 如果某些 JNI 函数需要返回值，返回合理的默认值（如 `0`、`false` 等）以避免 Java 端 NPE。

按照以上修改重新编译 DLL，测试即可。如果还有问题，可进一步检查是否有未覆盖的 JNI 入口（如 `nativeBeginFrame`、`nativeEndFrame`、`nativeClearDrawList` 等，它们访问了 `g_frameReadyEvent`，事件对象在清理时被关闭，但设置事件是安全的，不过也建议加上检查）。

祝调试顺利！
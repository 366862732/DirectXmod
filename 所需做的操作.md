

## 🔧 最终修复方案（手动实施）

请按照以下步骤，确保修改正确并重新编译：

### 1. 彻底删除 `RenderLoop` 中的强制绘制代码

打开 `d3d12bridge.cpp`，搜索 `FORCE NDC RED TRIANGLE TEST`，如果找到，**删除整个相关代码块**（包括 `if (g_currentCoordType == 2) { ... }` 以及内部的绘制逻辑）。确保没有任何残留。

### 2. 确认 `InitStage` 枚举包含 `ST_NDC_TEST`

在枚举定义中，确认顺序如下：
```cpp
enum InitStage {
    STAGE_EMPTY = 0,
    STAGE_BASE_DEVICE,
    ST_CMD_RES,
    ST_UI_PSO_CBUF,
    ST_3D_PSO_CBUF,
    ST_NDC_TEST,           // 确保这行存在，且位置正确
    ST_SKY_TEX,
    // ... 其他
    ST_FULL_READY
};
```

### 3. 在 `ProcessNextInitStage` 中添加 `ST_NDC_TEST` 处理

在 switch 中添加：
```cpp
case ST_NDC_TEST:
    Log("[INIT STAGE] ST_NDC_TEST - drawing red triangle");
    DrawRedTriangle();
    break;
```

### 4. 在 `RenderLoop` 中清空残留绘制命令

在 `if (g_initStage >= ST_FULL_READY)` 块的开头（进入正常渲染循环之前），添加：
```cpp
// 清空可能残留的绘制命令（调试用）
EnterCriticalSection(&g_stateLock);
g_drawChunks.clear();
g_imVertCount = 0;
g_imVBSize = 0;
LeaveCriticalSection(&g_stateLock);
```

### 5. 确保 `DrawRedTriangle` 函数只做最简单的绘制

该函数应只包含红色三角形的上传、绘制、Present 和同步，不涉及其他任何操作。

### 6. 重新编译并测试

- 清理所有构建缓存（删除 `build` 和 `.gradle` 下的缓存，或执行 `gradle clean`）。
- 重新编译 C++ DLL 和 Java 代码。
- 复制 DLL 到 `dx12mod` 目录。
- 运行游戏，观察日志中是否出现 `[INIT STAGE] ST_NDC_TEST`，并且没有 `DrawChunk` 相关的绘制日志（或极少）。

## 📋 预期结果

如果以上修改生效，你会看到：
- `[INIT STAGE] ST_NDC_TEST` 日志。
- 红色三角形单独在屏幕上稳定显示，无 GPU 超时。
- 不再有 `DrawChunk` 执行（因为已被清空）。

如果仍然出现 TDR，可以尝试在 `DrawRedTriangle` 中降低绘制复杂度（例如只绘制一个点），并确保在 `Present` 时使用 `SyncInterval=1` 限制帧率。


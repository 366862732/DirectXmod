
### 任务1：在 Java 层添加绝对路径 DLL 加载

在 `DX12LibClient.java` 的静态块中添加 `System.load`，使用最新 DLL 的绝对路径：

```java
// 在 DX12LibClient.java 类中
static {
    try {
        // 使用最新编译的 DLL 绝对路径
        String dllPath = "D:\\dx12-lib-template-26.1.2\\src\\main\\native\\windows\\d3d12bridge\\x64\\Release\\d3d12bridge.dll";
        System.load(dllPath);
        System.out.println("[GL4DX12] DLL loaded: " + dllPath);
    } catch (UnsatisfiedLinkError e) {
        System.err.println("[GL4DX12] DLL load failed: " + e.getMessage());
        throw e;  // 不吞没异常，让调用方知道
    }
}
```

### 任务2：优化异常处理，不再吞没错误

在 `D3D12Bridge.ensureDeviceInitialized()` 中，改进异常处理：

```java
// 在 D3D12Bridge.java 中
try {
    nativeInit(hwnd);
    d3d12Ready = true;
} catch (Throwable t) {
    // 不吞没，向上抛出并记录详细信息
    System.err.println("[GL4DX12] nativeInit FAILED: " + t.getMessage());
    t.printStackTrace();
    throw new RuntimeException("D3D12 nativeInit failed", t);
}
```

### 任务3：添加 `hwnd` 有效性校验

在调用 `nativeInit` 前，验证 `hwnd` 是否有效：

```java
public static void ensureDeviceInitialized() {
    if (d3d12Ready) return;
    
    // 获取窗口句柄
    long hwnd = getWindowHandle();
    if (hwnd == 0) {
        throw new IllegalStateException("Invalid window handle (hwnd=0)");
    }
    
    // 调用 nativeInit
    nativeInit(hwnd);
    d3d12Ready = true;
}
```

### 任务4：重新编译并测试

```cmd
cd D:\dx12-lib-template-26.1.2\src\main\native\windows
build.bat
```

---

## 📋 预期结果

执行完成后：
1. DebugView 出现 `[GL4DX12] DLL loaded: ...`
2. DebugView 出现 `[nativeInit] === STEP 0: ENTER ===`
3. 后续 STEP 1, 2, 3... 出现，帮助定位初始化挂起点

---

**请本地AI按以上任务顺序执行，并汇报结果。** 🚀

---

## 🛠️ 新的诊断方法

### 1. 添加 `g_ok` 检查，防止重复初始化

在 `nativeInit` 开头添加：

```cpp
if (g_ok) {
    Log("[nativeInit] Already initialized, returning true");
    return JNI_TRUE;
}
```

### 2. 在 `RenderLoop` 中添加更详细的退出日志

在 `while (true)` 循环开头添加：

```cpp
Log("[RenderLoop] Loop iteration start, g_run=%d, g_deviceLost=%d", g_run, g_deviceLost.load());
```

### 3. 检查设备丢失的原因分类

在 `EXIT` 日志中添加更多上下文：

```cpp
Log("[RenderLoop] EXIT: Device lost, g_deviceLost=%d, g_run=%d, g_ok=%d, reason=0x%08X", 
    g_deviceLost.load(), g_run, g_ok, reason);
```

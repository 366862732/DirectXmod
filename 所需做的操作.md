（请本地Agent执行）

### 操作1：在 `nativeInit` 中添加分段日志

在 `d3d12bridge.cpp` 中找到 `nativeInit` 函数，在每一步 D3D12 操作前后添加 `OutputDebugStringA`：

```cpp
JNIEXPORT jboolean JNICALL Java_com_dx12_DX12LibClient_nativeInit(JNIEnv* env, jobject thiz, jlong hwnd) {
    OutputDebugStringA("[nativeInit] === STEP 0: ENTER ===");
    
    // 创建设备
    OutputDebugStringA("[nativeInit] === STEP 1: Creating D3D12Device ===");
    HRESULT hr = D3D12CreateDevice(nullptr, D3D_FEATURE_LEVEL_11_0, IID_PPV_ARGS(&g_dev));
    if (FAILED(hr)) {
        char buf[256]; sprintf_s(buf, "[nativeInit] ERROR: D3D12CreateDevice failed: 0x%08X", hr);
        OutputDebugStringA(buf);
        return JNI_FALSE;
    }
    OutputDebugStringA("[nativeInit] === STEP 1: D3D12Device created OK ===");
    
    // 创建交换链
    OutputDebugStringA("[nativeInit] === STEP 2: Creating SwapChain ===");
    // ...
    OutputDebugStringA("[nativeInit] === STEP 2: SwapChain created OK ===");
    
    // 创建命令队列
    OutputDebugStringA("[nativeInit] === STEP 3: Creating CommandQueue ===");
    // ...
    OutputDebugStringA("[nativeInit] === STEP 3: CommandQueue created OK ===");
    
    // ... 后续步骤
    OutputDebugStringA("[nativeInit] === STEP DONE: All initialized ===");
    return JNI_TRUE;
}
```

### 操作2：编译并替换

```cmd
cd d:\dx12-lib-template-26.1.2\src\main\native\windows
build.bat
```

### 操作3：运行并观察

启动 DebugView，运行游戏，观察最后出现的 STEP 编号。

---

## 📋 预期结果

如果卡在：
- `STEP 1` → D3D12CreateDevice 挂起（驱动问题）
- `STEP 2` → CreateSwapChain 挂起（窗口/显示问题）
- `STEP 3` → CreateCommandQueue 挂起（资源问题）
- `STEP 4+` → 其他 D3D12 操作挂起

---

**请本地Agent执行上述修改，然后用户重新编译测试，提供新的 DebugView 日志。** 🔍
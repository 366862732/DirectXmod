# D3D12 全异步渲染架构 — 可行性审查

> 审查时间：2026-08-29  
> 方案文件：`多线程方案.md`  
> 审查范围：当前 native/Java 代码 vs 方案描述的 10 个章节

---

## 一、当前架构摘要（对比基线）

| 维度 | 当前实现 | 方案目标 |
|------|----------|----------|
| **命令录制** | 单线程（Minecraft 渲染线程），所有 draw 同步执行 | Bundle 录制分散到 N 个工作线程 |
| **Command Allocator** | 3 个 ring（`allocators[3]`，`fenceValue%3`）| 每个工作线程独立 allocator + 主 executor 独立 allocator |
| **Descriptor Heap** | 全局 `gNextSrv/gNextSampler` 单调递增 + free-list 归还 | `ThreadSafeDescriptorAllocator`（无锁 CAS free-list） |
| **Draw Heap** | 4 段半区 ring（`kDrawHeapSections=4`，`fenceValue%4`） | 共享全局 SRV/Sampler 堆，Bundle 内直接写 GPU 句柄 |
| **提交路径** | `submitCommandList()` = Execute + Signal，**非阻塞** | `MainCommandExecutor.addBundle()` 收集 Bundle，`endFrame()` ExecuteBundle + Signal |
| **Fence 同步** | Java 侧 `waitForQueueFenceValue()` 阻塞等待 | `AsyncFenceManager` 后台 1ms 轮询 CompletableFuture |
| **资源释放** | `flushPendingDeletes()` 在 submit 后立即 flush | `ResourceManager.reap()` 后台 ScheduledExecutor 每 16ms 检查 |

---

## 二、结构性问题（必须解决，否则无法运行）

### 🔴 BUG-ASYNC-01：Bundle 内写入全局 Descriptor Heap → 并发崩溃

**问题**：方案 `BundleRecorder::setDescriptorTable()` 直接调用
```cpp
m_bundle->SetGraphicsRootDescriptorTable(slot, handle);
```
`handle` 来自全局 `gCtx.srvHeap`（非 SHADER_VISIBLE 或 SHADER_VISIBLE）。多个 worker 同时录制 Bundle 时，都向**同一个全局 srvHeap** 写描述符，没有任何互斥保护。

当前代码的 `pushDescriptors()` 之所以安全，是因为它在**单线程**上串行执行（渲染线程依次调用 `dx12PushDescriptors`）。方案将其改成并发后，free-list 的 CAS 操作只保证"槽位不重复分配"，但：
- Worker A 分配槽位 X，写入描述符
- Worker B 分配槽位 X（CAS 抢到），覆盖 Worker A 的描述符
- Worker A 的 Bundle 包含被污染的 GPU 句柄

**修复方案**：
1. **最小改动**：每个 worker 持有独立的 SRV CPU-only 镜像堆（`srvCpuHeapWorker[N]`），Worker 写入 CPU 镜像堆，主线程 ExecuteBundle 前将 CPU 镜像堆内容复制到全局 srvHeap（通过 `CopyDescriptorsSimple`）
2. **较大改动**：`ThreadSafeDescriptorAllocator` 改为"分配即固化"，不再回收中间槽位（牺牲堆利用率，换正确性）

---

### 🔴 BUG-ASYNC-02：`ResourceBarrierCommand.execute()` 调用 `dx12ResourceBarrier(contextHandle,...)` 语义错误

方案文档明确说：
> 注意：Bundle 不能包含 ResourceBarrier！这个命令会在主 Command List 中执行

但 `execute()` 直接调用 `dx12ResourceBarrier(contextHandle, ...)`——这里 `contextHandle` 是主 Command List（`MainCommandExecutor.m_commandList`），而 Bundle 录制的 GPU 描述符来自 worker 线程，主线程 execute 时 worker 可能还没写完。**屏障的 Before/After 状态基于 bundle 录制时的资源状态，但主线程 execute 时资源实际状态可能已被其他 bundle 修改**，导致 barrier 的 Before 状态与实际不符，触发 D3D12 验证层 ERROR。

**修复方案**：
- 屏障不在 Bundle 系统中处理
- 改为在 `MainCommandExecutor::beginFrame()` 和 `endFrame()` 之间，按帧级依赖关系批量插入屏障（类似 vulkan 的 `VkDependencyInfo`）
- 或者：每个 Bundle 带一个 `resourceAccesses` 列表，主线程聚合所有 Bundle 后计算最小组Barrier

---

### 🔴 BUG-ASYNC-03：`MainCommandExecutor.waitForPreviousFrame()` 阻塞主线程，并行度为零

```cpp
bool MainCommandExecutor::waitForPreviousFrame() {
    ...
    DWORD result = WaitForSingleObject(m_fenceEvent, 16);
    if (result == WAIT_TIMEOUT) return false;  // 跳过本帧
    return result == WAIT_OBJECT_0;
}
```
此函数在主线程调用，如果上一帧 GPU 还在执行，主线程会 **block 最多 16ms**。这意味着：
- 当 GPU 繁忙时，主线程 16ms 都在等，根本没有任何"构建下一帧"的工作可做
- 方案声称"4-5 帧飞行"，但实际上主线程变成 **GPU 同步阻塞**，没有真正的流水线重叠

**修复方案**：
- 移除 `waitForPreviousFrame()` 中的阻塞等待
- 改为 `tryBeginFrame()`：尝试 begin，若 fence 未完成则返回 false（丢弃本帧或等下一帧重试）
- 使用 **fence event + 轮询**：主线程每帧检查 fence，未完成则提交当前帧的 bundle（不等），下一帧再检查

---

### 🟠 BUG-ASYNC-04：`AsyncFenceManager` 每 1ms 轮询 pendingFences ConcurrentHashMap，性能极差

```java
monitorExecutor.scheduleAtFixedRate(this::checkFences, 0, 1, TimeUnit.MILLISECONDS);
...
for (Map.Entry<Long, CompletableFuture<Void>> entry : pendingFences.entrySet()) {
    if (isFenceComplete(fenceValue)) {
        future.complete(null);
        pendingFences.remove(fenceValue);
    }
}
```
问题：
1. `isFenceComplete()` 调用 `Dx12Native.dx12IsFenceComplete(fenceValue)` → JNI 调用 → C++ `fence->GetCompletedValue()`，每帧最多 n 次 JNI
2. ConcurrentHashMap 遍历 O(n)，n 是 pending fence 数量（随帧飞行增加）
3. 每 1ms 唤醒一次 scheduled executor，造成大量上下文切换

**正确做法**：
- 使用 `SetEventOnCompletion(fenceValue, event)` + `WaitForMultipleObjects`，一次等待所有 pending fence
- 或在 C++ 层实现 fence monitor thread，用 `WaitForSingleObject` 代替 1ms 轮询
- Java 侧只需 `CompletableFuture.runAsync(() -> dx12WaitForFence(fenceValue), executor)` 即可

---

### 🟠 BUG-ASYNC-05：`ResourceManager.reap()` 后台线程调用 `dx12DestroyResource`

```java
reaper.scheduleAtFixedRate(this::reap, 16, 16, TimeUnit.MILLISECONDS);
...
Dx12Native.dx12DestroyResource(entry.nativeHandle);  // 在后台线程调用
```
D3D12 COM 对象（`ID3D12Resource*` 等）**必须在创建它的线程上释放**（或同一线程的 COM 初始化上下文中）。后台线程没有 `CoInitialize`，直接调用 COM 释放方法会导致：
- 访问违规（AV）
- 或不一致的引用计数（COM apartment 不同）

**修复方案**：
- 将待释放资源登记到 C++ 层的 `gPendingDeletes` 向量（已实现，见 [dx12_device.cpp:29-53](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L29-L53)）
- 由主线程在 `submitCommandList()` 末尾 `flushPendingDeletes()` 统一释放
- Java 侧只调 `dx12DestroyResource(resourceId)`（排队），不调用 native destroy

---

## 三、设计层面问题（影响正确性或复杂度）

### 🟡 ISSUE-ASYNC-06：`AsyncRenderCommand` 对象分配与 GC 压力

方案为每个 draw call 创建一个 `DrawIndexedCommand`（内含 `ByteBuffer.allocateDirect(20)`），按 Minecraft 的典型帧 draw 量（数千次），GC 压力巨大。

当前代码之所以没有这个问题，是因为 Java 侧直接调 JNI（`dx12DrawIndexed`），没有中间对象。方案引入的 command 包装层增加了：
- 每次 draw：1 个 `AsyncRenderCommand` + 1 个 `ByteBuffer`
- CommandPool 的 `localPool` 只缓解部分压力，但 `acquire()` 返回的仍是新对象（非真正 reuse）

**建议**：如果不做完整的 command 序列化，直接用 JNI 从 worker 线程录制 Bundle（跳过 Java command 层），避免 Java 对象分配。

---

### 🟡 ISSUE-ASYNC-07：`CommandPool.acquire()` 的 flyweight 语义错误

```java
if (index < POOL_SIZE && pool[index] != null) {
    AsyncRenderCommand cmd = pool[index];
    cmd.data.clear();  // ← 危险：其他线程可能正在使用此 cmd 的 data！
    cmd.type = type;
    index++;
    return cmd;
}
```
`ThreadLocal` 保证每个线程有自己的 pool，但 `cmd.data.clear()` 后复用的 ByteBuffer 如果在上一个 batch 处理完毕前被另一个线程（主线程 execute 时）访问，会出现数据竞争。

**建议**：`AsyncRenderCommand` 不做对象池复用，改为直接 new（配合 Off-Heap ByteBuffer 复用）。

---

### 🟡 ISSUE-ASYNC-08：方案未考虑 Minecraft 渲染管线的事件驱动特性

Minecraft 的渲染是**事件驱动**（`GameRenderer.render()` → `renderLevel()` → 各 pass）：
- 每个 pass 在 Java 侧按顺序调用 `setPipeline` → `pushDescriptors` → `drawIndexed`
- Pass 之间有隐式依赖（terrain pass 输出到 texture，gui pass 采样该 texture）

方案的 `FrameBuilder.build(AsyncRenderer)` 接口要求调用方**自己组织依赖关系**，这需要重写 Minecraft 的所有渲染 pass 调度逻辑（terrain/entity/particle/gui），工作量极大且侵入性极强。

**建议**：不要改 Java 侧的 pass 调度，只在 native 层做 Bundle 录制分流（见下文"推荐路径"）。

---

### 🟡 ISSUE-ASYNC-09：`BundleRecorder.drawIndexedIndirect` 实现有误

```cpp
void BundleRecorder::drawIndexedIndirect(... commandBuffer, ...) {
    m_bundle->ExecuteIndirect(nullptr, drawCount, commandBuffer, offset, nullptr, 0);
    //                                           ^^^^^ 应为 DrawIndexedInstanced 的 CommandSignature
}
```
第一个参数 `nullptr` 应为 `cmdSigIndexed.Get()`，否则 D3D12 不知道 indirect command 的布局，导致 GPU 错误。

---

### 🟢 ISSUE-ASYNC-10：`AsyncWorker.processCommands()` 每批命令只录一个 Bundle

```java
private void processCommands(int count) {
    recorder.begin();
    try {
        for (int i = 0; i < count; i++) {
            cmd.execute(recorder.getCurrentBundleHandle(), 0);
        }
    } finally {
        recorder.end();
    }
}
```
`BATCH_SIZE=64`，意味着 64 次 draw 在一个 Bundle 里。但不同 pass（terrain vs gui）的 PSO/rootSig 不同，Bundle 内切换 PSO 需要 Reset（方案未说明如何管理 Bundle 边界）。如果 terrain pass 的 64 个 draw 和 gui pass 的 64 个 draw 被分到同一 batch，会录制出无效 Bundle（PSO 不一致时 Reset 丢失先前命令）。

**建议**：Bundle 边界按 "PSO 变化" 或 "pass 结束" 切分，而非固定 batchSize。

---

## 四、性能收益评估（现实预期）

### 当前瓶颈分析

Minecraft 26.2 渲染线程的主要瓶颈（按典型场景排序）：
1. **Shader 编译**（首次加载，非每帧）
2. **Draw call 数量**（terrain 数千 draw，受限于 CPU→GPU 命令提交延迟）
3. **Descriptor 重新绑定**（每帧数千次 `SetGraphicsRootDescriptorTable`）
4. **State change**（PSO/buffer/viewport 切换）

### 方案能解决哪些？

| 瓶颈 | 方案能否解决 | 原因 |
|------|-------------|------|
| Draw call 数量 | ❌ 不能 | Bundle 只是把命令录制移到另一线程，GPU 执行时间不变 |
| Descriptor 绑定 | ⚠️ 部分 | 并发录制时减少单线程瓶颈，但描述符写入竞争是新瓶颈 |
| State change | ❌ 不能 | PSO 切换发生在录制时，不改变 GPU 端状态切换次数 |
| CPU→GPU 命令提交延迟 | ✅ 能 | Bundle 录制并行化可减少主线程耗时 |
| 帧时间（CPU 侧） | ✅ 理论 -70% | 但受限于上述 bug，实际收益可能 <20% |

### 现实预期

| 指标 | 当前 | 方案可行后（保守估计） | 方案乐观估计 |
|------|------|----------------------|-------------|
| 主线程帧时间 | ~8ms | ~5ms（-37%） | ~3ms（-62%） |
| CPU 利用率 | 60-70%（单核） | 80-90%（多核） | 90-95% |
| 帧时间波动 | 大（有 GC spike） | 中等 | 小 |
| 开发工作量 | — | **6-8 周（修 bug 为主）** | 3-4 周（假设无 bug） |

**结论**：对于当前 Minecraft D3D12 后端，**CPU 侧并行化的收益相对有限**（瓶颈更多在 GPU 和 draw call 本身），但架构复杂度大幅上升。

---

## 五、推荐实施路径（分阶段）

### Phase 0：先解决 level=null 黑屏根因（🔴 P0）
- 黑屏问题未解决前，任何性能优化都是空中楼阁
- 优先级：**远高于**异步渲染

### Phase 1：Bundle 录制单线程预研（2-3 周）
1. 在现有单线程渲染路径上，**选择性**地录制 Bundle：
   - 对每个 pass（terrain/gui/particle）单独录制一个 Bundle
   - 主 Command List 仅做 ExecuteBundle，不做具体 draw
   - **目的**：验证 Bundle 在现有架构下的正确性，不引入多线程
2. 测量 Bundle 录制的 CPU 时间 vs 直接 draw 的时间差
3. 若 Bundle 录制本身比直接 draw 慢（JNI 开销），则多线程收益更小

### Phase 2：引入 2 线程并行（3-4 周）
1. 工作线程只负责 **command list 录制**（不写 descriptor heap）
2. 描述符仍在全局堆中由主线程写入（录制前先推 descriptor，再录 draw）
3. 这样彻底规避 **BUG-ASYNC-01**（并发 descriptor 竞争）
4. 用 **2 线程**（而非 N 线程）降低复杂度

### Phase 3：full async（评估是否值得）
- 在 Phase 1/2 完成后，根据实测数据决定是否投入全量异步改造

---

## 六、总结

| 评估维度 | 结论 |
|----------|------|
| **正确性** | 🔴 当前方案有 3 个阻塞性 bug（并发 descriptor、屏障语义、主线程阻塞），0 个可接受 |
| **可行性** | 🟠 可行，但需大幅修改（尤其 BUG-01/02/03 的修复涉及架构重构） |
| **收益比** | 🟡 理论 CPU 时间 -70%，但实际瓶颈多在 GPU，收益存疑 |
| **复杂度** | 🔴 引入 7 个新组件（DescriptorAllocator/Bundler/AsyncQueue/FenceMgr/FrameMgr/ResourceManager/PerfProfiler），维护成本大幅上升 |
| **建议** | **暂不实施全量异步**；先解决 level=null，再做 Phase 1 单线程 Bundle 预研 |

---

## 附录：方案与当前代码的关键差异对照表

| 功能 | 当前代码位置 | 方案对应位置 | 差异说明 |
|------|-------------|-------------|---------|
| 命令提交 | [dx12_device.cpp:1314-1359](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1314-L1359) | `MainCommandExecutor.endFrame()` | 当前非阻塞；方案加了主线程阻塞等待 |
| Descriptor 分配 | [dx12_device.cpp:56-68](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L56-L68)（`gNextSrv/gNextSampler`）| `ThreadSafeDescriptorAllocator` | 当前单调递增+free-list；方案 CAS 无锁 |
| Fence 等待 | [dx12_device.cpp:1390](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L1390)（`waitForQueueFenceValue`）| `AsyncFenceManager`（Java 1ms 轮询）| 当前阻塞等待事件；方案轮询 CompletableFuture |
| 资源销毁 | [dx12_device.cpp:40-53](file:///d:/dx12-lib-template-26.1.2/native/src/dx12_device.cpp#L40-L53)（`flushPendingDeletes`）| `ResourceManager.reap()`（后台线程）| 当前主线程 flush；方案后台线程 destroy（⚠️ 线程不安全） |
| Bundle 录制 | ❌ 无 | `BundleRecorder` | 当前无 Bundle 支持，需从零实现 |

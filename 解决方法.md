## 问题：GUI 管线创建失败，报 "same Semantic" 错误

### 现象
```
[inputEl[0]: fmt=2 slot=0 offset=0 class=0 step=0 semantic=TEXCOORD
 inputEl[1]: fmt=28 slot=0 offset=12 class=0 step=0 semantic=TEXCOORD]
=== D3D12 validation messages ===
InfoQueue[ERROR] ID3D12Device::CreateInputLayout: Element[1] and Element[0]
  have the same Semantic (TEXCOORD 0). All Semantics in the Input Layout
  must be unique.
CreateGraphicsPipelineState hr=0x80070057 (E_INVALIDARG)
```

日志显示 Java 端已正确传递 `POSITION` 和 `TEXCOORD0`（见 `[dx12-java] inputs=... sems=[POSITION, TEXCOORD0]`），但 C++ 端 `inputEl` 两个元素都显示 `semantic=TEXCOORD`。

### 根因

`dx12_device.cpp` 的 `createGraphicsPipeline` 中，`baseName` 是 `for` 循环内的**局部 `std::string`**：

```cpp
for (const PipelineDesc::InputElement& el : desc.inputElements) {
    std::string baseName;
    // ... 解析语义名到 baseName ...
    ie.SemanticName = baseName.c_str();   // ← 悬空指针！
}
```

每次循环迭代结束，`baseName` 析构，其内部缓冲区可能被重新分配。**`ie.SemanticName` 保存的是指向已析构局部变量的 `c_str()` 指针**，是典型的 use-after-free。

D3D12 在后续 `buildPso` lambda 内读取 `inputLayout[i].SemanticName` 时，恰好复用了最后分配的 `"TEXCOORD"` 静态内存（MSVC debug 模式下的字符串池），导致两个元素都显示 `TEXCOORD` → D3D12 报语义重复错误。

**这不是"忽略了 semanticName"，而是"正确赋值了但指针已悬空"。**

### 修复

新增 `std::vector<std::string> semanticStore` 持久化存储语义名，`ie.SemanticName` 指向持久化内存：

```cpp
std::vector<std::string> semanticStore;
semanticStore.reserve(desc.inputElements.size());

for (const PipelineDesc::InputElement& el : desc.inputElements) {
    D3D12_INPUT_ELEMENT_DESC ie{};
    std::string baseName;
    // ... 解析语义名到 baseName ...
    semanticStore.push_back(baseName);
    ie.SemanticName = semanticStore.back().c_str();  // ✅ 持久化
    // ...
}
```

### 验证

修复后日志（`游戏日志 - 26.2-Fabric_0.19.3.log`）：
- 135 次管线创建全部成功，无 `E_INVALIDARG` / `same Semantic` 错误
- 语义解析正确：`POSITION idx=0`、`TEXCOORD0 idx=0`、`TEXCOORD1 idx=1`、`TEXCOORD2 idx=2`
- 游戏正常进入，无崩溃

### 修改文件

- `native/src/dx12_device.cpp`：`createGraphicsPipeline` 中新增 `semanticStore` 持久化存储
- 重新编译 → `native/build/bin/Release/dx12_mc.dll` → 复制到 `fabric/src/main/resources/dx12_mc.dll`

太好了！方案一才是正确的架构。让我们来实现它。

## 🛠️ 方案一实现：RenderLoop 等待数据准备好

### 1. 在 `d3d12bridge.cpp` 中添加全局事件

在文件顶部的全局变量区域（大约第 50 行附近）添加：

```cpp
// 在现有全局变量后面添加
static HANDLE g_frameReadyEvent = nullptr;   // 帧数据就绪事件
static HANDLE g_frameDoneEvent = nullptr;    // 帧渲染完成事件（可选）
```

### 2. 在 `InitD3D12` 中创建事件

在 `InitD3D12` 函数末尾，`g_thread = CreateThread(...)` 之前添加：

```cpp
// 创建事件
g_frameReadyEvent = CreateEventW(nullptr, FALSE, FALSE, nullptr);
g_frameDoneEvent = CreateEventW(nullptr, FALSE, FALSE, nullptr);
if (!g_frameReadyEvent) {
    Log("ERROR: CreateEvent failed");
    return false;
}
Log("Frame sync events created");
```

### 3. 修改 `nativeEndFrame` - 通知数据已准备好

找到 `nativeEndFrame` 函数，修改为：

```cpp
JNIEXPORT void JNICALL Java_com_dx12_DX12LibClient_nativeEndFrame(JNIEnv*, jclass) {
    // 通知 RenderLoop 线程：当前帧的顶点数据已经录制完成
    if (g_ok && g_frameReadyEvent) {
        SetEvent(g_frameReadyEvent);
    }
}
```

### 4. 修改 `RenderLoop` - 等待数据准备好

找到 `RenderLoop` 函数，修改主循环：

**原来的代码：**
```cpp
while (g_run) {
    Sleep(16);
    RepositionOverlay();
    CaptureMCFrame();
    // ... 清空和绘制 ...
}
```

**修改为：**
```cpp
while (g_run) {
    // 等待 Java 端通知数据已准备好（最多等待 100ms 避免完全卡死）
    DWORD waitResult = WaitForSingleObject(g_frameReadyEvent, 100);
    
    if (waitResult == WAIT_OBJECT_0) {
        // 数据已准备好，执行渲染
        RepositionOverlay();
        CaptureMCFrame();
        
        // ... 原有的渲染代码（清空 g_drawChunks 的部分保留）...
    } else if (waitResult == WAIT_TIMEOUT) {
        // 超时，跳过本帧（没有新数据）
        continue;
    } else {
        // 错误
        break;
    }
}
```

### 5. 保留 `RenderLoop` 中的清空逻辑

**重要**：`RenderLoop` 中清空 `g_drawChunks` 的代码**必须保留**，因为现在只有它负责清空：

```cpp
// 在 RenderLoop 中，执行绘制之前清空上一帧数据
EnterCriticalSection(&g_stateLock);
g_imVertCount = 0;
g_imVBSize = 0;
g_drawChunks.clear();      // 清空上一帧的数据
LeaveCriticalSection(&g_stateLock);
```

### 6. 修改 `nativeBeginFrame` - 保持空实现

```cpp
JNIEXPORT void JNICALL Java_com_dx12_DX12LibClient_nativeBeginFrame(JNIEnv*, jclass) {
    // Intentionally empty - RenderLoop thread handles frame cleanup and waits for data
}
```

### 7. 在 `CleanupD3D12` 中关闭事件句柄

在 `CleanupD3D12` 函数末尾添加：

```cpp
if (g_frameReadyEvent) { CloseHandle(g_frameReadyEvent); g_frameReadyEvent = nullptr; }
if (g_frameDoneEvent) { CloseHandle(g_frameDoneEvent); g_frameDoneEvent = nullptr; }
```

---

## 📝 完整的数据流

现在的正确流程：

```
1. RenderLoop 线程启动，等待 g_frameReadyEvent
2. Minecraft 渲染线程：
   a. BufferBuilderMixin 捕获 MeshData → processMeshData → nativeRecordVertices
   b. LevelRendererMixin 阻断 renderLevel → renderFullFrame
   c. renderFullFrame 调用 nativeEndFrame → SetEvent(g_frameReadyEvent)
3. RenderLoop 收到信号，开始渲染：
   a. 清空上一帧数据（g_drawChunks.clear()）
   b. 此时 g_drawChunks 包含当前帧录制的数据
   c. 遍历 g_drawChunks，执行 D3D12 绘制
   d. Present
   e. 回到步骤 1，继续等待下一帧
```

---

## 🚀 编译测试

```bash
cd D:\dx12-lib-template-26.1.2\src\main\native\windows
compile.bat
cd D:\dx12-lib-template-26.1.2
gradlew build
```

然后部署测试，按 F6 看是否能看到 D3D12 渲染的方块几何体。

如果还是黑色但有红色三角形，说明 `g_drawChunks` 可能仍然是空的。请在 `RenderLoop` 中添加日志：

```cpp
if (waitResult == WAIT_OBJECT_0) {
    Log("RenderLoop: frame ready, drawChunks=%zu", g_drawChunks.size());
    // ...
}
```

这样就能看到是否收到了数据。

请修改后测试，告诉我结果！
# GL4DX12 模组测试协作流程

> 本文档描述 Trae IDE 中的 AI（代码编写/编译）与豆包（测试部署/日志收集）的协作流程

## 协作角色

| 角色 | 负责内容 | 工具 |
|------|---------|------|
| **Trae AI** | 代码编写、编译构建、问题分析 | Trae IDE（IntelliJ 平台） |
| **豆包** | 部署到测试实例、启动游戏、收集日志、环境监控 | PCLCE 启动器、PowerShell 脚本 |
| **用户** | 沟通桥梁（Trae AI ↔ 豆包）、决策、审核代码 | 对话界面 |

## 测试环境信息

```
项目根目录:        D:\dx12-lib-template-26.1.2
测试实例:          D:\.minecraft\versions\26.1.2-Fabric_0.19.3\
测试实例 mods:     D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\
测试实例 dx12mod:  D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\
启动器:            PCLCE (PCL2_CE_Beta_x64.exe)
MC 版本:           26.1.2
Fabric 版本:       0.19.3
Java 版本:         25
GPU:               NVIDIA GeForce RTX 4070
```

## 通信机制

Trae AI 和豆包**无法直接通信**，所有消息传递通过用户中转。

### 标记文件机制（推荐）

Trae AI 在项目根目录创建标记文件，豆包通过检测标记文件触发操作：

| 标记文件 | 含义 | 自动清理 |
|---------|------|---------|
| `_ready_for_deploy.txt` | 编译完成，可以部署 | 部署后删除 |
| `_need_logs.txt` | 需要收集日志 | 日志收集后删除 |

### 用户中转（兜底）

当标记文件机制不适用时，Trae AI 或豆包通过用户传递消息。用户在对话中一句话即可转达。

## 协作流程

### 阶段 1：Trae AI 编译

1. 修改代码（Java / Rust）
2. 执行编译：
   - **Java 模组**: `cd fabric && .\gradlew.bat build`（产物：`fabric\build\libs\gl4dx12-0.1.0.jar`）
   - **Rust 原生库**:
     - 开发模式：`cd rust && cargo build`（产物：`rust\target\debug\wgpu_mc_jni.dll`）
     - 发布模式：`cd rust && cargo build --release`（产物：`rust\target\release\wgpu_mc_jni.dll`）
   - **⚠️ 两模块必须都编译成功才能部署**，单一模块失败应该停止
3. 记录当前 git commit hash：
   ```powershell
   git rev-parse HEAD > deploy_commit.txt
   ```
4. 编译成功后，通过用户通知豆包进行部署测试（或触发 `_ready_for_deploy.txt`）

### 阶段 2：豆包部署与启动

1. 检查 `_ready_for_deploy.txt` 或收到用户通知
2. 备份旧版本（如需回滚）：
   ```powershell
   Copy-Item "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\gl4dx12-0.1.0.jar" "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\gl4dx12-0.1.0.jar.bak"
   Copy-Item "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll" "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll.bak"
   ```
3. 执行部署脚本：
   ```powershell
   .\test-deploy.ps1 -LaunchPCL
   ```
4. PCLCE 启动后，手动点击启动按钮
5. 游戏启动后，豆包监控日志输出

### 阶段 3：测试与日志收集

1. 在游戏中进行测试操作（见下方测试 Checklist）
2. 遇到崩溃/异常时，退出游戏
3. 豆包执行日志收集：
   ```powershell
   .\test-collect-logs.ps1
   ```
4. 日志输出到项目根目录：`游戏日志 - 26.1.2-Fabric_0.19.3.log`

### 阶段 4：Trae AI 分析

1. Trae AI 读取项目根目录下的日志文件
2. 分析崩溃/异常原因
3. 修改代码，回到阶段 1

### 阶段 5：回滚（异常时）

如果部署后游戏无法启动或严重异常：

1. 豆包执行回滚：
   ```powershell
   Copy-Item "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\gl4dx12-0.1.0.jar.bak" "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\gl4dx12-0.1.0.jar"
   Copy-Item "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll.bak" "D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll"
   ```
2. 通知 Trae AI 回滚结果
3. Trae AI 分析异常日志，修复代码

## 关键路径速查

### 编译产物 & 部署路径

| 产物 | 构建命令 | 源路径 | 目标路径 |
|------|---------|-------|---------|
| Java 模组 JAR | `.\gradlew.bat build` | `fabric\build\libs\gl4dx12-0.1.0.jar` | `D:\.minecraft\versions\26.1.2-Fabric_0.19.3\mods\gl4dx12-0.1.0.jar` |
| Rust DLL (debug) | `cargo build` | `rust\target\debug\wgpu_mc_jni.dll` | `D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.dll` |
| Rust DLL (release) | `cargo build --release` | `rust\target\release\wgpu_mc_jni.dll` | 同上 |
| Rust PDB (debug) | `cargo build` | `rust\target\debug\wgpu_mc_jni.pdb` | `D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\wgpu_mc_jni.pdb` |
| 版本标记 | — | `deploy_commit.txt` | `D:\.minecraft\versions\26.1.2-Fabric_0.19.3\dx12mod\deployed_commit.txt` |

> **注意**：debug 和 release DLL 均部署到同一目标路径，最后一次部署的产物生效。

### 日志文件

| 日志 | 路径（相对测试实例） | 说明 |
|------|---------------------|------|
| 游戏主日志 | `logs\latest.log` | 最新运行日志 |
| Debug 日志 | `logs\debug.log` | 详细调试日志 |
| 崩溃报告 | `crash-reports\*.txt` | Java 崩溃报告 |
| **收集输出** | `游戏日志 - 26.1.2-Fabric_0.19.3.log` | 项目根目录，供 Trae AI 分析 |

## 脚本使用说明

### test-deploy.ps1 - 部署脚本

```powershell
# 完整部署（JAR + DLL）并启动 PCLCE
.\test-deploy.ps1 -LaunchPCL

# 只部署 JAR（仅 Java 改动时）
.\test-deploy.ps1 -SkipDll

# 只部署 DLL（仅 Rust 改动时）
.\test-deploy.ps1 -SkipJar

# 仅部署，不启动启动器
.\test-deploy.ps1
```

### test-collect-logs.ps1 - 日志收集脚本

```powershell
# 收集最新日志（默认）
.\test-collect-logs.ps1

# 收集所有日志（包括 debug 和历史）
.\test-collect-logs.ps1 -CollectAll

# 实时监控日志输出
.\test-collect-logs.ps1 -Watch

# 自定义输出文件名
.\test-collect-logs.ps1 -OutputName "测试日志-第3轮.log"
```

## 测试 Checklist

每次测试前确认部署状态：
- [ ] Java 模组已重新编译
- [ ] Rust DLL 已重新编译（如有改动）
- [ ] 部署脚本执行成功
- [ ] 部署前备份了旧版本（如需回滚）
- [ ] `deploy_commit.txt` 已写入测试实例
- [ ] 模组版本号与预期一致

游戏内测试项：
- [ ] 游戏能正常启动到主菜单
- [ ] 能正常创建/进入世界
- [ ] 渲染正常（无黑屏/花屏）
- [ ] 天空渐变正常（深蓝↔地平线颜色）
- [ ] HUD/UI 正常显示
- [ ] **白天/夜晚亮度有明显差异**
- [ ] **火把等方块光源效果正常**
- [ ] **洞穴/地下光照正确**
- [ ] **切换维度时光照贴图更新**
- [ ] 实体（动物/怪物）渲染可见
- [ ] 粒子效果可见
- [ ] 无明显性能问题（掉帧/卡顿）

## 常见问题

### 模组没加载？
1. 检查 `mods\gl4dx12-0.1.0.jar` 是否存在
2. 检查 Fabric API 是否安装在 mods 目录
3. 检查版本号是否匹配（Fabric 0.19.3 + MC 26.1.2）
4. 查看 `latest.log` 中是否有 `GL4DX12 Mod initializing...` 日志

### DLL 加载失败？
如果日志中出现 `UnsatisfiedLinkError` 或 `Can't find native library`：
1. 检查 `dx12mod\wgpu_mc_jni.dll` 是否存在
2. 检查 DLL 架构是否为 x64（`dumpbin /headers wgpu_mc_jni.dll | Select-String "machine"`）
3. 确认 DLL 是 debug 还是 release 构建（debug / release 的运行时依赖不同）
4. 查看日志中的 `[D3D12Bridge]` 相关输出

### 游戏崩溃？
1. 收集 `crash-reports\` 目录下的崩溃报告（`.txt` 文件）
2. 查看 `latest.log` 中的异常栈
3. 常见错误：
   - `ExceptionInInitializerError` → 静态初始化失败，通常由 JNI 链接问题导致
   - `UnsatisfiedLinkError` → DLL 加载失败
   - TDR 超时 → GPU 驱动重置，检查 GL/D3D12 HWND 争用

### 构建状态不一致？
- 如果 Rust build 通过但 Java build 失败：**不部署**，通知 Trae AI
- 如果 Java build 通过但 Rust build 失败：**不部署**，通知 Trae AI
- 只有两模块都编译成功才进行部署

## 通信约定

### Trae AI → 豆包（通过用户中转）

| Trae AI 说 | 含义 |
|-----------|------|
| "部署测试" / "部署到测试实例" | 执行部署脚本 |
| "启动测试" / "开始测试" | 部署 + 启动 PCLCE |
| "收集日志" / "拿日志" | 收集日志到项目目录 |
| "监控日志" | 实时监控日志输出 |
| "需要回滚" | 恢复到上一个备份版本 |

### 豆包 → Trae AI 的交付物

- 日志文件：`游戏日志 - 26.1.2-Fabric_0.19.3.log`
- 状态报告：
  - 部署是否成功
  - 游戏是否启动到主菜单
  - 是否进入世界
  - 是否崩溃（崩溃时附上 crash-report 路径）
  - 关键日志行摘要（如 `[dx12-wm]` 输出、错误/警告）


## 一、源码强制修复：给全部JNI入口加前置空指针校验（必做）
### 通用校验模板，每个JNI函数最顶部插入
```cpp
// 所有nativeUpload/nativeRender系列函数第一行统一添加
if (!g_device || !g_commandQueue || !g_rtvHeap)
{
    LOG("WARN: JNI function called before D3D12 full init, skip execute");
    return;
}
// 有返回值(long/int)的函数替换为 return 0;
```
### 需要补全校验的全部函数清单
1. 纹理模块
`nativeUploadTexture`、`nativeBindTexture`
2. 实体模块
`nativeUploadEntities`、`nativeRenderEntities`
3. 液体模块
`nativeUploadLiquid`、`nativeRenderLiquid`
4. 天空/日月
`nativeSetSkyParameters`、`nativeRenderSky`
5. 半透明
`nativeUploadTransparent`、`nativeRenderTransparent`
6. 原有粒子
`nativeUploadParticles`、`nativeRenderParticles`

### 示例修改（nativeUploadTexture）
```cpp
JNIEXPORT jlong JNICALL Java_com_dx12_DX12LibClient_nativeUploadTexture
(JNIEnv* env, jclass clazz, jint width, jint height, jbyteArray data) {
    // 新增初始化校验
    if (!g_device || !g_commandQueue || g_nextSrvOffset == UINT_MAX)
    {
        LOG("ERROR: Texture upload skip, D3D uninitialized");
        return 0;
    }
    // 原有业务代码不变
}
```

## 二、修复纹理创建异步同步漏洞（第二崩溃诱因）
原纹理上传仅创建临时命令列表但**未等待GPU完成资源转换**，主线程立刻绑定未就绪SRV导致空访问。
修改`nativeUploadTexture`尾部同步逻辑：
```cpp
// 原临时cmd执行后强制等待GPU完成
g_commandQueue->ExecuteCommandLists(1, ppTempCmd);
// 新增栅栏等待，必须等拷贝+资源转换完成才能返回纹理ID
WaitForPreviousFrame();
// 再存入全局纹理map，避免上层立刻使用未就绪资源
{
    std::lock_guard<std::mutex> lock(g_textureMutex);
    g_textures[texId] = std::move(tex);
}
```

## 三、修复渲染循环空对象保护
渲染循环最开头增加全局设备存活判断，一旦设备销毁直接跳过整帧绘制：
```cpp
// RenderLoop 第一行
if (!g_device)
{
    Sleep(16);
    continue;
}
```

## 四、修复结构体成员访问潜在空崩
1. `ParticleDrawCall`/`TransparentDrawCall`/`EntityDrawCall`全部确保`VertexType type`初始化默认值
结构体构造处赋值默认`VERTEX_TYPE_UNKNOWN`，避免未定义内存读取
```cpp
struct ParticleDrawCall {
    ComPtr<ID3D12Resource> uploadBuffer;
    UINT vertexCount;
    UINT vertexSize;
    D3D12_GPU_VIRTUAL_ADDRESS gpuAddress;
    VertexType type = VERTEX_TYPE_UNKNOWN; // 默认初始化
};
```
2. 遍历绘制队列前判断`vertexCount > 0`，跳过空绘制任务
```cpp
for (auto& dc : g_entityDrawCalls)
{
    if (dc.vertexCount == 0) continue;
    // 绘制逻辑
}
```

## 五、编译&覆盖步骤
1. 全量替换修复上述所有代码
2. 清理编译产物重新编译
```powershell
cd src\main\native\windows
del *.obj *.lib *.exp *.dll
.\compile.bat
cd ../../../../
.\gradlew clean build
```
3. 替换mods内旧`gl4dx12.dll`与mod jar
4. 清空DX12缓存文件夹：`.minecraft\.gl4dx12_cache`

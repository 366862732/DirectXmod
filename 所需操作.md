
## 目标文件
`d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp`
## 全局前置统一引入（所有修改依赖）
文件最顶部追加头文件
```cpp
#include <wrl.h>
using namespace Microsoft::WRL;
// 设备有效性工具函数，全局可用
inline bool IsDeviceValid()
{
    return g_dev.Get() != nullptr;
}
// 全局原子设备丢失标记，线程安全
std::atomic<bool> g_deviceLost = false;
```
## 执行顺序（分阶段执行，每阶段编译校验无报错再下一阶段）
### 阶段1 Task1 重写AUTO-CORRECT坐标判断（最高优先级，阻断当前直接崩溃诱因）
1. 全局检索`AUTO-CORRECT`全部判定代码块
2. 删除**强制修改coordType**逻辑，Java传入coordType永久最高优先级
3. 新增疑似UI顶点计数日志、警告日志，仅诊断不篡改管线
4. 代码模板替换原有判断：
```cpp
float zDelta = zMax - zMin;
bool allXYInScreen = 全部顶点XY在屏幕区间;
static int suspectUICount = 0;
if (allXYInScreen && fabs(zDelta) < 0.001f)
{
    suspectUICount++;
    char warnBuf[512];
    sprintf_s(warnBuf, "[AUTO-CORRECT WARN#%d:顶点XY屏幕平面、Z无变化，Java传入coordType=%d，不自动切换管线", suspectUICount, g_currentCoordType);
    OutputDebugStringA(warnBuf);
}
else
{
    OutputDebugStringA("[AUTO-CORRECT INFO:顶点包含深度，使用传入坐标类型渲染");
}
// 管线切换仅依赖Java传入值，不受顶点自动检测干扰
if(g_currentCoordType == 1)
{
    // 正交UI管线
}
else
{
    // 透视3D管线
}
```
5. 修改完成后：VS x64 Release重新生成，确认无编译报错再进入阶段2

### 阶段2 Task4 前置：全局设备校验+Present设备丢失捕获（兜底防崩溃）
1. 在`RenderLoop`while循环最开头增加设备/丢失校验
```cpp
while(g_run)
{
    if(g_deviceLost.load() || !IsDeviceValid())
    {
        OutputDebugStringA("[FATAL]设备已销毁，终止渲染循环");
        break;
    }
    // 原有循环逻辑
}
```
2. 定位`g_swap->Present(1, 0)`代码段，完整包裹错误捕获+设备标记
```cpp
HRESULT presentHr = g_swap->Present(1, 0);
if(FAILED(presentHr))
{
    HRESULT devErr = g_dev->GetDeviceRemovedReason();
    char errBuf[512];
    sprintf_s(errBuf, "[FATAL]Present失败 错误码0x%08X", devErr);
    OutputDebugStringA(errBuf);
    // 标记全局设备丢失，所有线程终止
    g_deviceLost.store(true);
    g_run = false;
    break;
}
```
3. 所有JNI导出函数（nativeRecordVertices/nativeInit等）函数第一行增加校验
```cpp
JNIEXPORT void JNICALL nativeRecordVertices(...)
{
    if(g_deviceLost.load() || !IsDeviceValid())
    {
        env->ThrowNew(env->FindClass("java/lang/RuntimeException"),"D3D设备已丢失，渲染停止");
        return;
    }
    // 原有顶点接收逻辑
}
```
4. 编译校验无报错，进入阶段3

### 阶段3 Task2 分模块全局替换D3D裸指针为ComPtr（分批替换，避免批量报错）
替换规则：
1. 仅替换**全局D3D资源**（device、交换链、根签名、PSO、缓冲、命令队列/列表、堆）
2. 删除所有手动`ptr->Release();`代码，ComPtr自动生命周期管理
3. 创建API传地址统一使用`.GetAddressOf()`
    示例：
    ```cpp
    // 旧
    ID3D12Device* g_dev;
    CreateDevice(..., &g_dev);
    if(g_dev) g_dev->Release();
    // 新
    ComPtr<ID3D12Device> g_dev;
    CreateDevice(..., g_dev.GetAddressOf());
    // 无需手动Release
    ```
分批替换步骤：
1. 交换链、设备、命令队列/列表/分配器 → 编译校验
2. RTV堆、描述符堆、同步栅栏 → 编译校验
3. 根签名、3D/UI两套PSO → 编译校验
4. 顶点/常量/上传全部缓冲 → 编译校验
每批替换后必须完整重新生成，出现报错即时修正，全部全局资源替换完成后进入阶段4

### 阶段4 Task3 重构初始化状态机（12阶段分帧异步加载，根除TDR GPU超载）
1. 全局枚举替换旧单变量InitStage，扩充至12阶段
```cpp
enum InitStage
{
    STAGE_EMPTY = 0,
    STAGE_BASE_DEVICE,        // 基础设备/交换链
    ST_CMD_RES,               // 命令队列分配器
    ST_UI_PSO_CBUF,           // UI管线+UI常量缓冲
    ST_3D_PSO_CBUF,           // 3D透视管线+世界常量缓冲
    ST_SKY_TEX,               // 天空盒纹理上传
    ST_SKY_VB,                // 天空顶点缓冲
    ST_ENTITY_STATIC_VB,      // 实体静态顶点
    ST_INSTANCE_BUFFER,       // 实例缓冲
    ST_PARTICLE_RES,          // 粒子资源
    ST_TEXTURE_POOL,          // 通用贴图池
    ST_ALPHA_PSO,             // 半透明管线
    ST_FULL_READY             // 全部初始化完成
};
InitStage g_initStage = STAGE_EMPTY;
```
2. RenderLoop循环开头新增状态机分支，每一阶段仅执行一类资源创建，执行完强制栅栏等待GPU完成再进入下一阶段
```cpp
while(g_run)
{
    if(g_deviceLost.load() || !IsDeviceValid()) break;
    if(g_initStage < ST_FULL_READY)
    {
        char stageLog[256];
        sprintf_s(stageLog,"[INIT STATE MACHINE] 当前阶段:%d",g_initStage);
        OutputDebugStringA(stageLog);
        // 分阶段资源创建函数ProcessNextInitStage();
        ProcessNextInitStage();
        // 强制GPU完全同步，分散负载
        WaitForGpu();
        g_initStage++;
        // 本帧仅初始化，跳过正式渲染
        continue;
    }
    // 原有正常渲染逻辑
}
```
3. 剥离`nativeInit`中所有重型资源创建，仅保留窗口、工厂基础初始化，所有PSO/纹理/缓冲全部迁移到状态机`ProcessNextInitStage`内部
4. 每阶段执行完成打印阶段日志，方便DebugView定位卡顿阶段；编译无报错进入最后收尾阶段

### 阶段5 Task4 后半部分 完整SafeCleanD3D安全销毁函数（收尾兜底）
1. 新增全局资源清理函数，设备丢失、JNI销毁统一调用
```cpp
void SafeCleanD3D()
{
    OutputDebugStringA("[CLEANUP] 开始安全销毁全部D3D资源");
    // 1. 标记渲染线程停止
    g_run = false;
    g_deviceLost.store(true);
    // 2. 等待渲染线程退出（可搭配事件等待）
    WaitForSingleObject(g_renderThreadHandle, 3000);
    // 3. ComPtr自动释放所有D3D资源，无需手动Release
    g_dev.Reset();
    g_swap.Reset();
    // 4. 关闭同步事件、窗口附属资源
    CloseHandle(g_frameReadyEvent);
    CloseHandle(g_frameDoneEvent);
    OutputDebugStringA("[CLEANUP] D3D资源销毁完成");
}
```
2. 两处自动调用入口：
   - Present捕获设备丢失后直接调用`SafeCleanD3D();`
   - JNI`nativeShutdown`函数第一行调用`SafeCleanD3D();`
3. 最终完整重新生成DLL，复制到`src/main/resources`，执行`.\gradlew clean build`打包

## 全部改造完成后测试验证清单
1. DebugView不再出现`D3D12: Removing Device`
2 AUTO-CORRECT仅警告，不会擅自切换3D/UI投影管线
3 无`0x00000000内存不能为written`弹窗
4 事件查看器不再生成nvlddmkm EventID153报错
5 游戏长时间运行稳定，无显卡驱动重置、程序闪退
6 设备丢失后优雅清理资源，Java调用JNI不会抛出空指针异常当前文件内容过长，豆包只阅读了前 48%。
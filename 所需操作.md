# 整合完整版、带前置校验、规避全部风险的标准化执行手册
## 前置硬性要求（执行前必做）
1. 备份 `d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp` → `d3d12bridge.cpp.bak`
2. VS 切换配置：x64 Release；确认项目包含 DirectX12 SDK、WRL 头文件可正常引入
3. 前置添加函数前向声明（文件头部include区域下方）
```cpp
void SafeCleanD3D();
void ProcessNextInitStage();
```
4. 执行规则：**每完成单个Stage/批次必须完整重新生成解决方案，零报错再推进下一环节**
5. ComPtr仅处理全局`g_*`变量，函数局部、入参裸指针一律保留不改动

---
# 分阶段执行指令（按顺序）
## Stage0：基础头文件、全局ComPtr、设备检测（最低风险，优先）
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: #include <d3d12.h>
replaceText: #include <d3d12.h>
#include <wrl.h>
#include <atomic>
#include <cstdio>
using namespace Microsoft::WRL;

// 前向声明
void SafeCleanD3D();
void ProcessNextInitStage();

// 全局设备状态原子标记
std::atomic<bool> g_deviceLost = false;

// 全局D3D核心智能指针（删除原有裸指针全局定义）
ComPtr<ID3D12Device> g_dev;
ComPtr<IDXGISwapChain1> g_swap;
ComPtr<ID3D12CommandQueue> g_cmdQueue;
ComPtr<ID3D12CommandAllocator> g_cmdAlloc;
ComPtr<ID3D12GraphicsCommandList> g_cmdList;

// 设备有效性检测（放在g_dev声明后方，消除前向引用报错）
inline bool IsDeviceValid()
{
    return g_dev.Get() != nullptr;
}
### Stage0校验动作
1. 删除文件中原 `ID3D12Device* g_dev;` 等同名裸指针全局变量
2. 编译，确认无未定义标识符、头文件缺失报错

## Stage1：修复崩溃根源——AUTO-CORRECT自动管线逻辑（核心修复）
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: bool allXYInScreen = ...;
float zDelta = zMax - zMin;
if(allXYInScreen && fabs(zDelta) < 0.001f)
{
    g_currentCoordType = 1;
}
replaceText: bool allXYInScreen = ...;
float zDelta = zMax - zMin;
static std::atomic<int> warnCount{0};
if(allXYInScreen && fabs(zDelta) < 0.001f)
{
    int cnt = warnCount.fetch_add(1);
    char logBuf[512] = {0};
    sprintf_s(logBuf, "[AUTO-CORRECT WARN#%d:平面UI顶点，Java传入coordType=%d，不再自动强制切换渲染管线", cnt, g_currentCoordType);
    OutputDebugStringA(logBuf);
}
else
{
    OutputDebugStringA("[AUTO-CORRECT INFO:顶点存在深度差值，沿用Java上层指定3D管线");
}
// 关键修复：移除自动覆盖coordType代码，渲染管线完全由Java侧控制
### Stage1校验动作
编译，打开GUI界面，DebugView可输出对应WARN日志，不再触发0xc0000005空指针崩溃

## Stage2：渲染线程设备防护、Present异常处理（分2个子步骤）
### 2-1 渲染循环头部失效拦截
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: DWORD RenderThreadProc(LPVOID param)
{
    while(g_run)
    {
replaceText: DWORD RenderThreadProc(LPVOID param)
{
    while(g_run)
    {
        // 设备失效直接终止渲染
        if(g_deviceLost.load() || !IsDeviceValid())
        {
            OutputDebugStringA("[FATAL D3D: 检测到设备失效，退出渲染循环");
            break;
        }
### 2-2 Present失败优化（解决线程死锁，自动触发清理）
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: HRESULT hr = g_swap->Present(0, 0);
if(FAILED(hr))
{
    g_deviceLost.store(true);
    g_run = false;
    break;
}
replaceText: HRESULT hr = g_swap->Present(0, 0);
if(FAILED(hr))
{
    char errLog[512] = {0};
    sprintf_s(errLog, "[FATAL D3D Present 失败，错误码0x%08X，执行安全资源清理", hr);
    OutputDebugStringA(errLog);
    g_deviceLost.store(true);
    g_run = false;
    // 唤醒阻塞等待事件，避免线程互相等待死锁
    SetEvent(g_frameReadyEvent);
    SetEvent(g_frameDoneEvent);
    SafeCleanD3D();
    break;
}
### 2-3 JNI函数防护（手动模板，禁止批量正则）
每个native渲染函数函数体最开头粘贴：
```cpp
if(g_deviceLost.load() || !IsDeviceValid())
{
    env->ThrowNew(env->FindClass("java/lang/RuntimeException"), "D3D渲染设备丢失，API调用无效");
    return;
}
```
优先级覆盖：nativeRecordVertices > nativeDraw > nativeSetMvp > 其余纹理/粒子渲染接口

## Stage3：分批替换全局ComPtr（仅g_全局，3批次迭代，每批编译）
### 批次3-1 核心交换链/设备创建修正
全局已替换ComPtr，修改所有D3D创建API传参：
原 `IID_PPV_ARGS(&g_dev)` → `IID_PPV_ARGS(g_dev.GetAddressOf())`
全局删除所有 `g_dev->Release();`、`g_swap->Release();` 手动释放代码
### 批次3-2 命令队列、分配器、列表同规则修正
### 批次3-3 全局PSO、根签名、顶点缓冲、栅栏全局变量替换
限制：仅文件顶层`ComPtr<xxx> g_xxx;`，函数内部局部ID3D12*、函数入参不改动
### Stage3校验
无类型不匹配编译报错，运行资源创建逻辑正常

## Stage4：12阶段异步初始化状态机改造
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: int g_initStage = 0;
replaceText: enum InitStage
{
    ST_EMPTY = 0,
    ST_BASE_DXGI,
    ST_CMD_FAMILY,
    ST_UI_PSO_CBUF,
    ST_3D_PSO_CBUF,
    ST_SKY_TEX,
    ST_SKY_VB,
    ST_ENTITY_STATIC_VB,
    ST_INSTANCE_BUFFER,
    ST_PARTICLE_RES,
    ST_TEXTURE_POOL,
    ST_ALPHA_PSO,
    ST_FULL_READY
};
InitStage g_initStage = ST_EMPTY;
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: if(g_initStage < 12)
{
    ProcessInit();
}
replaceText: if(g_initStage < ST_FULL_READY)
{
    char stageLog[256] = {0};
    sprintf_s(stageLog, "[INIT_STATE_MACHINE] 当前初始化阶段:%d", g_initStage);
    OutputDebugStringA(stageLog);
    ProcessNextInitStage();
    WaitForGpu();
    g_initStage = (InitStage)(g_initStage + 1);
    continue;
}
### Stage4校验
实现`ProcessNextInitStage()`分阶段逻辑，DebugView打印完整阶段日志，无资源半初始化野指针

## Stage5：SafeCleanD3D安全销毁（防线程死锁）
toolName: edit_file_search
filePath: d:\dx12-lib-template-26.1.2\src\main\native\windows\d3d12bridge.cpp
searchText: void nativeCleanup(JNIEnv* env, jobject thiz)
replaceText: void SafeCleanD3D()
{
    OutputDebugStringA("[CLEANUP] 启动D3D资源安全释放流程");
    g_run = false;
    g_deviceLost.store(true);
    // 唤醒阻塞事件，消除线程死锁
    SetEvent(g_frameReadyEvent);
    SetEvent(g_frameDoneEvent);
    // 限时2秒等待渲染线程，超时不阻塞主程序
    DWORD waitRet = WaitForSingleObject(g_renderThreadHandle, 2000);
    if(waitRet == WAIT_TIMEOUT)
    {
        OutputDebugStringA("[WARN CLEANUP: 渲染线程2秒未退出，跳过等待直接释放资源");
    }
    // ComPtr自动释放，无需手动调用Release
    g_dev.Reset();
    g_swap.Reset();
    g_cmdQueue.Reset();
    g_cmdAlloc.Reset();
    g_cmdList.Reset();
    // 关闭同步内核事件句柄
    CloseHandle(g_frameReadyEvent);
    CloseHandle(g_frameDoneEvent);
    OutputDebugStringA("[CLEANUP] D3D全部资源释放完成");
}
void nativeCleanup(JNIEnv* env, jobject thiz)
{
    SafeCleanD3D();
}

# 全流程收尾校验任务
toolName: todo_write
todo:
1. VS x64 Release完整重新生成，0编译错误、0警告
2. 编译输出gl4dx12.dll替换游戏natives目录
3. 启动游戏，打开GUI界面复现原崩溃场景
4. DebugView观测三类日志：初始化阶段、AUTO-CORRECT管线提示、设备失效拦截日志
5. 长时间运行验证：无JVM EXCEPTION_ACCESS_VIOLATION弹窗、无nvlddmkm驱动重置、退出程序无线程卡死
6. 模拟设备丢失场景，程序自动清理资源优雅关闭
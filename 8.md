
## 🔧 立即修复

### 方案：检查并修复坐标提取

在 `processMeshData` 中，`vertices` 数组只存储了位置数据，但**没有经过任何坐标空间转换**。MC的 `MeshData` 中存储的顶点坐标可能是在**模型空间**或**视图空间**，而不是世界空间。

### 具体修改

在 `processMeshData` 中，提取顶点位置后，添加日志输出原始坐标范围：

```java
// 在提取顶点数据后，添加日志
float minX = Float.MAX_VALUE, maxX = -Float.MAX_VALUE;
float minY = Float.MAX_VALUE, maxY = -Float.MAX_VALUE;
float minZ = Float.MAX_VALUE, maxZ = -Float.MAX_VALUE;
for (int i = 0; i < vertexCount; i++) {
    float x = vertices[i * 3], y = vertices[i * 3 + 1], z = vertices[i * 3 + 2];
    if (x < minX) minX = x; if (x > maxX) maxX = x;
    if (y < minY) minY = y; if (y > maxY) maxY = y;
    if (z < minZ) minZ = z; if (z > maxZ) maxZ = z;
}
System.out.println("[GL4DX12] Vertex range: X[" + minX + ", " + maxX + "] Y[" + minY + ", " + maxY + "] Z[" + minZ + ", " + maxZ + "]");
```

